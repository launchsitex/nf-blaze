# Changelog

> ## 🔴 כלל מחייב #1 — כל שינוי חייב להיות מתועד כאן. בלי יוצא מן הכלל.
>
> **כל פעולה או שינוי בפרויקט נרשם בקובץ הזה** — קוד, הגדרות, תשתית,
> פריסות, סקריפטים, תיעוד, ושינויים באתר. אין «שינוי קטן מדי». אין git
> בפרויקט, ולכן **הקובץ הזה הוא הזיכרון היחיד של מה נעשה ולמה**.
>
> **מתי:** בסוף כל שינוי, לא בסוף היום. לפני שאומרים «סיימתי» —
> לוודא שכל מה שנגעו בו מאז הערך האחרון מופיע בו.
>
> ⚠️ **המלכודת שכבר תפסה אותנו:** נכתב ערך, ואז נמשכה העבודה ונוספו
> שינויים — והם לא נוספו לערך. **תמיד לבדוק אילו קבצים השתנו מאז,
> ולא להסתמך על הזיכרון.**
>
> **מה נרשם בכל ערך:** מה השתנה, **למה** (זו החשובה — אחרי חודש רק היא
> נשארת רלוונטית), ואיך זה אומת. גם החלטות תכנון ומגבלות ידועות —
> הקובץ הזה פנימי וכן לחלוטין.
>
> ## 🔴 כלל מחייב #2 — בכל עדכון גרסה: לעדכן את הקישור באתר ולהזכיר לנתי להעלות לשרת.
>
> גרסה חדשה לא «יוצאת» כשהמתקין נבנה — היא יוצאת כשהיא **על השרת**.
> עד אז אף לקוח לא מקבל אותה, וכפתור ההורדה באתר מפנה לקובץ ישן.
>
> **האחריות מחולקת כך:**
>
> | מי | מה |
> |----|----|
> | **הסוכן** | לעדכן את קישור ה-EXE בדף הנחיתה — **חובה, בכל גרסה, בלי להזכר** |
> | **הסוכן** | **לומר לנתי במפורש להעלות לשרת** בסיום כל בנייה. לא להניח שהוא זוכר |
> | נתי | להעלות בפועל: `public_html/download/` + `public_html/config.js` |
>
> **מה שהופך את זה לחסין שכחה** (ולא מסתמך על זיכרון):
> `npm run release` מעדכן לבד את `downloadUrl` ב-`landing/config.js`
> ומדפיס בסוף צ׳ק-ליסט של מה להעלות ולאן. הכפתור באתר ממילא קורא את
> `/download/version.json` בזמן ריצה ובונה ממנו את הקישור.
>
> ⚠️ **סדר ההעלאה קריטי:** קודם ה-exe וה-blockmap, ורק כשהם עלו במלואם —
> `latest.yml` ו-`version.json`. הם מכריזים על הגרסה וחוסמים מיידית כל
> לקוח על גרסה ישנה; הכרזה על קובץ שעוד באמצע העלאה נועלת לקוחות עד
> שהיא תסתיים. הנוהל המלא: `RELEASE_UPLOAD.md`.
>
> ## 🔴 כלל מחייב #3 — הקובץ הזה פנימי. אין לחשוף פגמים או סודות למשתמשים.
>
> ‏CHANGELOG.md מיועד לנו בלבד, והוא מפורט וכן בכוונה — כולל תיאורי באגים,
> פרצות שנסגרו ומגבלות ידועות. **אסור להעתיק ממנו טקסט אל מול המשתמש.**
>
> מה שהמשתמש רואה בפופ-אפ «מה חדש» מגיע **רק** מ-`src/shared/release_notes.ts`,
> שהוא הגרסה המסוננת. שם — ובכל טקסט אחר שמגיע ללקוח (ממשק, הודעות שגיאה,
> האתר, מיילים) — **אסור** שיופיע:
>
> - תיאור פרצה, באג אבטחה או עקיפה שתוקנה — זה מלמד תוקף בדיוק מה לחפש
>   בגרסאות ישנות שעדיין מותקנות אצל לקוחות אחרים.
> - שמות קבצים, נתיבים, שמות פונקציות, ערוצי IPC או מבנה קוד.
> - כתובות שרתים, מזהי פרויקטים, שמות סודות או פורמטים של מפתחות.
> - מגבלות ידועות, חוב טכני, או «מה עוד לא עובד».
> - ניסוח שמודה בכשל («תוקן באג שגרם לאובדן נתונים»).
>
> תיקון אבטחה מנוסח כשיפור: «הידוק האבטחה של חיבור החשבונות» — בלי מה,
> בלי איפה, ובלי מה היה אפשר לעשות קודם. גם הודעות שגיאה בממשק נשארות
> ברמת «מה לעשות עכשיו», בלי stack trace ובלי נתיבים.
>
> בדיקה אוטומטית אוכפת את זה על `release_notes.ts` (‏`tests/update_gate.test.ts`).
>
> ---
>
> שלושת הכללים נוספו ב-01.08.2026 לבקשת נתי. הם קודמים לכל שיקול אחר.

## 1.66.0 - 2026-08-02

### Feature — מרכז עזרה בתוך המערכת וערוץ משוב ישיר ללקוחות

נתי ביקש (02.08.2026) שני כפתורים בסרגל העליון: «עזרה» שמציג מידע על הגרסה
ומדריך שימוש כמו במערכות גדולות, ו«משוב/דיווח» שדרכו לקוחות שולחים משוב או
מדווחים על באג — והפניות מגיעות לפורטל הרישיונות, לטאב חדש, מסווגות לפי סוג
ועם התאריך, השעה ושם בעל הרישיון.

**מה נוסף באפליקציה**

- `src/shared/help.ts` — תוכן המדריך כדאטה מובנית: 8 נושאים (התחלה מהירה,
  מצבי עבודה, תצוגה חיה ובחירת אלמנטים, שליטה בשינויים, כלי עבודה, חיבורים
  ופרסום, אבטחה ופרטיות, פתרון תקלות) + קיצורי מקלדת. **הקובץ מסומן
  כלקוח-פונה בראשו** ונאכף ב-`tests/help_content.test.ts` — אותה רשימת
  דפוסים אסורים שמגינה על `release_notes.ts` (נתיבים, כתובות, ערוצי IPC,
  לשון «פרצה/דליפה/עקיפה»). בלי הבדיקה הזו, כל הרחבה עתידית של המדריך
  הייתה נקודת דליפה שקטה, כי המדריך נקרא בדיוק כמו הערות הגרסה.
- `HelpModal.tsx` — חלון 920px עם תפריט נושאים לצד תוכן. מסך «אודות» מרכז
  גרסה, מצב רישיון ותוקף, שם בעל הרישיון, תיקיית הנתונים (עם כפתור פתיחה),
  קיצורי מקלדת וקישורים לאתר/מדריכים/תנאים/פרטיות/נגישות.
- `FeedbackModal.tsx` — בחירת סוג (תקלה / שיפור / משוב / שאלה) עם הסבר מה
  מתאים לכל אחד, כותרת, פירוט עם מונה תווים, ואימייל לחזרה (לא חובה).
  שדה השם **אינו קיים ואינו ניתן לעריכה** — ראה למטה.
- `App.tsx` — «עזרה» (אייקון בלבד, `.btn-icon` חדש) ו«משוב». **העזרה זמינה
  גם בלי רישיון** בכוונה: מי שתקוע במסך הרישיון צריך דרך להבין מה המערכת
  עושה ואיך פונים אלינו. **המשוב מוצג רק עם רישיון פעיל**, כי הפנייה
  מזוהה מולו ובלעדיו אין למי לשייך אותה.
- `feedback.ts` (main) — שולח ל-Edge Function. לא בולע שגיאות ספק: 429
  מחזיר את הודעת מגבלת הקצב, 400 את הודעת האימות, וכל השאר הודעה ידידותית
  עם כתובת מייל חלופית — כדי שלקוח לא יישאר בלי דרך ליצור קשר.
- הערוץ `feedback:submit` **אינו** ב-`LICENSE_FREE_CHANNELS` — שומר ה-IPC
  חוסם אותו בלי רישיון, כמו כל שאר הערוצים.

**איך נמנעת התחזות (ההחלטה המרכזית כאן)**

הלקוח **לא** מצהיר על שמו. האפליקציה שולחת את **מפתח הרישיון החתום**,
בדיוק כמו ב-`validate-license`, וה-Edge Function מאמתת את חתימת ה-Ed25519
ומחלצת ממנה את `id` ואת השם. השם נלקח מ-`nfb_licenses` לפי המזהה (כך שינוי
שם בפורטל משתקף בפניות), ורק אם הרשומה נמחקה נופלים לשם שבתוך המפתח.
האלטרנטיבה — לשלוח שם מהקליינט — הייתה מאפשרת לכל אחד לשלוח פניות בשם
לקוח אחר, ולהפוך את התיבה לחסרת ערך. המפתח נקרא ב-`getStoredLicenseKey()`
בתהליך הראשי בלבד ולעולם אינו חוצה IPC ל-renderer.

**מה נוסף בפורטל**

- מיגרציה `0006_user_feedback.sql` — טבלת `nfb_user_feedback`. `license_id`
  ו-`customer_name` הם קביעת שרת ולא הצהרת לקוח. `insert` נשלל מ-`anon`
  ומ-`authenticated`, כך שהכנסה אפשרית רק ב-`service_role` דרך הפונקציה —
  אותו דפוס כמו `nfb_license_requests`. `customer_name` נשמר כתמונת מצב
  ולא כ-join, כדי שפנייה תישאר קריאה גם אחרי מחיקת הרישיון.
- Edge Function `submit-feedback` — אימות חתימה לפני כל פנייה למסד (חוסם
  ניחוש מזהים), אימות קלט ואורכים זהים לאלה שבאפליקציה, ומגבלת קצב של
  5 פניות ל-10 דקות **לפי מזהה רישיון** (ולא לפי IP: כאן יש זהות מאומתת,
  והיא מדד טוב יותר).
- טאב **«משוב ממשתמשים»** — סינון כפול (סוג × סטטוס), מונה פניות חדשות על
  הטאב, תאריך **ושעה**, סביבה (גרסה + מערכת הפעלה), אימייל לחיצה-לשליחה,
  וסטטוס חדשה/בטיפול/טופל + מחיקה. פנייה ארוכה מקוצרת עם «הצג הכל».

⚠️ **באג שנמנע תוך כדי:** ה-handler של מסנני הבקשות היה
`document.querySelectorAll('.filters button')` — סלקטור גלובלי. הוספת סרגל
סינון שני היה מחווט גם אותו לאותו handler ושובר את טאב הבקשות. שונה
ל-`#req-filters button`, ולסרגל הבקשות נוסף `id`.

⚠️ **אין התראה על פנייה חדשה** — כמו בבקשות הרישיונות, צריך להיכנס לפורטל.

**אימות**

- `npm run typecheck` — עובר (main + renderer).
- `npx vitest run tests/help_content.test.ts` — 6 בדיקות, עוברות.
- הפורטל נטען בדפדפן ללא שגיאות קונסול; נבדק ידנית עם שורות דמה: 3 טאבים,
  החלפה ביניהם, קיצור הודעה ארוכה (76px מתוך 149px), כפתורי פעולה בשורה
  אחת, ופריסת כרטיסים במובייל (375px) בלי גלילה אופקית.
- שני החלונות נבדקו ויזואלית מחוץ ל-Electron (סטאב של הגשר): מרכז העזרה
  920px עם תפריט 224px מימין ותוכן 694px שנגלל, מעבר בין נושאים, וטופס
  המשוב — כפתור השליחה חסום עד שיש כותרת ופירוט, מונה תווים, ומסך הצלחה.
- ⚠️ **לא אומת בתוך Electron עצמו** (אין רישיון פעיל בסביבת הפיתוח כאן).
  לפני שחרור — לפתוח את שני החלונות באפליקציה ולוודא שהם נראים כמצופה,
  ולשלוח פנייה אחת מקצה לקצה אחרי פריסת הפונקציה והמיגרציה.

**תשתית**

- `.claude/launch.json` — נוספה תצורת `license-portal` (פורט 4322) לצד
  `landing`, כדי שאפשר יהיה להריץ את הפורטל מקומית לבדיקה. אם היא מיותרת —
  אפשר למחוק אותה בלי שום השפעה.
- `.nf-ui-check/` — תיקיית בדיקה ויזואלית זמנית **נמחקה** בסיום. אינה בריפו.

**נדרש לפני שהתכונה עובדת בפועל** (מפורט ב-`license-portal/README.md`):
הרצת המיגרציה, `supabase functions deploy submit-feedback --no-verify-jwt`,
והעלאת `index.html` המעודכן לאחסון הפורטל.

**סטטוס פריסה (02.08.2026):** המיגרציה הורצה ✅ · `submit-feedback` נפרסה ✅
(אומתה בבקשה עם מפתח פסול: `400 · «מפתח לא תקין»`, כלומר הפונקציה חיה
וקראה את `LICENSE_PUBLIC_KEY` בהצלחה — כשל בסוד היה מחזיר 500) ·
`index.html` הועלה ✅. נותרה בדיקה מקצה לקצה: לשלוח פנייה מהאפליקציה
ולוודא שהיא מופיעה בטאב.

### Build — המתקין נבנה חסר משאבים, ותיקון שיתפוס את זה בעתיד

**מה קרה:** הבנייה הראשונה של 1.66.0 יצרה מתקין של **114MB** במקום ~214MB.
‏`resources/runtime/` (Node פורטבילי) ו-`resources/playwright/` (Chromium)
אינם בגיט ולא היו על המחשב הזה — ו-**electron-builder דילג עליהם בשקט**,
בלי אזהרה ובלי כשל. הבנייה «הצליחה».

**למה זה היה מסוכן:** שתי המערכות נכשלות בשקט אצל הלקוח, בלי הודעת שגיאה —
`findBundledRuntimeDir()` נופל ל-PATH של המערכת (**לקוח בלי Node נחסם
ב-RuntimeGate ולא יכול לפתוח פרויקט**), ו-`bundledBrowsersDir()` מחזיר null
כך שבדיקת הדפדפן ו«בסיס ירוק» מדלגות. בשילוב עם `mandatory: true` שהיא
ברירת המחדל, העלאת המתקין הזה הייתה **מכריחה כל לקוח קיים** לרדת לגרסה
פגומה. נתפס רק כי נתי שם לב לפער בגודל הקובץ.

**מה נעשה:**

- הושלמו שני המשאבים: `node-v22.14.0-win-x64` (98MB) → `resources/runtime/`
  (התוכן, לא התיקייה העוטפת), ו-`npx playwright install chromium-headless-shell`
  → העתקה מ-`%LOCALAPPDATA%\ms-playwright` ל-`resources/playwright/` (274MB).
- `release/` השבורה נמחקה ובנייה חדשה הורצה מאפס.
- **`RELEASE_CHECKLIST.md` — נוסף סעיף 0** («גודל המתקין») כשלב החוסם
  הראשון, עם שלושת התנאים לבדיקה ותיאור מה נשבר אצל הלקוח. הבדיקה הזולה
  ביותר היא גודל הקובץ: ~214MB תקין, ~114MB חסר.

**אימות הבנייה החדשה:**

- `NF-Blaze-Setup-1.66.0.exe` — **215.8MB** (226,254,198 בתים).
- `release/win-unpacked/resources/` מכיל `runtime` (98M) **וגם** `playwright`
  (274M), לצד `app.asar` (188M).
- `resources/runtime/node.exe -v` → **v22.14.0**, `npm.cmd -v` → **10.9.2**,
  שניהם מתוך החבילה הבנויה.
- `chromium_headless_shell-1228/chrome-headless-shell-win64/chrome-headless-shell.exe`
  קיים; התיקייה מתחילה ב-`chromium` כפי ש-`looksLikeBrowsersDir()` דורש.
- `version.json` → `1.66.0` · `mandatory: true`; `latest.yml` עם sha512 וגודל
  תואמים; `landing/config.js` מצביע ל-1.66.0.
- ⚠️ **טרם הותקן ונבדק על מכונה נקייה** — סעיף 3 ב-`RELEASE_CHECKLIST.md`.

### Build — הגרסה נבנתה, הועלתה ואומתה באוויר

`NF-Blaze-Setup-1.66.0.exe` · **215.8MB** (226,254,198 בתים) — הבנייה
התקינה, אחרי השלמת המשאבים החסרים (ראה «Build» למטה). הועלתה על ידי נתי,
ואומתה מרחוק ב-02.08.2026:

```
version.json  → 1.66.0 · mandatory: true          [HTTP 200]
NF-Blaze-Setup-1.66.0.exe → content-length 226254198  [HTTP 200]
```

הגודל תואם בדיוק לקובץ שנבנה — כלומר עלתה הגרסה הנכונה ולא זו שחסרו בה
המשאבים. **כאן נגמרת 1.66.0.** כל מה שנעשה אחרי הנקודה הזו נמצא ב-1.66.1.

---

## 1.66.2 - 2026-08-02

### Landing — חסימת תפריט ההקשר (קליק ימני)

נתי ביקש (02.08.2026) שלא יהיה אפשר ללחוץ קליק ימני באתר.

⚠️ **הוצג לו במפורש שזו אינה הגנה:** ‏Ctrl+U, ‏F12, ‏`curl` וכיבוי JavaScript
עוקפים אותה לחלוטין. היא הרתעה מזדמנת בלבד, והוא ביקש בכל זאת.

- **שדות טופס מוחרגים** (`input`, `textarea`, `contenteditable`) — בלעדיהם
  «הדבק» בטופס בקשת הרישיון מפסיק לעבוד, וזה שובר משתמש אמיתי בלי להוסיף
  שום הגנה.
- ⚠️ **החרגה של טקסט מסומן נשקלה ונפסלה תוך כדי.** מימשתי אותה תחילה
  (בהנחה שמי שסימן טקסט רוצה להעתיק), ואז הבנתי שהיא הופכת את החסימה
  לחסרת ערך — סימון מילה אחת מחזיר את התפריט. הוסרה.
- ⚠️ **שיקול נגישות שכדאי לזכור:** לאתר יש `accessibility.html` ו-`a11y.js`.
  חסימת תפריט הקשר עלולה להתנגש בטכנולוגיה מסייעת. אם תגיע תלונת נגישות —
  זו השורה שצריך להסיר.

**אימות:** גוף הדף, כותרת, לוגו, קישור וכפתור ההורדה — נחסמים; ארבעת שדות
הטקסט בטופס (כולל מלכודת הבוטים) — פתוחים. `<select>` נחסם, וזה מכוון.

**הקישור להורדה בדף הנחיתה** כבר עודכן ל-1.66.2 אוטומטית על ידי
`make-release.cjs`. ממילא הכפתור נבנה בזמן ריצה מ-`version.json` שעל השרת,
ו-`config.js` הוא רק גיבוי.

### Fixed — חיבור Supabase לא הופיע במסך ההגדרות

נתי שאל למה בטאב «חיבורים» יש GitHub ו-Vercel אבל אין Supabase.

**זה לא היה באג בתצוגה אלא כרטיס שמעולם לא נוסף.** חיבור החשבון קיים
במלואו מ-1.63.0 — `connectOAuth('supabase')`, `supabaseAccountStatus`,
`disconnectSupabaseAccount`, `listSupabaseProjects` — וגם
`OAuthConnectButton` תומך ב-`supabase` מאז. הכרטיס נוסף רק ל-
`IntegrationsPanel` (רמת פרויקט) ונשכח ב-`SettingsPage` (רמת חשבון).

נוסף כרטיס «Supabase (חשבון)» בין GitHub ל-Vercel, במבנה זהה: נורית מצב
עם שם הארגון, «התחבר עם Supabase» / «נתק חשבון», וקישור לניהול הרשאות.
מצב החשבון נקרא ב-`useEffect` נפרד — הטוקן מוצפן בתהליך הראשי ולכן אינו
מגיע עם `settings` כמו שאר הדגלים.

**אימות ויזואלי:** חמשת הכרטיסים בסדר הנכון (MCP · GitHub · **Supabase** ·
Vercel ב׳ · Vercel א׳), ומחזור מלא — «לא מחובר» → חיבור → «חשבון Supabase
מחובר · NF Digital» עם נורית ירוקה → ניתוק → חזרה ל«לא מחובר».

### Feature — דחיית עדכון ב-12 שעות, וחיווי עדכון שנראה

נתי ביקש (02.08.2026) שני דברים שהתבררו כאותו מנגנון: שהמשתמש **יראה** את
התקנת העדכון עם אחוזי ההורדה, ושיוכל **לדחות** אותו — «אם הוא באמצע עבודה
על פרויקט שלא ייסגר לו».

**הבעיה שנחשפה תוך כדי:** עד היום `UpdateGate` היה הממשק **היחיד** שדיבר
על עדכונים, והוא נטען רק כש-`update.required`. כלומר עדכון **לא-חובה**
(`mandatory: false`) היה **בלתי נראה לחלוטין** — ההורדה וההתקנה קרו בשקט
מוחלט. זה בדיוק מה שנתי ראה כשהעלינו את 1.66.1 עם `false` ושאל «למה
המשתמשים לא מקבלים עדכון».

**דחייה (`UPDATE_SNOOZE_MS` = 12 שעות)**

- לוגיקה טהורה ב-`src/shared/version.ts`: `snoozeVerdict` / `nextSnooze`,
  מכוסות ב-9 בדיקות חדשות.
- **מכסה: 2 דחיות לכל גרסה** (‏`MAX_UPDATE_SNOOZES`) = יממה, ואז חובה.
- **הדחייה נקשרת לגרסה** — גרסה חדשה יותר מאפסת את המונה, אחרת דחייה אחת
  הייתה מכסה גם על גרסאות עתידיות.
- **שעון שהוחזר אחורה אינו מאריך דחייה:** אם הזמן שנותר גדול מחלון
  הדחייה, הערך בהכרח שגוי (או נערך ידנית) והדחייה נחשבת גמורה.
- נשמרת ב-`settings.json` ולא באחסון המוצפן — **זו נוחות ולא גבול אבטחה**;
  ההגנה מפני דחייה אינסופית היא המכסה והקשירה לגרסה.

**הפרדת `required` מ-`mandatory`**

`UpdateState.mandatory` = מה שהשרת הכריז. `required` = חוסם **כרגע**,
כלומר `mandatory && !snoozeActive`. ‏`isUpdateRequired()` (ששומר ה-IPC
נשען עליו) מחשב את זה מחדש בכל קריאה, כך שדחייה שנגמרה מחזירה את החסימה
גם בלי שהמניפסט נבדק שוב — הבדיקה התקופתית (10 דק') משדרת את המצב.

⚠️ `APP_UPDATE_SNOOZE` **חייב** להיות ב-`UPDATE_FREE_CHANNELS`, אחרת
הכפתור נחסם על ידי השומר שהוא עצמו אמור לפתוח.

**‏`UpdateBanner` — חיווי בסרגל העליון**

מוצג כשיש גרסה חדשה שאינה חוסמת כרגע (לא-חובה, או חובה שנדחתה):
«מוריד עדכון 1.67.0 · 47%» → «גרסה 1.67.0 מוכנה — עדכן עכשיו · נדחה לעוד
7 שע׳». במצב מוכן הוא לחיץ ומתקין. פותר גם את בעיית העדכון הבלתי-נראה.

**אימות:** ‏`npm run typecheck` ✅ · `npm test` ✅ · `lint` 0 שגיאות ✅.
נבדקו ויזואלית **שבעה תרחישים**: חובה/מוריד, חובה/מוכן, חובה/מכסה-נגמרה
(ללא כפתור דחייה), נדחה/מוריד, נדחה/מוכן, לא-חובה/מוריד, לא-חובה/מוכן —
בכל אחד אומת מי מוצג (מסך חוסם מול חיווי), הטקסט, והאם החיווי לחיץ.
זרימת הדחייה נבדקה מקצה לקצה: לחיצה → IPC → המסך נסגר → החיווי עובר לסרגל.
⚠️ תוקן תוך כדי: הכיתוב הראה «עוד 6 שע׳» כשנותרו 6.9 (`floor` במקום `round`).

## 1.66.1 - 2026-08-02

> ⚠️ **למה גרסה נפרדת ולא המשך של 1.66.0:** ‏1.66.0 כבר באוויר כעדכון חובה
> ולקוחות כבר מתקינים אותה. ‏`decideUpdate` משווה גרסאות — דריסה של אותו
> מספר לא הייתה מגיעה לאף לקוח שכבר עדכן.

### Feature — משוב גם בלי רישיון, סוג «בקשת/חידוש רישיון», ומגבלת קצב לפי IP

נתי ביקש (02.08.2026): סוג פנייה נוסף לבקשת/חידוש רישיון, לאפשר שליחה גם
למי שאין לו רישיון, לחסום כל IP לפנייה אחת כל 3 שעות, ולוודא שאי אפשר
להריץ שאילתות דרך המשוב.

**למה לפתוח את הערוץ ללא-מורשים:** מי שתקוע בשער הרישיון (פג תוקף, חריגת
מושבים, אין מפתח) לא יכול לפנות אלינו בשום דרך מתוך המערכת — כלומר דווקא
במצב שבו הוא הכי צריך אותנו. `FEEDBACK_SUBMIT` הועבר ל-`LICENSE_FREE_CHANNELS`.

**איך זה לא הורס את מודל הזהות** (זו ההחלטה המרכזית): פנייה בלי מפתח נשמרת
עם `verified = false`, והפורטל מציג עליה תגית **«לא מאומתת»** עם הסבר. בלי
ההפרדה הזו כל אחד היה יכול להקליד שם של לקוח קיים והפנייה הייתה נראית זהה
לפנייה אמיתית — כלומר פתיחת הערוץ הייתה מבטלת בדיוק את ההגנה שבנינו
בסבב הקודם. בנוסף, **מפתח פגום נדחה ואינו נופל לפנייה אנונימית**, אחרת
אפשר היה לעקוף את השיוך בכוונה על ידי שליחת זבל בשדה המפתח.

**מגבלת קצב — למה במסד ולא בזיכרון:** המימוש הקודם היה `Map` בזיכרון
האינסטנס. לחלון של 10 דקות זה בלם סביר; **לחלון של 3 שעות זה חסר ערך**,
כי אינסטנס של Edge Function ממוחזר הרבה לפני כן והמונה מתאפס. לכן נוספה
טבלה `nfb_feedback_throttle` ופונקציה `nfb_feedback_throttle_check` שבודקת
ומסמנת **אטומית**: ה-`INSERT` תופס נעילה על המפתח וה-`ON CONFLICT DO UPDATE`
מתבצע רק אם החלון עבר, כך ששתי בקשות מקבילות מאותה כתובת לא יעברו יחד.
נשמר **גיבוב** של הכתובת ולא הכתובת עצמה.

⚠️ **המגבלה חלה על כולם, כולל בעלי רישיון** — כפי שהתבקש («כל IP»). שתי
השלכות שכדאי להכיר: משרד שכל המחשבים בו יוצאים דרך אותה כתובת חולק מכסה
אחת, ולקוח ששלח דיווח לא יוכל לשלוח לו המשך במשך 3 שעות. להחרגת בעלי
רישיון — לדלג על הבדיקה כשקיים `verified`. המגבלה בזיכרון לפי רישיון
(5 ל-10 דקות) הוסרה כי היא מסובכת מ-3 השעות ולכן מתה.

⚠️ **כשל בבדיקת המגבלה חוסם** (503), בניגוד ל-fail-open של ספירת המושבים:
שם fail-open מגן על לקוח משלם, כאן הוא היה פותח ערוץ בלי שום הגנה.

**בדיקת ההזרקה שהתבקשה — הממצא:** אי אפשר להריץ שאילתות דרך המשוב.
נסקרו כל הנתיבים שטקסט חופשי עובר בהם:

1. **אין בשום מקום בניית SQL ממחרוזות.** הכתיבה היחידה היא
   `.from('nfb_user_feedback').insert({...})`, הקריאה היחידה היא
   `.select().eq('license_id', <מזהה מאומת חתימתית>)`, והקריאה לפונקציה היא
   `.rpc()` עם פרמטרים. ‏`supabase-js` שולח את הערכים ל-PostgREST כפרמטרים
   מופרדים — טקסט של משתמש לעולם אינו הופך לחלק ממשפט SQL.
2. **פונקציית ה-RPC** היא plpgsql רגילה: `p_ip_hash` משמש כ**ערך** ב-INSERT,
   אין בה `EXECUTE` ואין `format()`. היא `security definer` **עם**
   `set search_path = public` (בלי זה זה וקטור השתלטות מוכר), והרשאת ההרצה
   נשללת מ-`public/anon/authenticated`.
3. **הקלט מסונן לפני הכל:** `kind` מול allowlist (וגם `CHECK` במסד),
   אימייל מול regex, וכל השדות חתוכים באורך.
4. **XSS בפורטל:** כל שדה טקסט מרונדר דרך `esc()`. אומת בפועל — פנייה עם
   `<script>alert(1)</script>` ועם `<img src=x onerror=...>` יצרה **0**
   אלמנטים ב-DOM והוצגה כטקסט.

הכיסוי נעול ב-`tests/feedback.test.ts` (14 בדיקות), כולל שומר-סף שייפול אם
מישהו יוסיף בעתיד `EXECUTE` / `format()` ל-Edge Function או למיגרציה.

**שאר השינויים**

- `validateFeedbackInput()` ב-`src/shared/types.ts` — ולידציה אחת שרצה
  ב-renderer, בתהליך הראשי ובשרת, במקום שלוש כפילויות שיזלגו זו מזו.
- הטופס: שדה «שם מלא» מופיע **רק** כשאין רישיון (עם רישיון השם נגזר בשרת
  ולכן שדה כזה היה מטעה), ואימייל הופך לחובה. ברירת המחדל של סוג הפנייה
  היא «בקשת/חידוש רישיון» כשאין רישיון.
- מיגרציה `0007_feedback_unlicensed.sql`: `license_id` הפך nullable,
  נוסף `verified`, ה-`CHECK` על `kind` הורחב, ורשומות קיימות סומנו
  `verified = true`.
- מרכז העזרה: נוסף פריט «הרישיון פג או שאין לכם מפתח» בפתרון תקלות.

**אימות**

- `npm run typecheck` ✅ · `npm test` — 17+6+1+3+1 קבצים עוברים ✅ ·
  `npm run lint` — 0 שגיאות ✅
- הטופס נבדק ויזואלית בשני המצבים: בלי רישיון מוצגים 5 סוגים עם «בקשת/חידוש
  רישיון» כברירת מחדל, שדה שם מופיע, וכפתור השליחה נפתח רק אחרי כותרת+פירוט+
  שם+אימייל **תקין**; עם רישיון אין שדה שם והשליחה נפתחת בלי אימייל.
- המטען שנשלח בפועל נבדק: `'; DROP TABLE nfb_user_feedback; --` עבר כמחרוזת
  רגילה בשדה `message`.
- הפורטל: 6 מסנני סוג, תגית «לא מאומתת» מופיעה רק בשורה הלא-מאומתת, ואין
  גלישה אופקית.
- ⚠️ **לא אומת מול השרת החי** — המיגרציה והפונקציה טרם נפרסו.

### 🔴 Licensing — חיבור לאינטרנט הפך לדרישה, ורישיון = מחשב אחד

**שינוי התנהגות שמשפיע על כל לקוח.** נתי ביקש (02.08.2026) שרישיון ל-3
ימים יפוג אחרי 3 ימים גם מול שינוי שעון, שהמערכת לא תעלה בלי רשת, ושרישיון
שיועבר למחשב אחר ייתן «כבר שומש».

**מה שהוצג לו לפני הביצוע (ומה שהוא החליט בכל זאת):**

שלוש מההנחות בבקשה לא תאמו את המצב, ואומתו בקוד:

1. **רישיון ל-3 ימים כבר פג אחרי 3 ימים, גם בלי רשת.** התפוגה חתומה במפתח,
   והבדיקה `left <= 0` רצה ב-`license.ts:455` — **לפני** כל בדיקה מקוונת
   (שורה 506). שינוי שעון לא עוזר: `trustedEvaluationMs` לוקח מקסימום על
   5 מקורות. החשיפה המקסימלית הייתה 48 שעות, לא אינסוף.
2. **לא דרשנו רשת קודם.** היה חלון חסד של 48 שעות, מתועד ומכוון.
3. **כתובת IP היא מזהה שגוי לאכיפה** — מתחלפת בכל אתחול ראוטר, שונה בין
   בית/משרד/בית קפה, ומשותפת ב-CGNAT. היא הייתה נועלת לקוחות משלמים כל
   כמה ימים בלי לעצור תוקף שממילא עובר לאותה רשת.

הצגתי גם שתי השלכות של דרישת רשת מלאה: מודלים מקומיים (Ollama / LM Studio)
הם תכונה שאנחנו מפרסמים והם עובדים אופליין, ו**תקלה ב-Supabase משביתה את
כל הלקוחות בו-זמנית** — כולל את מתג החירום, שמגיע דרך אותה Supabase.
נתי החליט להמשיך, בנימוק שהמערכת ממילא עובדת מול מודלי AI מרוחקים.

**מה שונה בפועל**

- `ONLINE_REVALIDATE_GRACE_MS`: ‏48 שעות → **15 דקות**.
  `ACTIVATION_GRACE_MS`: ‏24 שעות → 15 דקות (זהה — ההפעלה ממילא ממתינה
  לאימות מקוון).
  ⚠️ **הערך אינו יכול להיות 0**, למרות שזו הייתה הבקשה המילולית: הבדיקה
  היא `now - anchor > grace`, והפרש מילישניות בין שעון הלקוח לשעון השרת
  קיים תמיד — 0 היה חוסם כל לקוח מיד אחרי אימות מוצלח.
- `LICENSE_REVALIDATE_INTERVAL_MS` חדש: האימות ברקע ירד מ-6 שעות ל-**5
  דקות**, כלומר שלושה ניסיונות בתוך חלון החסד. ⚠️ ‏288 קריאות ליום ללקוח
  (~8,600 בחודש) — המכסה החינמית של Supabase (500K) מספיקה לכ-57 לקוחות
  פעילים. מעבר לזה צריך להאריך את המרווח.
- **חזרה לחלון מריצה אימות מקוון ולא רק קריאת סטטוס** (`App.tsx`). בלי זה
  מחשב שהיה בשינה חצי שעה היה מתעורר אל מעבר לחלון ומציג «נדרש אימות
  מקוון» למרות רשת תקינה. יש throttle של דקה כדי לא לפנות לשרת בכל מזעור.
- **מסכי החסימה כבר היו קיימים** (`RevalidateGate` / `SeatGate` עם «בדוק
  שוב») — לא נדרש UI חדש, רק שינוי הפרמטרים.

**רישיון = מחשב אחד** (מיגרציה `0008_single_seat_and_ip.sql`)

- ברירת המחדל של `nfb_licenses.seats`: ‏3 → **1**.
  ⚠️ **רישיונות קיימים נשארו על 3 במכוון** — שינוי ברירת מחדל אינו נוגע
  בשורות קיימות, והורדה גורפת הייתה נועלת מיד כל לקוח שעובד על שני מחשבים.
- **הזיהוי נשאר `machineFingerprint()`** ולא IP, מהסיבות למעלה. הכתובת כן
  נשמרת ב-`nfb_license_seats.last_ip` ומוצגת בפורטל — **לזיהוי בלבד**.
  `nfb_claim_seat` קיבלה פרמטר `p_ip` **בסוף ועם ברירת מחדל**, כדי שגרסת
  אפליקציה ישנה שקוראת עם 3 ארגומנטים תמשיך לעבוד.
- **ניהול בפורטל** (היה חסר לגמרי, ועכשיו הוא חובה): עמודת «מחשבים» עם
  מונה `תפוס / מכסה` שנצבע באדום כשמלא, פתיחה לרשימת המכונות (שם, נראה
  לאחרונה, נרשם, IP, טביעת אצבע) עם **«שחרר מחשב»**, ועמודת «מכסה» לעריכה
  ישירה (`0` = ללא הגבלה). בלי זה כל לקוח שמפרמט היה דורש SQL ידני.
- **הרשאת העדכון מצומצמת ברמת העמודה:** ‏`revoke update ... ; grant update
  (seats)`. ‏RLS לא יודע להגביל עמודות, ולכן בלי זה המנהל היה יכול לשנות
  מהדפדפן גם תאריך תפוגה, שם, או את המפתח עצמו.

**אימות:** `npm run typecheck` ✅ · `npm test` — כל הסוויטות ✅ ·
`npm run lint` 0 שגיאות ✅. שתי בדיקות ב-`tests/license.test.ts` **נפלו
כמצופה** כי הן קיבעו את 48/24 השעות — הן נכתבו מחדש כך שהן שומרות על
המדיניות החדשה (כולל בדיקה שמרווח האימות קטן מחלון החסד פי 3 לפחות).
ניהול המחשבים בפורטל נבדק ויזואלית: `1 / 1` באדום, `1 / 3` רגיל,
`2 / ∞` למכסה בלתי מוגבלת, ורשימת מכונות עם כפתור שחרור.

⚠️ **לא אומת מול השרת החי** — מיגרציה `0008` ו-`validate-license` טרם נפרסו.

### Fixed — הפורטל החזיר «permission denied» בטאב הרישיונות

אחרי הרצת `0008`, טעינת רשימת הרישיונות נכשלה עם
`permission denied for table nfb_licenses`.

**הסיבה — ובאג שלי:** מיגרציה `0003` שללה `select` **ברמת הטבלה** והעניקה
**רשימת עמודות מפורשת**, כדי ש-`license_key` לא ייחשף לדפדפן. העמודה
`seats` נוספה ב-`0005` ומעולם לא נוספה לרשימה — כל עוד הפורטל לא ביקש
אותה זה לא הורגש. ב-1.66.1 הוא מציג ועורך מכסה, ולכן ה-`select` נכשל כולו.
ב-`0008` הוספתי `grant update (seats)` ופספסתי ש-SELECT עובד באותה שיטה.

`0009_grant_seats_select.sql` מוסיף `grant select (seats)`, ובנוסף מעניק
במפורש `select`/`delete` על `nfb_license_seats` במקום להישען על ברירות
המחדל של Supabase.

⚠️ **הלקח לעתיד:** ב-`nfb_licenses` יש הרשאות **ברמת עמודה**. כל עמודה
חדשה שם חייבת `grant` מפורש — `grant all on all tables` לא מכסה טבלה
שנשללה ממנה ההרשאה ברמת הטבלה.

### Release — 1.66.1 נבנתה ושוחררה כעדכון לא-חובה

`NF-Blaze-Setup-1.66.1.exe` · **215.8MB** (226,256,198 בתים), שני המשאבים
בפנים (98M runtime + 274M playwright), `node.exe -v` → v22.14.0 מתוך החבילה.

**`version.json` נערך ידנית ל-`mandatory: false`.** ‏`npm run release` מייצר
`true` כברירת מחדל, אבל 1.66.1 מקצרת את חלון החסד מ-48 שעות ל-15 דקות —
כלומר טעות בצד השרת נועלת את **כל** הלקוחות תוך רבע שעה, בלי זמן תגובה.
משחררים קודם כהודעה בלבד, מאמתים על מכונה נקייה, ורק אז משנים ל-`true`.

### UX — «פתח בדפדפן» בשורת הכתובת של התצוגה

נתי ביקש (02.08.2026) חץ בצד שורת הכתובת שפותח את התצוגה המקדימה, שייראה
מקצועי. נוסף `.address-open` — כפתור אייקון (`ArrowUpRight`) בקצה
`.address-bar` ב-[WorkspacePage.tsx](src/renderer/src/pages/WorkspacePage.tsx),
מעוצב כאייקון שקוף שנצבע ב-hover (כמו בסרגלי כתובת של דפדפנים) ולא ככפתור
מלא שיתחרה על תשומת הלב עם הכתובת.

- **מוצג רק במצב «תצוגה»** — במצב קוד/אבטחה/בדיקות אין מה לפתוח.
- **תצוגה סטטית מסוננת:** `openExternal` מאשר http/https בלבד, ולכן כתובת
  `nfblaze://` הייתה נכשלת בשקט. נוסף `externalPreviewUrl` שמסנן אותה
  **לפני** נקודת הלחיצה, במקום לגלות את זה בזמן ריצה.
- **התצוגה לא רצה → מרים ואז פותח.** הכתובת נקראת מ-`livePreviewUrlRef`
  ולא מה-state: ‏`ensureVitePreview` מעדכן את ה-ref סינכרונית, בעוד ה-state
  עדיין לא התרנדר בתוך אותה סגירה.

**אימות:** נבדק ויזואלית מול ה-CSS האמיתי — 22×22, בתוך הסרגל, בקצה
(`atRightEnd`), אחרי הכתובת, ממורכז אנכית; רקע שקוף כברירת מחדל, `opacity 0.4`
ו-`cursor: default` במצב `disabled`, וכללי ה-hover וה-disabled קיימים בגיליון.
‏`npm run typecheck` ✅ · `eslint` על הקובץ ✅.

### Security — מודל איומים לעקיפת הרישוי (מסמך פנימי)

נתי ביקש (02.08.2026) דירוג של תוקפים לפי יכולת, מהקל לקשה, מול ההגנות
הקיימות. נוצר [SECURITY_THREAT_MODEL.md](SECURITY_THREAT_MODEL.md) — **מסמך
פנימי בלבד**, עם אזהרה בראשו: הוא ממפה במפורש איפה ההגנות נשברות, וחשיפתו
שקולה למסירת מפת דרכים לפריצה. חל עליו כלל #3 שבראש הקובץ הזה.

⚠️ **הוא אינו ב-`build.files`** ולכן לא נארז בהתקנה — אבל **אין להעלות אותו
לאתר ואין לצרף אותו למסירה ללקוח**.

שני ממצאים חדשים שעלו מהסקירה ולא היו מתועדים:

1. **‏`bytecodePlugin` מוגדר רק על `main`.** אומת על החבילה הבנויה של 1.66.0:
   ‏`out/main/index.jsc` הוא bytecode, אבל `out/preload/index.js` וכל
   `out/renderer/assets/*.js` הם **JavaScript קריא**. זה לא שובר את ההגנה —
   שער ה-IPC יושב ב-main — אבל זו מפת דרכים שמקצרת עבודה לתוקף.
2. **ה-bytecode אינו מסתיר את המפתח הציבורי.** מחרוזות קבועות שורדות
   ב-constant pool, ומפתח Ed25519 ב-PEM הוא באורך קבוע — כלומר ניתן להחלפה
   בייט-בבייט בלי לקמפל מחדש. זו נקודת השבירה המלאה, ואין נגדה הגנה
   בארכיטקטורה הנוכחית (וגם לא יכולה להיות באימות שרץ אצל הלקוח).

המסקנה המעשית במסמך: דרגות 0–2 סגורות היטב, מדרגה 3 ומעלה אין הגנה
אפקטיבית **ואין שום יכולת זיהוי** של עותק פרוץ. ההמלצות מדורגות לפי תשואה,
ושתי הראשונות כבר קיימות ב-`RELEASE_CHECKLIST.md` ולא בוצעו: **גיבוי המפתח
הפרטי** ו**חתימה דיגיטלית על המתקין**.

## 1.65.0 - 2026-08-02

### Security — ספירת מושבים בשרת, חלון חסד 48ש', וסגירת אחסון הסודות

נובע מביקורת אבטחה שנתי ביקש (02.08.2026) על הגרסה הארוזה 1.64.0: «האם המשתמש
חשוף לפרטים שלא צריך, והאם הוא יכול לפרוץ את הרישיון». הממצאים המלאים למטה —
**זה החלק החשוב של הערך הזה**, כי התיקונים כאן סוגרים חלק מהם בלבד.

**🔴 הממצא המרכזי: המפתח היה «כרטיס נושא».** ‏`validate-license` קיבל רק
`key`+`nonce` — **לא ראה את טביעת האצבע ולא ספר הפעלות**. הקשירה למכונה נשמרה
מקומית ב-`license-fingerprint`, והתנאי הוא `if (boundTo && ...)` — כלומר מחיקת
הקובץ מבטלת אותה. בפועל מפתח אחד עבד על מספר בלתי מוגבל של מחשבים, בלי שום
פריצה ובלי שנדע. זה לא היה באג בקוד אלא **תכונה חסרה בשרת**.

- **טבלה `nfb_license_seats` + `nfb_claim_seat()`** (מיגרציה `0005`). מכסה
  ‏`seats` על `nfb_licenses`, ברירת מחדל **3**. התפיסה נעשית ב-`for update`
  בטרנזקציה אחת — אחרת שתי בקשות במקביל היו עוברות יחד את הבדיקה וחורגות.
  ‏RLS: קריאה ומחיקה למנהל המחובר, **הכנסה ב-`service_role` בלבד** (אחרת לקוח
  היה רושם לעצמו מושבים מהדפדפן עם המפתח הציבורי).
- **מושב נתפס רק כשהרישיון תקף** — אחרת מפתח שפג היה ממשיך לתפוס מקום וחוסם
  את הלקוח אחרי חידוש.
- **חריגת מושב אינה מרעננת את עוגן החסד** (`license.ts`). בלי זה מחשב שנדחה היה
  מאריך לעצמו את החלון בכל פנייה וממשיך לעבוד ללא הגבלה.
- **אין טביעת אצבע בבקשה → מדלגים על האכיפה.** ‏fail-open מכוון, כדי שעדכון
  השרת לבדו לא ינעל לקוחות על גרסה ישנה.

**🔴 חלון החסד האופליין ירד מ-30 יום ל-48 שעות**, ולפני האימות המוצלח הראשון
ל-24 שעות (`ACTIVATION_GRACE_MS`) — עד כה אפשר היה להפעיל מפתח על מחשב מנותק
ולעבוד חודש בלי שהשרת ידע על ההפעלה, כלומר גם בלי שספירת המושבים תראה אותה.
‏`LICENSE_ACTIVATE` **ממתין** עכשיו ל-`revalidateOnline` במקום להריץ ברקע, אחרת
מחשב שחורג מהמכסה היה מקבל «רישיון פעיל» ורק אז מושלך החוצה.

⚠️ **לא 0 ולא 30 — ובכוונה.** אימות בכל עלייה היה קושר את זמינות המוצר לזמינות
של Supabase: תקלה של שעתיים אצלם משביתה את כל הלקוחות בבת אחת, ולקוח בטיסה
נתקע. זה גם סותר את עקרון ה-fail-open שנקבע במערכת העדכונים. 48 שעות מקצרות את
תפיסת הביטול מחודש ליומיים בלי להפוך רעש רשת לתקלה.

**🔴 מתג חירום (`gr` בתשובה החתומה + `MAX_SERVER_GRACE_MS`).** חלון החסד מוטמע
בבינארי, ולכן **תקלה בצד השרת הייתה נועלת את כל הלקוחות תוך 48 שעות בלי דרך
לשחררם מלבד גרסה חדשה** — בניגוד למערכת העדכונים, שם עריכת `version.json`
משחררת מיד. משתנה הסביבה `LICENSE_GRACE_OVERRIDE_MS` מאריך את החלון בתשובה
**חתומה**; הלקוח חותך אותו ב-30 יום כדי ששרת שנפרץ לא יהפוך את האימות לאות מתה.

**🔴 `secrets.ts` — הקידומת `plain:` התקבלה תמיד**, גם כשהצפנת מערכת ההפעלה
זמינה. כלומר אפשר היה לשתול קובץ סוד שהאפליקציה מקבלת בלי לעבור דרך
‏`safeStorage`. בשילוב עם הממצא הבא זה הפך את זיוף מצב הרישיון לעריכת קובץ טקסט.
מעכשיו `plain:` מתקבל **רק** כשההצפנה אינה זמינה — כלומר במצב שבו הוא גם נכתב,
כך שלקוחות שנשענים על ה-fallback לא נשברים.

### Fixed — קפיצת שעון קדימה נעלה לקוח משלם לצמיתות

התגלה בבדיקת ההתנהגות שנתי ביקש («מה קורה כשמשנים תאריך»), **אחרי** שהמתקין
כבר נבנה — ותוקן לפני ההעלאה, כדי לא לחייב עדכון חובה שני תוך ימים.

**מה היה:** רצפות הזמן היו **מונוטוניות עולות בלבד**. שעון שקפץ קדימה (סוללת
BIOS מרוקנת, תאריך שהוגדר לא נכון) נצרב ב-`license-seen`, ב-mtime של קובץ
העוגן, וב-mtime של כל קובץ שנכתב באותו רגע. מאותו רגע:
תיקון השעון **לא עזר**, ואימות מקוון **לא עזר** — `nextHighWater` מחזיר את
הערך הישן כש-`nowMs <= seenMs`, כלומר גם זמן שרת אמיתי לא הוריד את הרצפה.
הרישיון נראה פג **לצמיתות**, ולא הייתה דרך לשחרר מרחוק; רק הדרכת הלקוח למחוק
קבצים מ-`AppData`. אומת בסימולציה: קפיצה של 200 יום → פג → תיקון שעון → עדיין
פג → אימות מקוון → עדיין פג. הקלאמפ הקיים תופס רק מעל 400 יום, כלומר **הטווח
הפגיע היה יום עד 400 יום** — בדיוק הטווח של תקלות שעון אמיתיות.

**התיקון — זמן השרת החתום מותר לו גם להוריד:**
- `trustedEvaluationMs` מקבל `trustedServerMs`. רצפה שגבוהה ממנו ביותר
  מ-`MAX_SERVER_GRACE_MS + ROLLBACK_GRACE_MS` היא בהכרח תוצר שעון שגוי ונזרקת.
- `alignSeenToServer()` ו-`setAnchorExact()` — **המסלול היחיד שמוריד רצפה**,
  ורק מתשובת שרת חתומה.
- `readOnlineState()` הועבר לפני הערכת הזמן כדי לספק את התקרה.

**למה זה בטוח:** תשובת השרת חתומה Ed25519 ומאומתת מול המפתח הציבורי המוטמע,
וה-`nonce` אקראי ונבדק — אי אפשר לזייף אותה ואי אפשר לשדר מחדש תשובה ישנה.
זהו מקור הזמן היחיד שראוי להוריד לפיו רצפה.

⚠️ **מה שלא השתנה בכוונה:** באופליין עם שעון שגוי הרישיון עדיין ייראה פג — אין
דרך לדעת את הזמן האמיתי בלי רשת. הריפוי מתרחש באימות המוצלח הראשון.
**הגנת ההחזרה אחורה לא נחלשה** — נבדק שהחזרה של 30 יום מתחת לתקרה עדיין
מסומנת `rolledBack`, ושסטייה לגיטימית של יומיים אינה נזרקת.

### Changed — מסכי הרישיון ותיעוד

- **מסך «הרישיון בשימוש במחשבים אחרים»** (`SeatGate` ב-`LicenseGate.tsx`) עם
  מונה «בשימוש X מתוך Y» וכפתור «בדוק שוב». מסך נפרד ולא הודעת שגיאה, כי הזנת
  מפתח מחדש לא פותרת את המצב הזה והייתה שולחת את הלקוח למעגל.
- **`RevalidateGate` מציג את `messageHe` מהשירות** במקום טקסט קבוע — הישן אמר
  «עברו יותר מדי ימים», שכבר לא נכון ב-48 שעות. ההודעה מבחינה עכשיו בין «נדרש
  חיבור לאימות» לבין «נדרש חיבור להשלמת ההפעלה».
- `release_notes.ts` — ערך 1.65.0 בלשון תועלת. שינוי ההתנהגות (דרישת חיבור,
  מכסת מחשבים) **חייב** להופיע ללקוח, אחרת זו הפתעה שמייצרת פניות תמיכה.
- `PROJECT_CONTEXT.md` — נוספו ספירת המושבים וחלון החסד; מספרי הגרסה תוקנו
  (עמדו על 1.63.0 בעוד שהקוד היה 1.64.0).

### ⚠️ ממצאים שלא תוקנו — לדעת עליהם

1. **ה-HMAC על `license-seen`/`license-online` ניתן לזיוף.** המפתח הוא
   `machineFingerprint() + salt`; ה-fingerprint הוא
   `sha256(hostname|user|platform|arch)` — ידוע למי שיושב על המכונה — וה-salt
   מוטמע **בטקסט גלוי** ב-`.jsc`. אומת בפועל: נוצרו ערכים תקפים לשני הקבצים.
   סגירת `plain:` מייקרת את הניצול (צריך לעבור דרך `safeStorage`) אך **אינה
   מבטלת אותו**. המיתון האמיתי הוא שהחלון ירד ל-48 שעות ושהקביעה עברה לשרת.
2. **ה-EXE אינו חתום** (‏`NotSigned`, אומת). ה-fuses מוגדרים נכון (‏wire
   ‏`01001101`) ומניפסט ה-asar integrity קיים ותואם — אבל **שניהם בטקסט גלוי
   בקובץ שאין עליו חתימה**, וניתנים להיפוך בכלי סטנדרטי. חתימת קוד היא מה
   שהופך את שתי השכבות למשמעותיות.
3. **`bytecodePlugin` אינו מסתיר מחרוזות.** ‏`prompts/system.md` **חולץ במלואו**
   (10,527 תווים) מה-`.jsc` המשוחרר — הוא base64 ו-V8 שומר string literals
   בטקסט. ההערה ב-`scripts/embed-prompt.cjs` («ועם bytecode, גם לא קריא») **אינה
   נכונה** ותוקנה. נחשפים גם 1,828 שמות פונקציות פנימיות.
4. **אין ממשק לשחרור מושב בפורטל.** לקוח שמחליף מחשב ננעל עד ששורה נמחקת
   ידנית מ-`nfb_license_seats` (המיגרציה נותנת הרשאת `delete` למנהל). מקובל
   בהיקף של 2–3 לקוחות; חייב ממשק לפני הפצה רחבה.

**מה שנבדק ונמצא תקין:** ה-asar נקי (‏9,580 קבצים — אין `.env`, `secrets/`,
‏`.pem`, טסטים או evals); אין sourcemaps של קוד הפרויקט; `webPreferences` נעולים
(‏`contextIsolation`, `sandbox`, ‏DevTools כבויים בארוז); שומר ה-IPC הוא
‏fail-closed; ומה שנראה כמו מפתחות ב-renderer הוא placeholders בטפסים.

אומת: ‏typecheck נקי · **392 טסטים עוברים** · lint 0 שגיאות. נוספו טסטים
ל-`seat_exceeded`, לסדר הקדימויות מול `revoked`, לחלון ההפעלה הקצר, ולחיתוך
מתג החירום בתקרה. ‏`license_lifecycle` עודכן — הוא קידד את חלון 30 הימים.

### פריסה (02.08.2026)

- ✅ **`validate-license` נפרס** לפרויקט `ilsidpixxkfwowsnkntg` דרך
  `npx supabase@latest` (ה-CLI אינו מותקן גלובלית; אין `config.toml`, אך
  ‏`--project-ref` מספיק). אומת: מפתח פגום מחזיר **400 ולא 401** — כלומר
  ‏`--no-verify-jwt` תפס; מפתח חתום מחזיר 200 עם טוקן ש**חתימתו אומתה מול
  המפתח הציבורי המוטמע**; ה-nonce מוחזר נכון; סטיית שעון מול השרת 19 שנ'.
  ⚠️ **`--no-verify-jwt` הוא חובה** — האפליקציה שולחת רק `apikey` בלי
  ‏`Authorization`. פריסה בלעדיו מפילה כל אימות, ועם חלון 48 השעות זה נועל
  את כל הלקוחות תוך יומיים.
- ✅ **המיגרציה `0005` הורצה** ב-SQL Editor של הדשבורד (`db push` דורש סיסמת
  מסד — אותו חסם שתועד ב-1.64.0). אומת מבחוץ עם המפתח הציבורי:
  ‏`rpc/nfb_claim_seat` עברה מ-`PGRST202` (לא קיימת) ל-**`42501 permission
  denied`** — כלומר הפונקציה קיימת וה-`revoke` תפס; `nfb_license_seats` קיימת
  ומחזירה רשימה ריקה לאנונימי, כלומר ה-RLS נועל.
- ✅ **לוגיקת המכסה אומתה בפועל** ב-SQL Editor, בטרנזקציה עם `rollback`: מושב
  1 ו-2 נתפסים (1/2, 2/2), **השלישי נדחה** (`allowed=false`), מחשב מוכר
  **מתרענן בלי להיספר פעמיים** (נשאר 2/2), ורישיון לא-קיים מוחזר כמותר (0/0).
  השורה הרביעית היא הקריטית — ספירה כפולה של אותו מחשב הייתה חוסמת לקוח משלם
  אחרי מספר הפעלות.
- ✅ **המתקין נבנה** — `NF-Blaze-Setup-1.65.0.exe`, ‏224,876,075 בתים,
  ‏sha512 `QbPDNNHx5XmY…FH4KOg==`. אומת שהקוד החדש נכנס לחבילה: `seatExceeded`,
  ‏`safeHostHint`, `effectiveGraceMs` ו-`seat_exceeded` נמצאים ב-`index.jsc`
  הארוז (`nfb_claim_seat` נעדר בצדק — הוא צד-שרת בלבד).
  ‏`make-release.cjs` רוכז ל-`release/upload/` ועדכן את `landing/config.js`.
  ℹ️ `releasedAt` ב-`version.json` נכתב `2026-08-01` כי הסקריפט גוזר תאריך
  ב-UTC והבנייה רצה ב-01:41 שעון ישראל. שדה תצוגה בלבד, ללא השפעה על ההחלטה.
- ✅ **נבנה מחדש אחרי תיקון השעון.** המתקין הסופי: ‏224,876,584 בתים,
  ‏sha512 `9xPiZUzVg4+1…sD4eQA==`. אומת ש-`alignSeenToServer`, `setAnchorExact`
  ו-`trustedServerMs` נמצאים ב-`index.jsc` הארוז. ⚠️ **הבנייה הראשונה
  (‏`QbPDNNHx5XmY…`) אינה תקפה** — היא מכילה את באג השעון. לוודא ש-`latest.yml`
  שעולה הוא זה שנוצר אחרי הבנייה השנייה.

### Infrastructure — הפרויקט נכנס לגיט (02.08.2026)

עד היום לא היה גיט בפרויקט, ולכן `CHANGELOG.md` היה הזיכרון היחיד. הרצנו
‏`git init -b main` והפרויקט נדחף ל-`natircity-stack/NF-Blaze` (**פרטי**).

⚠️ **ההיסטוריה הקיימת נשמרה ולא נמחקה.** בריפו כבר היו שני קומיטים מ-30.07
(גרסה 1.36.0 + `.github/workflows/ci.yml`), וההיסטוריה המקומית החדשה הייתה
נפרדת מהם. במקום `push --force` שהיה מוחק אותם, נעשה
`merge --allow-unrelated-histories -s ours` — התוכן העדכני הוא התוצאה,
והקומיטים הישנים נשארים אבות. ‏`.github/workflows/ci.yml` שוחזר מהריפו לעץ
המקומי כדי שלא ייעלם מהראש.

**נבדק לפני הדחיפה שלא דלף דבר:** ‏`.gitignore` הקיים כבר חסם את `secrets/`,
‏`release/`, `out/`, `node_modules`, `resources/runtime|playwright` ו-`.env`.
אומת בפועל ש-`license-private.pem` ו-`issued-licenses.jsonl` **אינם** בעץ
המבוים, ושסריקת ערכי סוד על 337 הקבצים שנדחפו החזירה רק שלוש התאמות שפירותיהן
תבנית טקסט: שורת צ׳ק-ליסט, `.replace()` על כותרת PEM בקוד, ו-fixture בטסט.
אומת גם **אחרי** הדחיפה מול העץ בשרת: אין `secrets/`, אין `.pem`, אין `out/`.
⚠️ הריפו **חייב להישאר פרטי** — הוא מכיל את מלוא לוגיקת אכיפת הרישוי.
⚠️ **הרישיון של נתי פג ב-01.08** — להנפיק לעצמו מפתח חדש **לפני** הפריסה, אחרת
הוא נועל את עצמו ראשון.

## 1.64.0 - 2026-08-01

### Added — נגישות בתקן ישראלי, מדיניות פרטיות ותקנון

לבקשת נתי. המחקר המשפטי תפס **שלושה דברים שרוב האתרים בישראל כותבים והם לא נכונים** — ראה למטה.

**נגישות — קודם האתר עצמו, אחר כך הווידג'ט:**

- **תוקן כשל תקן אמיתי:** ‏`--text-dim` עמד על **3.62:1** במקום 4.5 הנדרשים (ת"י 5568 סעיף 1.4.3) — צבע ששימש בתחתית הטופס, בכותרות הפוטר, בפירורי הלחם ועוד. הועלה ל-5.8:1 תוך שמירה על ההיררכיה הוויזואלית (16.8 / 7.7 / 5.8).
- **תוקן דילוג בהיררכיית הכותרות** בכל העמודים. זה לא ניקיון — **סעיף 2.4.10 הוא AA בתקן הישראלי** בעוד שבעולם הוא AAA בלבד.
- **קישור «דלג לתוכן הראשי»** בכל עמוד; **מיקוד מקלדת** הועבה ל-3px.
- **תפריט נגישות** (`a11y.js` + `a11y.css`) עם 7 התאמות + שליטה בגודל טקסט, שמירה מקומית, ונגישות מלאה של התפריט עצמו: לכידת מיקוד, ‏Esc, החזרת מיקוד, ‏`aria-pressed`, וסימון מצב פעיל ב-✓ ולא בצבע בלבד.

**⚠️ שלוש נקודות שהמחקר תיקן ואסור לשבור בעריכות עתידיות:**

1. **ת"י 5568 = WCAG 2.0 (ולא 2.1).** מהדורת ספטמבר 2023. שבע חריגות לאומיות, שהחשובה בהן היא העלאת 2.4.10 (היררכיית כותרות) ל-AA.
2. **ווידג'ט נגישות אינו מקיים את התקן.** התקן חל על קוד האתר. הצהרת הנגישות אומרת זאת במפורש — לכתוב «האתר הונגש באמצעות תוסף» זו הודאה, לא הגנה. (‏FTC קנסה את accessiBe במיליון דולר על טענה כזו, ו-22.6% מהתביעות ב-2025 היו נגד אתרים שכבר היה בהם ווידג'ט.)
3. **תוכנה להורדה כנראה כן מזכה בזכות ביטול.** חוק המחשבים מגדיר «מידע» **למעט תוכנה**, וחריג האריזה דורש אריזה פיזית. לכן התקנון נותן 14 יום ביטול לצרכן פרטי במקום לשלול — זה מייתר את השאלה ועולה מעט.

**מדיניות פרטיות** — לפי סעיף 11 לחוק הגנת הפרטיות, כולל סעיף 2א שנוסף בתיקון 13 (שם בעל המאגר ופרטי קשר) שמושמט אפילו מהמדריך של הרשות. **ההודעה מופיעה גם בטופס עצמו** ולא רק בעמוד נפרד, כי החוק דורש אותה במקום האיסוף. אין באנר עוגיות — האתר לא משתמש בהן, ובישראל אין חובה כזו ממילא. הזכויות מנוסחות לפי הדין הישראלי (עיון ותיקון) ולא כזכות מחיקה גורפת בנוסח GDPR, שאינה קיימת כאן.

**תקנון** — כולל את גילויי סעיף 14ג לחוק הגנת הצרכן (מכר מרחוק חל גם בלי עגלת קנייה), **אכיפת הרישיון ועדכוני החובה מפורטים במפורש** (אי-גילוי של מנגנון שמשבית את המוצר הוא בדיוק מה שנתקף כתנאי מקפח), אחריות **מוגבלת בתקרה ולא מוחרגת**, וסמכות שיפוט ישראלית.

אומת: כל 8 העמודים עוברים — 0 כשלי ניגודיות, 0 דילוגי כותרות, ‏h1 יחיד, 0 אלמנטים ללא שם נגיש, קישור דילוג ותפריט נגישות בכולם. 0 קישורים פנימיים שבורים.

⚠️ **חסרים פרטי עסק אמיתיים.** בשלושת העמודים יש מציין `[[...— למלא]]` בשם בעל העסק, ח.פ./ע.מ., כתובת וטלפון. **אסור לפרסם לפני מילוי** — פרטי העוסק הם דרישת חובה בסעיף 14ג, ורכז נגישות בלי דרך פנייה הוא בדיוק הליקוי שתובעים מחפשים.

### Changed — «הורדה» בתפריט הראשי, וסרגל שקוף יותר

- **«הורדה» נוסף לתפריט העליון** בכל חמשת העמודים (היה קיים רק בתפריט המובייל).
- **הסרגל שקוף יותר** — ‏0.55 במנוחה ו-0.72 בגלילה (היה 0.72/0.9). הטשטוש עלה ל-20px עם רוויה, כי בלעדיו הטקסט על הסרגל נהיה קשה לקריאה כשהתוכן מבצבץ מאחור.
- ⚠️ **נקודת המעבר למובייל עלתה מ-900 ל-1050 פיקסלים.** עם הפריט השביעי הפריטים נדחסו ונדבקו זה לזה במסכים בינוניים — עדיף המבורגר מסודר מתפריט צפוף. הערך סונכרן גם ב-`app.js` (סגירת התפריט בשינוי גודל), אחרת היה נוצר טווח שבו התפריט נסגר בזמן שההמבורגר עדיין הניווט היחיד.
- במובייל הסרגל נשאר אטום יותר (0.82) — התפריט הנפתח נשען עליו, ובשתי שכבות שקופות הטקסט שמאחור מבצבץ.

אומת ב-1440 (רגיל ובגלילה), ‏1049 ו-390: אין גלישה אופקית, ההמבורגר מופיע ונעלם בדיוק בנקודה הנכונה.

### Changed — הוסרה רשת הרקע מראש העמודים באתר

לבקשת נתי. הוסרה מכל חמשת העמודים (הבית + ארבעת המדריכים) ולא רק מהראשי, כדי שהמראה יישאר עקבי; גם כלל ה-CSS עצמו נמחק ולא הושאר כקוד מת. הרקע נשען עכשיו על זוהר הכתמים והגחלים בלבד.

### Added — בקשות רישיון: מהטופס באתר ישירות לפורטל

הטופס ב-nf-blaze.dev שלח עד היום מייל דרך Resend. בקשה במייל נקברת בתיבה, אין לה סטטוס, ואין דרך לדעת מה טופל. עכשיו הבקשות נשמרות בטבלה ומנוהלות בפורטל.

- **טבלה חדשה `nfb_license_requests`** בפרויקט הרישוי, עם RLS: קריאה/עדכון/מחיקה למנהל המחובר בלבד, ו**הכנסה אפשרית רק ב-`service_role`** — כלומר דרך ה-Edge Function ולא מהדפדפן. אומת בפועל: ניסיון הזרקה עם המפתח הציבורי הוחזר ב-401, וקריאה אנונימית מחזירה רשימה ריקה.
- **טאב «בקשות רישיונות» בפורטל** עם מונה בקשות ממתינות על הטאב עצמו, סינון (ממתינות / אושרו / נדחו / הכל), וכפתורי אישור·דחייה. בקשה שטופלה ניתנת להחזרה לממתינה או למחיקה (עם אישור) — בלי מחיקה תיבת הבקשות מתמלאת בספאם ואין דרך לנקות.
- **הטופס באתר שונה:** הוסרו «טלפון» ו«חברה/עסק»; נוסף **«איך הגעת אלינו?»** (פייסבוק / גוגל / המלצה / אחר) — נתון שיווקי שלא היה קודם; **כל השדות חובה**, נאכף גם בדפדפן וגם בשרת. נוספה הבטחה מפורשת שהמפתח יישלח במייל **תוך 48 שעות**, בשני מקומות: בטקסט המקדים ובתיבה בולטת מעל כפתור השליחה.
- **Resend אינו בשימוש יותר** בזרימה הזו. הסוד `RESEND_API_KEY` כבר לא נדרש.

⚠️ **מגבלה מכוונת:** אין התראה על בקשה חדשה — צריך להיכנס לפורטל כדי לראות אותה. המונה על הטאב מקל, אבל מי שלא נכנס לא יידע. אם זה יפריע בפועל, אפשר להוסיף התראה בהמשך.

ℹ️ המיגרציה הורצה על הפרויקט החי דרך Edge Function חד-פעמית שנמחקה מיד אחריה (הקונסולה לא מאפשרת DDL דרך ה-API הרגיל, ו-`db push` דורש סיסמת מסד). ה-SQL שמור ב-`license-portal/supabase/migrations/0004_license_requests.sql`.

אומת: שבעה מסלולי אימות בשרת (שדה חסר, מייל פסול, מקור לא חוקי, מלכודת בוטים, בקשה תקינה), שתי בדיקות RLS, ואימות ויזואלי של הטופס והפורטל ב-390px וב-1280px — כולל פריסת הטבלה לכרטיסים במובייל, כי טבלה של שבע עמודות אינה קריאה בטלפון.

### Added — עדכון גרסה כפוי + פופ-אפ «מה חדש»

הפצה עברה לאחסון של `nf-blaze.dev`, ונוסף מנגנון שמוודא שכל הלקוחות על אותה גרסה.

- **זיהוי חי של גרסה חדשה.** האפליקציה קוראת `version.json` מהאחסון כל 10 דקות וגם בכל חזרה לחלון, כך שגרסה שעולה נתפסת תוך דקות ולא בהמתנה למחזור ארוך. ההורדה מתחילה ברקע מיד, כך שכשהמשתמש לוחץ «עדכן» הקובץ כבר אצלו.
- **חסימה מלאה עד להתקנה.** שער חוסם עם התקדמות ההורדה וכפתור «עדכן גרסה והפעל מחדש», בלי אפשרות סגירה. **האכיפה היא בשומר ה-IPC בתהליך הראשי** ולא במסך: כל ערוץ חסום מלבד ארבעה שדרושים להורדה ולהתקנה, כך שגם עקיפה של ה-UI לא פותחת את המערכת. העדכון קודם אפילו למסך הרישיון.
- **פופ-אפ «מה חדש» אחרי העדכון.** מוצג פעם אחת לכל גרסה, ומי שדילג על כמה גרסאות רואה את כולן ולא רק את האחרונה.
- **`npm run release`** מייצר `release/upload/` עם בדיוק מה שצריך לעלות — exe, blockmap, latest.yml, version.json ו-.htaccess שמונע הגשה ממטמון. בלי זה קל להעלות exe בלי `latest.yml` ואז אף לקוח לא מקבל את העדכון.

**שתי החלטות תכנון שחשוב לא לשבור:**

1. **בטוח-כשל לכיוון הפתוח — ההפך מהרישוי.** מניפסט חסר, פגום או לא נגיש **אינו** נועל את המשתמש. אחסון שנפל הוא בעיה שלנו, לא של הלקוח; חסימה קורית רק על תשובה תקינה שאומרת במפורש שהגרסה לא נתמכת. כל ענף בהחלטה הזו מכוסה בבדיקות, כי טעות כאן נועלת לקוחות משלמים מחוץ למערכת.
2. **דרך מילוט.** אחרי שני כשלונות הורדה מוצעת הורדה ידנית מהאתר, כדי שאנטי-וירוס או רשת חסומה לא ישאירו לקוח תקוע בלי מוצא.

אפשר גם לכבות חיוב מרחוק: עריכת `version.json` על השרת ל-`mandatory: false` משחררת חסימה של גרסה שיצאה תקולה, בלי לחכות לגרסה מתקנת. ‏`minVersion` מאפשר לחייב רק את מי שמתחת לרף מסוים.

### Changed — 🔴 נוסף כלל: בכל עדכון גרסה — לעדכן את הקישור באתר ולהזכיר להעלות לשרת

הכלל המלא בראש הקובץ (כלל #2). הרקע: גרסה חדשה לא «יוצאת» כשהמתקין נבנה אלא כשהיא על השרת — עד אז אף לקוח לא מקבל אותה, וכפתור ההורדה באתר מפנה לקובץ ישן. חלוקת האחריות שנקבעה: **הסוכן** מעדכן את קישור ה-EXE בדף הנחיתה ו**אומר במפורש להעלות לשרת** בסיום כל בנייה, ואינו מניח שנתי זוכר; **נתי** מעלה בפועל את `public_html/download/` ואת `public_html/config.js`.

כדי שזה לא יסתמך על זיכרון — `npm run release` מעדכן לבד את `downloadUrl` ומדפיס צ׳ק-ליסט בסוף כל ריצה, וכפתור ההורדה באתר בונה את הקישור מהמניפסט בזמן ריצה.

### Changed — 🔴 נוסף כלל: כל שינוי חייב להיות מתועד כאן

הכלל המלא בראש הקובץ (כלל #1) וב-`PROJECT_CONTEXT.md`. הרקע: בדיקה של מה שהשתנה היום מול מה שמתועד גילתה שתוספות שנעשו **אחרי** כתיבת ערך ה-CHANGELOG לא נרשמו בו, ואף השאירו בקובץ שורה שכבר לא נכונה. מכיוון שאין git בפרויקט, הקובץ הזה הוא הזיכרון היחיד של מה נעשה ולמה — ולכן התיעוד נעשה בסוף כל שינוי, ולפני «סיימתי» בודקים בפועל אילו קבצים השתנו מאז הערך האחרון במקום להסתמך על הזיכרון.

### Changed — קישור ההורדה באתר מתעדכן לבד בכל פרסום

הקישור בדף הנחיתה היה מוצמד למספר גרסה בקוד, כלומר היה מתיישן בכל release ודורש זכירה ידנית. עכשיו כפתור ההורדה **קורא את `/download/version.json` בזמן ריצה** — אותו מניפסט שמתפרסם ממילא — ובונה ממנו את הקישור, כולל הצגת מספר הגרסה ליד הכפתור. הנתיב יחסי בכוונה: אותו מקור, בלי תלות ב-www ובלי צורך בהרשאת CORS.

- ה-`downloadUrl` ב-`config.js` נותר כ**גיבוי** למקרה שהמניפסט לא נטען, ו-`scripts/make-release.cjs` מעדכן גם אותו אוטומטית בכל בנייה — אין עריכה ידנית.
- הסקריפט מדפיס בסוף כל ריצה צ׳ק-ליסט של מה להעלות ולאן, כולל האזהרה על סדר ההעלאה ותזכורת לעדכן את הערות הגרסה.
- נוסף `.claude/launch.json` להרצת דף הנחיתה מקומית (`npx serve landing`).

### Changed — 🔴 נוסף כלל: אין לחשוף פגמים או סודות למשתמשים

הכלל המלא בראש הקובץ הזה וב-`PROJECT_CONTEXT.md`. בקצרה: ‏`CHANGELOG.md` פנימי ומפורט; מה שמגיע ללקוח עובר דרך `src/shared/release_notes.ts` בלבד ומנוסח בלשון תועלת. תיאור של פרצה שנסגרה מלמד תוקף בדיוק מה לחפש בגרסאות ישנות שעדיין מותקנות אצל לקוחות אחרים.

בדיקה אוטומטית אוכפת את זה: היא נכשלת אם בהערות הגרסה מופיעים נתיבי קבצים, כתובות שרת, פורמטים של מפתחות, או מילים כמו «פרצה» ו«עקיפה». (הבדיקה כבר תפסה ניסוח בזמן הכתיבה.)

‏19 בדיקות חדשות, ‏190 עוברות בסך הכל.

## 1.63.0 - 2026-08-01

### Added — מדריכי חיבור מודל באתר (`landing/guides/`)

תפריט «מדריכים» בסרגל העליון (נפתח בריחוף, ובמובייל כקבוצה בתפריט) מוביל לשלושה עמודים — ChatGPT · OpenAI, ‏Google Gemini, ו-Anthropic Claude. כל עמוד מסביר למי שלא עשה את זה מעולם איך משיגים מפתח API, איך קובעים תקרת הוצאה שלא תפתיע, ואיך לא לחשוף אותו.

- **הפער שהמדריכים סוגרים:** הטעות הנפוצה ביותר היא ההנחה שמנוי ChatGPT Plus / Claude Pro / Google AI Pro כולל גישת API. הוא לא — אצל כל השלושה זו מערכת חיוב נפרדת לגמרי, וזה הדבר הראשון שכתוב בכל עמוד.
- **דגש על תקרת הוצאה** בכל מדריך, כי זו ההגנה היחידה שעובדת גם אחרי שהמפתח כבר דלף. ב-OpenAI מוסבר במפורש שההתראה **אינה** עוצרת את החיוב וצריך להדליק את המתג של התקרה הקשיחה.
- **פרק אבטחה בכל עמוד**: מפתח API הוא אמצעי תשלום ולא סיסמה, עם נתונים אמיתיים (עלייה של 81% בחשיפת מפתחות AI בקוד ציבורי; מקרה מתועד של חיוב ~82,000$ תוך 48 שעות), רשימת «אל תעשה / תעשה», ומה עושים בפועל אם המפתח דלף.
- **הבדלים אמיתיים בין הספקים** ולא טקסט משוכפל: ‏Anthropic מאפשרת תאריך תפוגה למפתח ומשביתה אוטומטית מפתח שנחשף ב-GitHub ציבורי; ל-Gemini יש שכבה חינמית בלי כרטיס אשראי אבל הגנת ה-GitHub שלו חלשה יותר ומחיקת מפתח מתפשטת באיחור; ‏OpenAI דורשת אימות טלפון למפתח הראשון ומעניקה את בקרת ההוצאה הטובה ביותר.

**התוכן אומת מול התיעוד הרשמי, ותפס שלושה דברים שהיו נשלחים כשגיאות:** קונסולת Anthropic עברה מ-`console.anthropic.com` ל-`platform.claude.com`; מפתחות Gemini כבר לא מתחילים ב-`AIza` אלא ב-`AQ.`, והישנים ייפסלו אחרי ספטמבר 2026; ותיעוד OpenAI עבר ל-`developers.openai.com`.

סט האייקונים הוצא ל-`sprite.js` משותף (36 סמלים) במקום להשתכפל בכל עמוד. אומת בדפדפן: חמשת העמודים מחזירים 200, **אפס קישורים פנימיים שבורים**, אפס הפניות `<use>` לסמל חסר, ואין גלישה אופקית ב-375 ו-1440.

### Added — דף נחיתה למוצר (`landing/`) + טופס בקשת רישיון

אתר סטטי בעברית RTL ל-`nf-blaze.dev`, בלי שלב build ובלי תלויות: `index.html` + `styles.css` + `app.js` + `config.js`. הפלטה זהה לזו של האפליקציה, כך שהאתר והמוצר נקראים כמערכת אחת.

- **התוכן נכתב מול קריאה של הקוד**, לא מהזיכרון — כולל שלושה דברים שאסור לשווק: אין תקופת ניסיון (בוטלה ב-1.57.0), ‏Vercel עדיין לא בחיבור OAuth בלחיצה (העמוד מדבר על פרסום מיידי בלי חשבון, שזה נכון), ואין הבטחה של «מוצפן תמיד» למפתחות.
- **סט אייקונים ייעודי** — כל אייקון פונקציונלי צויר לעמוד הזה עם מוטיב חוזר של ניצוץ בן ארבע קרניים, כך שהסט נקרא כמשפחה אחת. סימני GitHub/Supabase/Vercel נשארו המקוריים, שם דווקא חשוב שיזוהו.
- **אנימציות**: כתמי אור נעים, גחלים עולים, חשיפה בגלילה, וחלון «הסוכן עובד» שמדפיס סבב עבודה אמיתי בלולאה. האנימציה מתחילה רק כשהחלון נראה — לא מבזבזת סוללה ברקע.
- **טופס בקשת רישיון** → Edge Function `request-license` → מייל ל-`Info@nf-blaze.dev` דרך Resend. מפתח ה-API של Resend לא יכול לשבת בדפדפן, ולכן הוא סוד בצד השרת. הגנות: מלכודת בוטים (בוט שממלא את השדה המוסתר מקבל «הצלחה» מזויפת ולא לומד מה נכשל), אימות קלט, חיתוך אורך, ומגבלת קצב של 3 בקשות ל-10 דקות לכל IP.
- **קישור ההורדה** מוגדר ב-`config.js` (ב-1.64.0 הוא הפך לאוטומטי — ראה שם).

אומת בדפדפן בפועל: **אין גלישה אופקית** ב-320/390/430/768 פיקסלים (נמדד `scrollWidth === clientWidth`), **כל יעדי המגע מעל 44px**, אין טקסט מתחת ל-12px, שדות הטופס ב-16px כדי למנוע זום אוטומטי ב-iOS, ‏`safe-area-inset` למכשירים עם מגרעת, ו-`prefers-reduced-motion` מכבה את כל התנועה. ארבעת מסלולי הטופס נבדקו מול הפונקציה החיה: שם חסר ומייל פסול נדחים ב-400 בעברית, מלכודת הבוטים מחזירה 200 מזויף, ובקשה תקינה מחזירה 503 מפורש עד שיוגדר מפתח Resend.

### Added — הסוכן פורס Edge Functions לפרויקט הלקוח

טבלאות ו-RLS נוצרות ב-SQL רגיל דרך `run_sql`, אבל ל-Edge Functions אין טבלה — הדרך היחידה ליצור אותן היא ה-Management API. לכן נוסף כלי נפרד.

- **כלי `deploy_edge_function` לסוכן**: מקבל `slug` וקוד TypeScript, ופורס לפרויקט המחובר. פריסה חוזרת של אותו slug **מעדכנת** את הקוד הקיים במקום ליפול על «כבר קיים» (בדיקה מקדימה ואז `PATCH` במקום `POST`). `verify_jwt` נשלט — ‏webhook מספק חיצוני חייב `false`.
- **הקוד נשמר גם בפרויקט** ב-`supabase/functions/<slug>/index.ts`, כך שמה שרץ בענן קיים גם בגיט ולא נעלם עם המחשב.
- **פרומפט המערכת מכוון את הסוכן להשתמש בזה** לכל לוגיקה שאסור שתרוץ בדפדפן — קריאה ל-API חיצוני עם מפתח סודי, webhooks, תשלומים, פעולות שדורשות service_role.
- שגיאת 403 מתורגמת לרמז מפורש שחסרה הרשאת «Edge Functions» באפליקציית ה-OAuth, במקום הודעת API גולמית.

### Changed — רשימת הבחירה נטענת מיד אחרי החיבור

אחרי «התחבר עם GitHub» היה צריך ללחוץ עוד פעם על «בחר ריפו קיים» כדי לראות את הרשימה — למרות שזו בדיוק הסיבה שבגללה מתחברים. עכשיו הרשימה נטענת אוטומטית מיד עם סיום החיבור, ואותו דבר ב-Supabase. הכפתור נשאר במצב «עובד» עד שהרשימה מוכנה, כדי שלא ייראה כאילו כלום לא קרה.

### Fixed — מסך «פרויקט חדש» עדיין שלח להנפקת טוקן ידנית

בטאב «ייבוא מ-GitHub», משתמש שלא חיבר חשבון קיבל קישור ל«צור token» — המסלול הישן. עכשיו מוצג שם אותו כפתור «התחבר עם GitHub», ומיד אחרי האישור נטענת רשימת הריפוזיטוריז שלו לבחירה.

### Added — חיבור חשבונות בלחיצה אחת: «Authorize NF-Blaze» ל-GitHub / Supabase / Vercel

עד היום כדי לחבר פלטפורמה המשתמש היה צריך ללכת לדשבורד שלה, ליצור Personal Access Token, להבין אילו הרשאות לסמן, ולהדביק אותו בשדה. זה חסם הפצה: אי אפשר לבקש מלקוח לא-טכני להנפיק טוקנים. עכשיו יש כפתור אחד — «התחבר עם GitHub» — שפותח את דף האישור של הספק בדפדפן, בדיוק כמו ב-Dyad ובכל כלי מקצועי אחר.

- **OAuth broker** (`license-portal/supabase/functions/oauth`, נפרס על פרויקט הרישוי): הוא הכתובת הרשומה אצל שלושת הספקים והיחיד שמחזיק את ה-`client_secret`. אפליקציית דסקטופ **לא יכולה** להחזיק סוד כזה — כל מי שמפרק את החבילה משיג אותו — וספקי OAuth דורשים כתובת חזרה קבועה ולא פורט אקראי במחשב הלקוח.
- **הטוקן לעולם אינו עובר בכתובת שהדפדפן רואה.** האפליקציה מייצרת `verifier` אקראי שנשאר אצלה ושולחת רק את ה-`challenge` (SHA-256) שלו. ה-broker מצפין את הטוקן במפתח שנגזר מה-challenge ומחזיר ללולאה המקומית **צופן בלבד**; רק מי שמחזיק את ה-verifier המקורי מקבל את הפענוח, דרך HTTPS. הצופן תקף 5 דקות.
- **ה-broker חסר-מצב** — אין טבלה ואין רשומות לנקות. כל ההקשר (ספק, פורט מקומי, challenge, מזהה) נוסע מוצפן וחתום ב-AES-GCM בתוך פרמטר ה-`state` של הספק, בתוקף 10 דקות.
- **המאזין המקומי מוגן**: מאזין על `127.0.0.1` בלבד, בפורט אקראי, בודק את כותרת ה-Host נגד DNS rebinding, ומשווה מזהה סשן בהשוואה עמידת-תזמון. פנייה זרה לפורט נדחית בלי להפיל את ההמתנה. פסק זמן 5 דקות, ולחיצה חוזרת מבטלת את הזרימה הקודמת ולא משאירה פורט תלוי.
- **Supabase — הקפיצה הגדולה בחוויה:** אחרי החיבור המשתמש **בוחר פרויקט מרשימה**, והמערכת מושכת בעצמה את כתובת הפרויקט ואת מפתח ה-anon דרך ה-Management API, כותבת `.env`, ונותנת לסוכן גישת `run_sql` — הכל בלי להעתיק URL, מפתח או Personal Access Token. הטוקן פג אחרי כיממה ומחודש אוטומטית מול ה-broker (ה-refresh token של Supabase חד-פעמי ומתחלף — נשמר תמיד החדש).
- **Vercel:** נעשה דרך מסלול ה-**Integration** ולא «Sign in with Vercel» — האחרון נותן זהות בלבד, וההרשאות ליצירת פרויקטים ודיפלויים נמצאות אצלם בביתא סגורה. מזהה הצוות (`teamId`) שחוזר ב-callback נשמר אוטומטית; בלעדיו כל קריאה ל-API של חשבון צוותי מוחזרת ב-403.
- **הזנה ידנית של טוקן נשארה** — מקופלת תחת «חיבור ידני (מתקדם)» בכל שלושת הכרטיסים, לחשבונות ארגוניים שחוסמים אישור אפליקציות ולפתרון תקלות.

- **גישת ה-SQL נבדקת ולא מונחת.** בקישור פרויקט מורצת שאילתת בדיקה (`select 1`) מול ה-Management API, והתג «הסוכן מריץ SQL» נקבע לפי התוצאה. בלי זה, משתמש ששכח לסמן את הרשאת `Database` היה רואה תג ירוק ומגלה את התקלה רק כשהסוכן נכשל באמצע עבודה.

אומת: 4 בדיקות יחידה על הזרימה המלאה בצד הלקוח (כולל אימות שקשר ה-PKCE נאכף בפועל — ה-verifier שנשלח בהחלפה הוא זה שממנו נגזר ה-challenge; דחיית מזהה שגוי; העברת שגיאת ספק כלשונה; ביטול זרימה קודמת), ‏163 בדיקות עוברות, typecheck ו-lint נקיים, ה-broker נפרס ונבדק חי (`/health` מחזיר 200, ‏`/exchange` עם צופן פגום נדחה ב-400 בעברית, ספק בלי סודות מוחזר 503), ואימות ויזואלי בצילומי מסך של האפליקציה הרצה — הכפתורים והמצבים המקופלים נכונים ב-RTL.

**אימות חי מקצה לקצה (01.08.2026):** ‏GitHub ו-Supabase נרשמו, הסודות הוגדרו, והזרימה המלאה נבדקה על האפליקציה הרצה — לחיצה על «התחבר», אישור בדפדפן, וחזרה אוטומטית למצב מחובר. ב-Supabase אומתו כל ההרשאות בפועל: שם הארגון נמשך (`Organizations`), ‏8 פרויקטים הוצגו לבחירה (`Projects`), מפתחות ה-anon וה-service_role נמשכו ונכתבו ל-`.env`/`.env.local` (`Secrets`), ושאילתת הבדיקה מול המסד עברה (`Database`). **Vercel טרם נרשם** — הכרטיס שלו מציג הודעה מפורשת והזנה ידנית נשארת זמינה.

הוראות רישום והגדרת סודות: `license-portal/OAUTH.md`.

## 1.62.0 - 2026-08-01

### Fixed — בורר האלמנטים בתצוגה המקדימה עבד רק בפרויקטים שנוצרו ב-NF-Blaze

בפרויקטים מיובאים / של Dyad הבחירה בתצוגה «לא עשתה כלום» — כי סקריפט הבורר מוזרק דרך plugin ב-vite.config, ופרויקטים זרים לא כללו אותו. תוקן:

- **הזרקה אוטומטית של ה-plugin** ל-vite.config של כל פרויקט Vite לפני הרצת התצוגה (`ensureNfSourceInViteConfig`). עכשיו הבורר עובד גם בפרויקטים מיובאים.
- **התלות ב-@babel הפכה אופציונלית** (טעינה עצלה) — פרויקט בלי babel עדיין מקבל את הבורר (בחירה לפי selector), רק בלי תיוג file/line. כך ההזרקה לעולם לא שוברת שרת פיתוח של פרויקט זר.
- הבחירה כבר הועברה לסוכן (הוא מתייחס לאלמנט שסומן); התיקון הוא בשכבת ההזרקה.

אומת: בדיקת יחידה על ה-patcher, ובדיקה חיה מקצה-לקצה על פרויקט Vite «זר» — סקריפט הבורר הוזרק, האלמנט הודגש ויזואלית, וה-selector נשלח (`button#btn`).

### Changed — מסך ההתקנה: עברית RTL מלאה, תיבת אישור, ומיתוג

- **RTL + עברית מלאה** בכל המתקין: יצרתי קובץ שפה עברי ל-MUI2 (`build/Hebrew.nsh`, נטען דרך ה-include path של electron-builder), והמתקין עבר לשפה עברית (LCID 1037). הטקסט מיושר לימין, פס הגלילה עובר לשמאל, וכל המחרוזות בעברית («הסכם רישיון», «הבא», «ביטול», «בחירת מיקום ההתקנה»…). ה-RTL מגיע מ-`Hebrew.nlf`.
- **תיבת סימון «קראתי ואני מאשר/ת את התקנון ותנאי השימוש»** בעמוד הרישיון — חובה לסמן כדי להמשיך (`MUI_LICENSEPAGE_CHECKBOX` ב-`build/installer.nsh`).
- **מיתוג בצבעי NF-Blaze**: תמונת כותרת ממותגת (לוגו NF) בכל המסכים + תמונת צד כהה במסך הסיום (`build/installer*.bmp`, נוצרות ב-`scripts/make-installer-art.ps1`).
- `warningsAsErrors: false` — מכיוון שכמה מחרוזות פנימיות של electron-builder אינן מתורגמות לעברית (אזהרה לא-קריטית); העמודים המרכזיים בעברית מלאה.
- **הוסר עמוד «בחירת משתמשים»** (`allowElevation: false`) — שהופיע עם תוויות ריקות (המחרוזות שלו לא תורגמו לעברית). ההתקנה היא per-user לתיקיית `AppData\Local\Programs` ממילא, כך שהעמוד היה מיותר. הזרימה עכשיה: רישיון → בחירת תיקייה → התקנה.

## 1.61.0 - 2026-08-01

### Added — הסוכן מריץ SQL ישירות מול ה-Supabase של הפרויקט (כמו Dyad), ופרסום ל-GitHub

עד היום חיבור Supabase לפרויקט נתן לסוכן רק *הקשר* לכתיבת קוד — ליצירת טבלאות הוא היה כותב SQL ומבקש מהמשתמש להריץ ידנית. עכשיו, אם מחברים גם **Access Token**, הסוכן מקבל גישה ישירה למסד.

- **כלי `run_sql` לסוכן** — מריץ SQL ישירות דרך ה-**Supabase Management API** (`POST /v1/projects/{ref}/database/query`), בדיוק המנגנון של Dyad. יצירת טבלאות + RLS, שינוי סכמה, קריאה/כתיבה של נתונים — הכל אוטומטי, בלי להעתיק SQL ידנית.
- **חיבור:** נוסף שדה «Access Token» בכרטיס Supabase (Personal Access Token מ-Supabase → Account → Access Tokens). נשמר מוצפן במחשב בלבד. בלי הטוקן — הסוכן חוזר להתנהגות הישנה (כותב SQL למשתמש). הטוקן מאומת מול המסד בזמן החיבור.
- **הגנה על פעולות הרסניות:** DROP / DELETE / TRUNCATE / ALTER…DROP נחסמים אוטומטית — הסוכן חייב להזהיר את המשתמש בצ׳אט, לקבל אישור מפורש, ורק אז להריץ עם `confirm=true`. זיהוי ההרסניות מתעלם ממילות מפתח בתוך מחרוזות והערות.
- **יומן SQL:** כל הרצה נרשמת ל-`.nf-blaze/sql-log.jsonl` בפרויקט, לביקורת.

אומת: בדיקות יחידה (זיהוי הרסני, חילוץ ref), ובדיקת אינטגרציה חיה מול Supabase אמיתי — CREATE רץ, DROP בלי אישור נחסם והטבלה שרדה, DROP עם אישור מחק. המפתח הפרטי של הרישוי אינו קשור לזה; זהו טוקן של המשתמש לפרויקט שלו, וה-URL/anon המוטמעים נשארים כפי שהם.

### Added — פרסום הפרויקט ל-GitHub מתוך המערכת

- **כלי `publish_github` לסוכן** + כפתורים במסך הפרויקט:
  - פרויקט **מקושר** לריפו (יובא מ-GitHub) → נוצר **ענף חדש** (`nf-blaze-<אקראי>`) בלי לגעת בראשי — נוח לסקירה/PR.
  - פרויקט **לא מקושר** (אבל חשבון GitHub מחובר) → נוצר **ריפו חדש** בחשבון המשתמש והקוד מועלה — בדיוק כמו חיבור GitHub הראשוני.
- הסוכן יודע לפרסם כשמבקשים ממנו בצ׳אט («העלה/פרסם/גבה ל-GitHub»); הטוקן מוזרק לכתובת ה-remote רק לרגע ה-push ולא נשאר על הדיסק.
- אומת: בדיקות יחידה (שם ענף, בחירת מצב), ובדיקת מכניקת git חיה מול remote מקומי — main נשאר ללא שינוי, נוצר ענף חדש עם השינויים. יצירת ריפו משתמשת בקוד GitHub הקיים במערכת.

## 1.60.0 - 2026-08-01

### Added — אימות רישיון מקוון + ביטול מרחוק (מודל Office 365 / Adobe)

שדרוג מהותי לאבטחת הרישוי: המערכת מאמתת את הרישיון מול Supabase מדי פעם, עם חלון חסד אופליין. זה סוגר את הפרצה האחרונה של הגנת השעון (שעון השרת אמין ואי אפשר לזייף אותו) ומוסיף ביטול רישיון מרחוק.

- **פונקציית `validate-license` ב-Supabase** (על אותו פרויקט הרישוי). מקבלת מפתח + nonce, בודקת מול `nfb_licenses` (קיים / בוטל / פג לפי שעון השרת), ומחזירה תשובה **חתומה ב-Ed25519**. המפתח הפרטי נשאר ב-Secrets של השרת; האפליקציה מאמתת את החתימה עם המפתח הציבורי המוטמע — כך ששרת מזויף/MITM לא יכול לזייף «תקף», וה-nonce מונע שידור חוזר. חתימת המפתח הנכנס מאומתת בשרת לפני כל פנייה למסד, כך שניחוש מזהים לא מחזיר דבר.
- **חלון חסד אופליין (30 יום).** המערכת עובדת אופליין כרגיל; רק אם עברו 30 יום בלי אימות מקוון מוצלח היא מבקשת «בדוק שוב» (מסך ייעודי, המפתח נשאר שמור). מכיוון ש-NF-Blaze ממילא זקוקה לרשת כדי לפנות למודלים, האימות מתרחש באופן טבעי ובלתי-מורגש.
- **ביטול רישיון מרחוק.** מפעיל את שדות ה-`revoked` בטבלה: ביטול בפורטל → הרישיון מת באימות הבא, והמערכת חוזרת חי למסך הרישיון. מצב הביטול קשור למזהה הרישיון וחתום ב-HMAC, כך שהפעלה-מחדש של אותו מפתח אינה מאפסת אותו.
- **זמן השרת מחזק את הגנת החזרת-השעון.** כל אימות מוצלח נוטע עוגן זמן אמין (high-water), כך שגם ההגנה האופליין מתחזקת. החזרת שעון אינה מאריכה את חלון החסד.
- אימות מקוון רץ בעלייה, כל 6 שעות ברקע, ומיד אחרי הפעלת מפתח. כשל רשת אינו נועל — נשארים על החסד.

אומת מקצה-לקצה: בדיקות יחידה טהורות (חסד, חתימת תשובה, החלטת ביטול), בדיקת השרת החי (תקף/בוטל/פג/מפתח-מזויף/מזהה-לא-קיים), ובדיקת UI חיה מול האפליקציה הבנויה (הפעלה→אימות→ביטול-מרחוק→שער חי→ביטול-הביטול→תקף).

**מגבלה מובנית:** תוקף שנשאר אופליין וגם מקפיא את השעון יכול להאריך את חלון החסד — אבל ברגע שהוא מתחבר (וכדי להשתמש במודלים הוא חייב), הביטול נתפס. זה המודל של Office/Adobe.

### Changed — אזהרת תפוגה 12 שעות לפני (במקום יום)

הפופ-אפ שמזהיר לפני תפוגת הרישיון עבר מגרנולריות של יום לגרנולריות של שעות: הוא נדלק כשנותרו **12 שעות או פחות** לתוקף (`warnExpirySoon`/`hoursLeft` מחושבים מול הזמן האמין), ומציג את מספר השעות שנותרו. מוצג פעם אחת לכל תאריך תפוגה.

## 1.59.0 - 2026-07-31

### Security — נסגרו שתי עקיפות של אכיפת הרישיון

בעקבות בדיקת חדירה על מנגנון הרישוי. התקפות הזיוף הקריפטוגרפי (שינוי payload עם חתימה ישנה, חתימה עצמית בזוג מפתחות זר) כבר נחסמו כראוי על ידי Ed25519. נמצאו ותוקנו שתיים:

- **החזרת שעון המערכת אחורה (anti-rollback).** אימות התפוגה הסתמך על השעון הנוכחי בלבד, כך שלקוח עם רישיון שפג יכל להחזיר את שעון Windows אחורה ולהחיות אותו. אומצה **גישת מקורות-זמן מרובים** כמו ב-Office / Adobe / FlexNet: התפוגה נבדקת מול «זמן אמין» = המקסימום על ארבעה מקורות מונוטוניים —
  1. שעון המערכת;
  2. **high-water mark** מוצפן ב-safeStorage וחתום ב-HMAC (עריכה ידנית של הערך נפסלת), ששורד הסרת רישיון;
  3. **תאריך ההנפקה החתום (`iat`)** שברישיון — רצפה שאי אפשר לזייף בלי המפתח הפרטי;
  4. **ה-mtime הרחוק ביותר של קבצי האפליקציה** — עותק מבוזר (בסגנון SLStore של Adobe) ששורד מחיקה של קובץ ה-high-water היחיד, בתוספת קובץ עוגן ייעודי.
  
  מקור זמן «עתידי» אבסורדי (שעון/קובץ שהוגדר לשנה עתידית) נחתך מול תקרה כדי לא לנעול לקוח משלם, ותיקוני NTP/אזור-זמן קטנים אינם נחשבים מניפולציה (חלון חסד). כשמזוהה החזרת שעון מוצגת הודעה מפורשת (בסגנון «clock set back» של Adobe). כל הענפים נבדקו ביחידות טהורות ובאינטגרציה מול השירות האמיתי, כולל התרחיש של מחיקת ה-high-water + החזרת שעון.
- **יום שימוש עודף אחרי התפוגה.** תנאי התפוגה היה `daysLeft < 0`, אך `daysUntil` מחזיר 0 לאורך כל היום שאחרי תאריך התפוגה — כלומר הרישיון נשאר תקף יום שלם מעבר למה ששולם. תוקן ל-`<= 0`: תאריך התפוגה הוא היום האחרון בתוקף.

**מגבלה מובנית:** אימות אופליין לחלוטין. תוקף שמוחק את *כל* מקורות הזמן (קובץ ה-high-water, קובץ העוגן, וכל קובץ אפליקציה עם mtime עדכני) *וגם* שולט בשעון, יכול עדיין לאפס רישיון מוגבל-זמן עד תאריך ההנפקה — זה גבול מהותי של רישוי בלי שרת (גם Office/Adobe אינם חסינים אופליין). המיתון המלא הוא אימות מקוון תקופתי (נדחה בכוונה). ה-`iat` החתום מונע חזרה לפני מועד ההנפקה בכל מקרה.

### Added — תגובה חיה למצב הרישיון

- **הסרת רישיון מוציאה מיד לשער.** קודם `LicenseCard` עדכן רק את עצמו והמשתמש נשאר בתוך המערכת עד הפעלה מחדש. השינוי מדווח ל-App, והשער מופיע מיד.
- **תפוגה חיה בזמן אמת.** ה-renderer בודק את סטטוס הרישיון כל 30 שנ' ובכל חזרה לחלון. ברגע שהרישיון פג — גם תוך כדי עבודה — המערכת חוזרת למסך הרישיון בלי צורך בהפעלה מחדש. שכבת ה-IPC ממילא חוסמת כל פעולה אחרי תפוגה; הבדיקה החיה היא ל-UX.
- **פופ-אפ «נשאר יום אחד».** ביום האחרון לתוקף קופצת הודעה מעוצבת בעברית (לפי שפת העיצוב של המערכת) המיידעת שהמערכת תבקש מפתח חדש למחרת. מוצג פעם אחת לכל תאריך תפוגה.

כל השלושה אומתו בבדיקת UI חיה מול האפליקציה הבנויה (הסרה→שער, פופ-אפ יום אחרון, איבוד רישיון→שער דרך הפולינג), בנוסף לבדיקות יחידה על מחזור החיים.

## 1.58.2 - 2026-07-31

### Fixed — מטמון הדפדפן החזיק גרסה ישנה של הפורטל

אחרי הידוק ההרשאות ב-1.58.1, דפדפן ששמר את `index.html` הישן המשיך לשלוח `select('*')` וקיבל `permission denied for table nfb_licenses` — למרות שהקובץ שעל השרת היה תקין. נוספו כותרות מניעת מטמון ל-`index.html` ו-`.htaccess` לאחסון מבוסס Apache. הפתרון המיידי למי שכבר נתקל: `Ctrl+Shift+R`.

## 1.58.1 - 2026-07-31

### Fixed — נעילת ההרשמה בפורטל לא שרדה מחיקת משתמש

הנעילה נגזרה מקיום שורה ב-`auth.users`, ולכן כל מחיקת חשבון המנהל פתחה מחדש את מסך «הקמת חשבון מנהל» לכל מי שהגיע לאתר — במקום מסך כניסה. הנעילה הועברה לדגל קבוע ב-`nfb_portal_state` שנדלק פעם אחת ואינו מתאפס מעצמו. התפיסה נעשית אטומית (‏`update ... where admin_bootstrapped = false`) כך ששתי בקשות במקביל לא ייצרו שני מנהלים, ומשוחררת אם יצירת המשתמש נכשלה. שחזור גישה מכוון מתועד ב-README כפקודת SQL.

### Changed — מפתח הרישיון מוצג פעם אחת בלבד

הרשימה בפורטל מציגה מעתה את **הלקוחות** (שם, אימייל, תאריך, תוקף, סטטוס) בלי המפתח; המפתח מופיע רק ברגע ההנפקה, להעברה ללקוח. ההסתרה נאכפת במסד ולא רק בממשק: הרשאת ה-SELECT ברמת הטבלה נשללה מ-`authenticated` והוענקה מחדש רק לעמודות המותרות, כך ש-`select=*` ובקשה מפורשת של `license_key` מוחזרות ב-403 גם עם הטוקן של המנהל.

**הערה:** ניסיון ראשון לשלול הרשאה ברמת העמודה בלבד לא השפיע — הרשאת SELECT ברמת הטבלה מכסה את כל העמודות וגוברת עליה. אומת בבדיקה מול הפרויקט החי לפני ואחרי התיקון.

## 1.58.0 - 2026-07-31

### Added — פורטל ניהול רישיונות (אתר) ‏`license-portal/`

אתר פרטי להנפקת מפתחות ולניהול רשימת הלקוחות, מחובר ל-Supabase (`ilsidpixxkfwowsnkntg`):

- **חשבון מנהל יחיד:** ההרשמה נפתחת פעם אחת בלבד. ברגע שנוצר המנהל, ה-Edge Function מחזירה 403 לכל ניסיון נוסף — מסך ההרשמה נעלם לתמיד ומוחלף במסך כניסה.
- **יצירת רישיון:** שם מלא של הלקוח, תאריך תוקף (או «ללא הגבלת זמן»), וכפתור אחד. המפתח נוצר בפורמט זהה ל-`scripts/license.cjs` (‏`NFB1.<payload>.<ed25519>`), כך ששני המסלולים מנפיקים מפתחות תואמים.
- **המפתח הפרטי לעולם אינו בדפדפן** — החתימה נעשית ב-Edge Function עם סוד הפרויקט (`LICENSE_PRIVATE_KEY`), והכתיבה למסד ב-`service_role` כך שאי אפשר להזריק רשומות מהלקוח.
- **טבלת רישיונות:** שם, אימייל, תאריך יצירה, תוקף, סטטוס (פעיל / פג / ללא הגבלה) והעתקת המפתח בלחיצה. ‏RLS מרשה קריאה למשתמש מחובר בלבד.
- ממשק עברית RTL בצבעי NF-Blaze; העמוד מסומן `noindex, nofollow`.
- **חסימת רישיון נדחתה לשלב הבא** לבקשת המשתמש. עמודות `revoked`/`revoked_at` כבר קיימות בסכמה, בלי ממשק ובלי לוגיקה.

הפריסה בוצעה ואומתה מקצה לקצה מול הפרויקט החי: הרשמה ראשונה עברה, ניסיון הרשמה שני נחסם ב-403, הנפקה ללא טוקן נדחתה ב-401, המפתח שהונפק אומת חתימתית מול המפתח הציבורי של האפליקציה והפעיל אותה בפועל, ו-RLS החזיר 0 שורות לקורא לא מחובר מול השורה שנראתה למחובר.

### Added — «ברוך הבא <שם>» בסרגל העליון

- אחרי הפעלת רישיון, השם שהוזן בפורטל מוצג בסרגל העליון של האפליקציה, עם תאריך התפוגה כ-tooltip. אומת בצילום מסך של האפליקציה הרצה.

## 1.57.0 - 2026-07-31

### Changed — 🔴 בלי מפתח רישיון אין גישה למערכת

- **תקופת הניסיון של 14 יום בוטלה.** היא נשענה על `installedAt` בקובץ הגדרות רגיל
  (`%APPDATA%/nf-blaze/nf-blaze-data/settings.json`) — כלומר **כל אחד היה יכול לאפס אותה
  לנצח בעריכת שורה אחת בקובץ טקסט, בלי שום ידע טכני**. עכשיו: אין מפתח → אין גישה.
- **האכיפה עברה לתהליך הראשי — וזה השינוי המהותי.** קודם מסך החסימה היה **UI בלבד**:
  שכבת ה-IPC ענתה לכל בקשה גם בלי רישיון, ולכן כל דרך לעקוף את המסך פתחה את המערכת
  במלואה. עכשיו `ipcMain.handle` עטוף בשומר שבודק רישיון בכל קריאה, עם **רשימת היתר
  מפורשת וקצרה** (הפעלת רישיון, גרסה, בדיקת סביבה, קריאת הגדרות). **ערוץ שאינו ברשימה
  חסום כברירת מחדל — כולל ערוצים שייווספו בעתיד** (fail-closed).
- אומת מקצה לקצה על האפליקציה: בלי מפתח מוצג מסך «נדרש מפתח רישיון» ו-`projects:list`
  נדחה מהתהליך הראשי; אחרי הדבקת מפתח חתום תקין המסך נפתח, הפרויקטים נטענים מיד
  והמצב `licensed`. הסרת הרישיון מחזירה לחסימה.
- **תיקון נלווה:** בלי רישיון המסך נתקע ב«טוען…» (טעינת הפרויקטים נכשלה ולא שחררה את
  מצב הטעינה) — עכשיו כשל טעינה במצב חסום מוביל ישירות למסך הרישיון.

### Tests

- ‏9 בדיקות חדשות (`tests/license_gate.test.ts`): אין מסלול ניסיון בשום תאריך, מפתח פסול
  נדחה, ערוצים רגישים חסומים בלי רישיון, **ערוץ חדש שלא הוגדר נחסם אוטומטית**, וערוצי
  ההפעלה נשארים פתוחים כדי שאפשר יהיה בכלל להזין מפתח.

### Changed — הקשחת הגנה על ההתקנה (נגד שינוי והעתקה)

שכבת הגנה חדשה ברמת הבינארי — **Electron Fuses**, נעילות שנצרבות בקובץ ה-EXE עצמו בזמן האריזה:

- **`runAsNode: false`** — אי אפשר להריץ את NF-Blaze.exe כ-Node גנרי (`ELECTRON_RUN_AS_NODE`) כדי להריץ דרכו קוד זר או לחלץ מודולים.
- **`enableNodeOptionsEnvironmentVariable: false`** — הזרקת קוד דרך משתנה הסביבה `NODE_OPTIONS` (למשל `--require` של סקריפט זדוני) חסומה.
- **`enableNodeCliInspectArguments: false`** — אי אפשר לחבר debugger (`--inspect`) לתהליך ולשאוב את הקוד מהזיכרון.
- **`enableEmbeddedAsarIntegrityValidation` + `onlyLoadAppFromAsar`** — קוד האפליקציה נטען אך ורק מה-asar, ו**עריכה של קוד האפליקציה מונעת עלייה**. אומת בפועל: שיבוש בייט בודד ב-`out/main/index.jsc` (לוגיקת הליבה) בתוך ה-asar → האפליקציה **לא עולה כלל** (אין חלון, התהליך מת). כך נחסמת עקיפת רישוי בדרך של עריכת הקוד הארוז.
- **DevTools כבויים בגרסה הארוזה** — אין עיון בקוד ה-renderer ובתעבורת ה-IPC דרך כלי הפיתוח.

**מה אומת בפועל על החבילה הבנויה:**

- מצב ה-fuses נקרא ישירות מהבינארי: ‏`RunAsNode=0`, ‏`EnableNodeOptionsEnvironmentVariable=0`, ‏`EnableNodeCliInspectArguments=0`, ‏`EnableEmbeddedAsarIntegrityValidation=1`, ‏`OnlyLoadAppFromAsar=1`, ‏`EnableCookieEncryption=1`.
- הרצה עם `ELECTRON_RUN_AS_NODE=1` והדגל `-e` **לא הריצה את הקוד** — הדגל נבלע והאפליקציה עלתה כרגיל.
- האפליקציה הארוזה עולה ועובדת (צילום חלון של 1.57.0 עם רשימת הפרויקטים); ה-Node המצורף שמריץ את פרויקטי המשתמש לא הושפע.

**גבולות ההגנה (חשוב לומר ביושר):**

- אימות שלמות ה-asar הוא **ברמת קובץ ובעצלתיים** — הוא נאכף כשקובץ נקרא. שיבוש בייט בתוך תלות שאינה נטענת בעלייה **לא** מנע הרצה (נבדק). ההגנה אפקטיבית על קוד האפליקציה עצמו, שהוא מה שתוקף היה רוצה לשנות.
- תוכנה שרצה אצל הלקוח לעולם אינה חסינה ב-100%. השכבות יחד — bytecode לליבה, רישוי חתום קשור-מחשב, והנעילות החדשות — הופכות העתקה ושינוי ממשהו שעושים בעורך טקסט למאמץ הנדסי ממשי. ההגנה המשפטית (תקנון + רישיון UNLICENSED) משלימה את התמונה.
- **חתימה דיגיטלית עדיין חסרה** והיא המשלימה הטבעית של סעיף זה — בלעדיה אפשר להחליף את ה-EXE כולו.

### Changed — אימות E2E על החבילה הארוזה עבר ל-PrintWindow

- נעילת ה-inspector חוסמת גם את מנגנון ההנעה של Playwright ל-Electron (הוא מסתמך על ה-Node inspector), ולכן **אי אפשר יותר לנהוג את הגרסה הארוזה דרך Playwright** — זה תקין ומכוון. אימות ויזואלי של החבילה הבנויה נעשה עכשיו בהרצה ישירה + `PrintWindow` דרך PowerShell. פיתוח (`electron .`) לא הושפע.

## 1.56.0 - 2026-07-31

### Changed — הצ'אט מרגיש חי: מכונת כתיבה במקום «בום» של טקסט

- **חשיפת מכונת-כתיבה לטקסט החי:** קודם כל צבר הטוקנים נשפך למסך במכה אחת כל ~80ms — עם ספקים ששולחים chunks גדולים (Gemini) זה נראה כקפיצות רובוטיות של פסקאות שלמות. עכשיו טיקר (50ms) חושף את הטקסט בהדרגה: לפחות 2 תווים לטיק, ומדביק את הפיגור באופן יחסי (עשירית מהצבר בכל טיק) — מהיר כשמאחור, עדין כשקרוב, ונעצר לבד כשאין מה לחשוף. הביצועים נשמרים: עדכון state אחד לכל טיק, לא לכל טוקן.
- **אומת מקצה לקצה** (Playwright על האפליקציה הבנויה, סבב Gemini אמיתי): הטקסט גדל ב-5 מדרגות קטנות (חציון 41 תווים) במקום להופיע בבת אחת, והצילום באמצע הזרימה תפס אותו קטוע באמצע מילה.

### Fixed — רנר ה-Evals קרס על ניקוי תיקייה נעולה ב-Windows

- **כשל `EBUSY` במחיקת תיקיית העבודה הזמנית הפיל את כל הסוויטה** אחרי המשימה הראשונה (נמצא בריצת ה-evals האמיתית הראשונה): ב-Windows תהליך שזה עתה נסגר או אנטי-וירוס מחזיקים את הקבצים הבינאריים (esbuild, rollup) עוד רגע אחרי הסיום. עכשיו הניקוי מנסה שוב (5 ניסיונות, 250ms ביניהם), וכשל ניקוי נרשם כאזהרה וממשיכים למשימה הבאה — תיקיית temp לא שווה איבוד ריצה שלמה.
- **הרנר לא סיים לצאת אחרי משימות BUILD** (נמצא גם הוא בריצה האמיתית): המנוע משאיר handles פתוחים (workers/תהליכי-בן) שמחזיקים את ה-event loop, והתהליך נשאר תלוי לנצח **אחרי** שהדוח כבר נכתב — נצפה בפועל 23 דקות של המתנה סתמית. נוספה יציאה מפורשת (`process.exit`) אחרי כתיבת הדוח, עם השהיה קצרה לריקון stdout.
- **תיקיות העבודה עברו מ-%TEMP% אל `evals/.work` בתוך הפרויקט**: ‏Storage Sense / כלי ניקוי של Windows מחקו תיקיית עבודה **באמצע משימה רצה** והריצה נכשלה על «תיקיית פרויקט המשתמש לא קיימת». תיקייה בתוך הפרויקט לא נסרקת על ידי מנקי Temp.

### Fixed — «בצע את זה» הופיע גם על שאלות תמימות

- **הבאנר «מצב ASK — ללא שינויי קבצים» עם כפתור «בצע את זה» הופיע אחרי כל שאלה**, גם על «מה אתה חושב על הפרויקט?» — שאין בה שום דבר לבצע. עכשיו הוא מופיע רק כשיש באמת מה להריץ: מצב PLAN (נוצרה תוכנית), או שהסוכן ניסה לכתוב קבצים ונחסם על ידי המצב (`modeBlockedWrites`). שאלת מידע רגילה מקבלת תשובה ותו לא — כמו שמצופה ממוצר חכם.
- זיהוי בקשות בנייה אמיתיות ממשיך לעבוד דרך מזהה הכוונה במצב «אוטומטי» — «תבנה לי דף» מנותב ל-BUILD כרגיל; הבאנר הוא רק רשת ביטחון למקרה שהסוכן רצה לפעול ונעצר.

## 1.55.0 - 2026-07-31

### Fixed — הצ'אט החי «נעלם» בסוף הסבב והוחלף בתוכן אחר

שני באגים שביחד גרמו לצ'אט להרגיש שבור לעומת סטרימינג חלק כמו בכלים המקצועיים:

- **מה שנשמר לצ'אט לא היה מה שהמשתמש ראה זורם.** ההודעה שנשמרה בסוף סבב הייתה טקסט **האיטרציה האחרונה בלבד** (`loopResult.text`) — כל ההסברים שהמודל כתב תוך כדי עבודה (בין קריאות כלים) נעלמו ברגע שהסבב הסתיים, והוחלפו בסיכום קצר ושונה. עכשיו נשמר הטקסט המצטבר המלא שהוזרם בלייב; זנב שלא הוזרם (למשל סיכום בדיקת דפדפן שמצורף אחרי הלולאה) מזוהה לפי עוגן ומצורף בלי כפילות (`mergeStreamedWithFinal` ב-`llm.ts`, עם 6 בדיקות).
- **ההחלפה בין הבועה החיה להודעה הסופית לא הייתה חלקה.** ההודעה הסופית נכנסה לרשימה בזמן שהבועה החיה עוד מוצגת (תוכן כפול), והבועה נעלמה רק אחרי כל הרענונים שבסוף הסבב — כולל בנייה מחדש של תצוגה במצב גיבוי, כלומר שניות של קפיצות. עכשיו ההודעה הסופית נטענת והבועה מוסתרת **באותו רנדר** (דגל `liveReplaced`), והרענונים רצים אחר כך.
- **גלילה מיידית בזמן סטרימינג:** גלילת `smooth` שהופעלה מחדש כל ~80ms אף פעם לא השיגה את הטקסט ונראתה כקפיצות; בזמן סטרימינג הגלילה עכשיו מיידית, ו-smooth נשאר להודעות רגילות.
- **אומת מקצה לקצה על האפליקציה הבנויה** עם סבב Gemini אמיתי (Playwright): הטקסט החי נדגם באמצע זרימה (נקטע באמצע מילה — הוכחה שהוא מוזרם בהדרגה), וההודעה הסופית שנשמרה מתחילה בדיוק בטקסט שהוזרם וממשיכה אותו — בלי היעלמות ובלי החלפה.

## 1.54.0 - 2026-07-31

### Changed — סרגל חלון בעיצוב המערכת

- **תפריט Electron הוסר.** `File / Edit / View / Window / Help` היה תפריט ברירת המחדל של Electron ולא שימש לכלום — כל הפעולות נמצאות בסרגל של המערכת עצמה.
- **כפתורי המזעור/הגדלה/סגירה נצבעו בצבעי המערכת** ויושבים בתוך סרגל האפליקציה, בגובה 56px. הם נשארו **כפתורים מקוריים של Windows**, כך ש-Snap Layouts, קיצורי מקלדת ונגישות ממשיכים לעבוד — בניגוד לכפתורים מצוירים ידנית.
- אפשר לגרור את החלון מכל שטח ריק בסרגל.
- **אומת ויזואלית על האפליקציה הרצה.** הצילום חשף ש-Electron ממקם את הכפתורים ב**צד שמאל** כשהממשק בעברית; ההנחה ההפוכה גרמה לכפתור «פרויקט חדש» להיחתך מתחתיהם. תוקן ואומת שוב.
- במערכות שאינן Windows נשארת מסגרת חלון רגילה.

## 1.53.0 - 2026-07-31

### Fixed — 🔴 סריקת האבטחה דיווחה «נקי» בלי לבדוק כלום

אותה הגבלת-נקודה התגלתה גם בסורק ה-RLS, ושם היא הייתה חמורה בהרבה:

- **מה קרה:** מיפוי הטבלאות פנה ל-`/rest/v1/` עם מפתח anon, קיבל 401, והחזיר **רשימה ריקה בלי לזרוק שגיאה**. הסורק עבר על אפס טבלאות וסיים בלי ממצאים — כלומר **הציג «אין בעיות אבטחה» מבלי שבדק ולו טבלה אחת**. משתמש עם טבלה פתוחה לגמרי לאורחים היה מקבל אישור ירוק.
- **התיקון:** המיפוי משתמש קודם ב-`service_role` (המפתח היחיד שמורשה לקרוא את הסכמה) ונופל חזרה ל-anon. בדיקת ה-RLS עצמה ממשיכה להשתמש ב-anon — זו כל מהות הבדיקה.
- **הכי חשוב:** כשל מיפוי כבר **אינו נספר כ«נקי»**. הוא מחזיר אזהרה מפורשת: «סריקת טבלאות Supabase לא בוצעה — אין לפרש את זה כאישור שאין טבלאות פתוחות», עם הנחיה להוסיף את מפתח ה-service_role.

## 1.52.0 - 2026-07-31

### Fixed — חיבור Supabase: הסיבה האמיתית

הודעת השגיאה המפורטת שנוספה ב-1.51.0 חשפה מיד את הבעיה האמיתית, ש**לא** הייתה כותרת ה-Bearer:

- **בדיקת החיבור פנתה ל-`/rest/v1/`** — נקודה שמחזירה את סכמת ה-OpenAPI ו**מוגבלת ל-`service_role` בלבד**. מפתח `anon` תקין לחלוטין נדחה שם עם «Only the `service_role` API key can be used for this endpoint». כלומר: כל מי שהזין פרטים נכונים קיבל שגיאה.
- הבדיקה עברה ל-**`/auth/v1/health`**, שמאמת את המפתח ומקבל `anon` ו-`publishable` כאחד. אומת מול ה-API החי.
- **הגנה נוספת:** אם Supabase תחזיר בעתיד הודעת הגבלת-נקודה כלשהי, היא נספרת כ**הצלחה** — היא מוכיחה שהמפתח התקבל. מפתח פסול אמיתי עדיין נדחה.

## 1.51.0 - 2026-07-31

### Fixed — חיבור Supabase נכשל ב-401 גם עם פרטים נכונים

- **הסיבה:** המפתח נשלח גם ב-`apikey` וגם ב-`Authorization: Bearer`. מפתחות הדור החדש של Supabase (`sb_publishable_…`) **אינם JWT**, ו-Supabase מתעדת במפורש שאסור לשלוח אותם ככותרת Bearer — הבקשה נדחית לפני שהיא מגיעה לפרויקט.
- עכשיו הכותרות נקבעות לפי סוג המפתח: **publishable → `apikey` בלבד**, **JWT מהדור הישן → שניהם**, כמו קודם. אומת מול ה-API האמיתי של Supabase.
- **הודעת השגיאה הפסיקה לבלוע את האבחון.** קודם הוצג «מפתח לא תקין או אין הרשאה (401/403)» בזמן ש-Supabase החזירה הסבר מדויק. עכשיו התשובה שלה מוצגת כלשונה («Invalid API key · Double check your API key»), יחד עם הנחיה מאיפה להעתיק את המפתח.
- **זיהוי מפתח בשדה הלא נכון:** הדבקת `service_role` בשדה של anon נתפסת מיד עם הסבר, במקום לדחות בשגיאה סתומה.
- **ניקוי הדבקה מלוכלכת:** רווח, שורה חדשה או תו רוחב-אפס שנדבקו עם המפתח מוסרים — הם היו נשמרים גם לקבצי ה-`.env` ושוברים את האפליקציה בזמן ריצה.
- כשל רשת (כתובת שגויה, אין אינטרנט) מוצג כשגיאת רשת ולא כמפתח פסול.

### Fixed — שגיאות GitHub ו-Vercel הוצגו באנגלית

- אומת שהשניים באמת פונים ל-API (לא תצוגה בלבד): GitHub מחזיר 401 «Bad credentials», Vercel מחזיר 403 «Not authorized».
- שתי ההודעות תורגמו לפעולה בעברית — מה קרה, ומה לעשות כדי לתקן.

## 1.50.0 - 2026-07-31

### Added — ייבוא פרויקט ישירות מ-GitHub

- אפשרות שלישית בחלון «פרויקט חדש»: **ייבוא מ-GitHub**. מדביקים כתובת ריפו, בוחרים תיקייה ריקה — הריפו משוכפל, התלויות מותקנות והתצוגה החיה עולה.
- **מקבל כל צורה שמשתמש עלול להדביק:** `https://github.com/owner/repo`, עם `.git`, `git@github.com:owner/repo`, או פשוט `owner/repo`.
- **ריפו ציבורי עובד בלי חיבור בכלל.** מי שחיבר חשבון GitHub מקבל גם **רשימה נפתחת של הריפוזיטוריז שלו** לבחירה בקליק, וגישה לריפו פרטי.
- שם הפרויקט נגזר אוטומטית משם הריפו אם משאירים ריק.
- **הטוקן לא נשאר על הדיסק** — הוא מוזרק לכתובת רק לרגע השכפול, ואז ה-remote מוחזר לכתובת נקייה.
- הודעות שגיאה מדויקות: ריפו פרטי בלי חיבור, טוקן בלי הרשאה, ריפו לא קיים. ואם השכפול הצליח אבל התיקייה אינה פרויקט Node — נאמר במפורש היכן הריפו כבר יושב, במקום להשאיר מבוי סתום.

### Added — קישור «מאיפה משיגים את הפרטים» בכל חיבור

- ליד סטטוס החיבור של **GitHub**, **Supabase** ו-**Vercel** נוסף קישור שפותח ישירות את העמוד שבו מפיקים את הטוקן / ה-URL והמפתח. קודם המשתמש ראה «הזן טוקן» בלי שום רמז מאיפה לקחת אותו.
- הטקסט משתנה לפי מצב: «צור token» כשלא מחובר, «נהל טוקנים» כשכן.

## 1.49.0 - 2026-07-31

### Fixed — סרגל הכלים מעל הצ'אט נחתך

- **הבעיה:** 7 כפתורי פעולה + בלוק הכותרת נדחסו לפאנל של 380px בלי אפשרות לרדת שורה, אז האייקונים בקצה פשוט נחתכו ולא היה ברור מה הם.
- **פאנל הצ'אט הורחב** מ-380px ל-440px (ובמסך צר מ-340 ל-400).
- **שורת הפעולות יורדת שורה בשלמותה** במקום להידחק החוצה, ובלוק הכותרת יכול להתכווץ במקום לדחוף אותה. גם אם ייווספו כפתורים בעתיד — הם ירדו שורה, לא ייעלמו.

### Changed — עמוד ההגדרות מסודר לטאבים

- **הבעיה:** 13 כרטיסים בגלילה אחת ארוכה, בסדר שבו נוספו ולא לפי נושא. כדי להגיע לטוקן של Vercel היה צריך לגלול מעבר לכל מפתחות ה-API.
- **החלוקה החדשה** — לפי מה שמחפשים ביחד:
  - **מודלים ומפתחות** — ברירת מחדל לספק/מודל, ומפתחות ה-API של כל הספקים
  - **חיבורים** — שרתי MCP, GitHub, Vercel (חשבון אישי ומארח)
  - **מערכת** — סביבת Node/npm, מנוע הסוכן
  - **רישיון** — סטטוס, הפעלה, הסרה
- הטאבים נצמדים לראש העמוד בגלילה, ומשתמשים באותו סגנון מפולח של סרגל התצוגה בסביבת העבודה.
- **אזהרת ההצפנה נשארת מעל הטאבים** — התראת אבטחה גלובלית לא מסתתרת מאחורי טאב. הודעת הסטטוס נשארת למטה וגלויה מכל טאב.

## 1.48.0 - 2026-07-31

### Added — Chromium מצורף: בדיקות הדפדפן עובדות אצל הלקוח

- **הבעיה:** Playwright חיפש דפדפן ב-`%LOCALAPPDATA%\ms-playwright`, שלא קיים במחשב של לקוח. גם בדיקת הדפדפן וגם «בסיס ירוק» דילגו בשקט — שתי תכונות שלא רצו אצל אף אחד.
- **הפתרון:** ה-headless shell (≈270MB) נארז בהתקנה, ו-`PLAYWRIGHT_BROWSERS_PATH` מוגדר בעליית האפליקציה. עובד אופליין מהרגע הראשון, בלי הורדה.
- ההרצה עברה ל-headless קבוע. Chromium המלא היה מוסיף ≈415MB רק כדי שייפתח חלון גלוי — לא שווה למשהו שרץ ברקע.
- המתקין גדל מ-136MB ל-≈250MB.

### Fixed — מערכת הרישוי: לא היה מפתח פרטי

- **בלי `secrets/license-private.pem` אי אפשר היה להנפיק אף רישיון**, בעוד האפליקציה כן דורשת אחד אחרי 14 יום. כלומר כל לקוח היה נתקע ביום ה-15 בלי שאפשר לפתוח לו.
- הונפק זוג מפתחות חדש, המפתח הציבורי הוטמע, והמסלול המלא אומת: `keygen → issue → verify` עם חתימה תקינה.
- **נוסף שומר סף בטסטים:** המפתח הציבורי המוטמע חייב להתאים ל-`secrets/license-public.pem`. `keygen` בלי לעדכן את הקוד הופך עכשיו לכשל בדיקה, במקום לתקלה אצל לקוח משלם.
- ⚠️ **המפתח הפרטי ב-`secrets/` אינו בגיט — חובה לגבות אותו.** אובדנו פירושו שכל הרישיונות שהונפקו מפסיקים לעבוד.

### Fixed — אכיפת העיצוב לא יכולה יותר לתקוע את הסוכן

- החסימה על צבעים גולמיים ותכונות RTL הייתה חסימה קשיחה. אם המודל לא היה מצליח לתקן, הסוכן היה נתקע מול הלקוח בלי דרך לכתוב את הקובץ.
- עכשיו קובץ נחסם **לכל היותר פעמיים**; מהשלישית הכתיבה עוברת. איכות נפגעת מעט, המשתמש לא נתקע. הספירה נפרדת לכל קובץ ומתאפסת בכל בקשה.

## 1.47.0 - 2026-07-31

### Added — מטמון פרומפט: 70–90% פחות עלות למשתמש

- **הקידומת היציבה (פרומפט המערכת + הגדרות הכלים) נשלחה מחדש בכל איטרציה** של הלולאה — עד 40 פעמים לבקשה, בתשלום מלא בכל פעם. עכשיו היא ממוטמנת.
- **Anthropic:** סמני `cache_control` על ה-system, על הכלים ועל ההיסטוריה, עם נקודות ביניים כל 15 בלוקים — כי מעל 20 בלוקים המטמון מפוספס **בלי שום שגיאה**.
- **OpenAI ו-Gemini:** ממטמנים אוטומטית לפי קידומת יציבה; הסדר הותאם ל«יציב ראשון, משתנה אחרון» וצריכת המטמון נמדדת.
- שימוש המטמון מדווח בכל סבב, כך שאפשר לראות בפועל כמה נחסך.

### Added — «בסיס ירוק» מקבל ניתוח השפעה

- שינוי שלא נוגע במה שמוצג בדפדפן (קובץ לוגיקה, קונפיג) כבר לא מפעיל בדיקה — סגירה טרנזיטיבית אחורה על גרף הייבוא קובעת מה יכול להישבר.

### Added — מפת פרויקט לפי מרכזיות

- **הסוכן קיבל מפה של מה חשוב, לא רק של מה קיים:** גרף ייבוא + דירוג PageRank מזהה שקובץ שמיובא מ-20 מקומות שווה יותר הקשר מ-helper שנקרא פעם. המפה נכנסת אוטומטית לפרומפט, מוגבלת ל-40 שורות, ואומרת במפורש כמה קבצים הושמטו.

### Added — סוויטת Evals

- **16 משימות ייחוס בעברית** (דף נחיתה לעורך דין, CRM, ניהול מלאי, קביעת תור…) עם **בדיקות קבלה דטרמיניסטיות** — לא שיפוט של מודל: אין טקסט ממלא, יש תוכן עברי, RTL תקין, מערכת עיצוב נשמרת, אין סודות בקוד, לרשימות יש loading/empty/error.
- `npm run eval` מריץ הכול בעותקים נקיים ושומר דוח JSON להשוואה בין ריצות. כשל הרצה נספר בנפרד מכשל משימה.
- **ב-PowerShell** מעבירים דרך משתני סביבה (`NF_EVAL_MODEL`, `NF_EVAL_KEY`, `NF_EVAL_TASK`, `NF_EVAL_LIST`) — הקונסולה בולעת `--` ריק, וכך גם המפתח לא נכנס להיסטוריית הפקודות. `--model` / `--key` עדיין עובדים ב-CMD ו-bash.

### Added — קריטריוני קבלה לפני בנייה

- `update_plan` מקבל `acceptance`: מה חייב לעבוד כדי שהמשימה תיחשב גמורה. נכתב **לפני** הבנייה ומוצג לסוכן בכל סבב.

### Added — אמון מדורג

- אחרי 3 אישורים רצופים שינוי קטן ונקי נסגר לבד; אחרי 10 גם שינוי בינוני. **ביטול אחד מאפס את הרצף.** שינוי גדול או סבב שנמצאה בו רגרסיה תמיד חוזרים לשאלה. הכול ב-`.nf-blaze/trust.json` — קובץ קריא ובר-מחיקה.

### Changed — אכיפת מערכת עיצוב

- **צבעים גולמיים ותכונות פיזיות נחסמים בכתיבה:** `bg-white`, `text-black`, `text-gray-500`, `bg-[#hex]`, וכן `ml-*`/`pl-*`/`text-left` שנשברים ב-RTL. השגיאה מחזירה את שם הטוקן הנכון. נאכף **רק** בפרויקט שמגדיר טוקנים, ורק בקבצי קומפוננטה; חריגה מכוונת מתאפשרת בהערה `nf-blaze: allow-raw-style`.

### Changed — הודעות שגיאה שמלמדות

- כל שגיאת כלי מוכרת חוזרת עם **מה לעשות עכשיו** ודוגמה לפורמט תקין, במקום קוד שגיאה בלבד. שגיאה עמומה עולה סבב שלם.

### Fixed — תהליכי node יתומים אחרי בדיקת דפדפן

- **בדיקת דפדפן השאירה שרת Vite חי בכל ריצה.** ב-Windows `spawn` עם `shell: true` יוצר שרשרת `cmd → npm → vite`, ו-`child.kill()` הרג רק את ה-cmd — vite נשאר לרוץ, תפס פורט וזיכרון, ומנע מהתהליך הקורא לצאת. אומת בפועל: 26 תהליכים יתומים הצטברו מארבע ריצות.
- נוסף `killProcessTree` (`agent/process_tree.ts`) שמשתמש ב-`taskkill /T` ב-Windows ובשרשור SIGTERM→SIGKILL ב-POSIX. מחובר לבדיקת הדפדפן **וגם** ל-`run_command` — שם אותה תקלה השאירה `npm install` רץ ברקע אחרי timeout או ביטול.
- זה סוגר את סעיף ה-QA «יציאה מהאפליקציה — אין תהליכי node יתומים» ב-RELEASE.md.

### Fixed — Evals: כשל ספק נספר ככשל איכות

- כשהלולאה נעצרה בשגיאת ספק (מפתח לא תקין, רשת) הרנר סימן את המשימה כ«נכשלה» והציג בדיקות שרצו על תבנית שאיש לא נגע בה. עכשיו `stopReason: error/aborted` נספר כ**שגיאת הרצה** עם הסיבה האמיתית, והבדיקות חסרות המשמעות לא מוצגות.

### Fixed — כשלי ספקים שנבלעו

- **Gemini:** JSON פגום בקריאות כלים (גדרות markdown, פסיק אחרון, גרשיים חכמים, תשובה קטועה) תוקן דטרמיניסטית במקום להפוך לקריאה עם זבל.
- **GPT:** שם פרמטר בהבדל אותיות/קו תחתון ומספרים שהגיעו כמחרוזת מותאמים לסכמה. לעולם לא ממציא ערכים ולא מוחק פרמטרים לא מוכרים.

## 1.46.0 - 2026-07-31

### Added — «בסיס ירוק»: המערכת מודיעה כשנשבר משהו שעבד

- **הבעיה:** הסוכן מתקן דבר אחד ושובר דבר אחר, ואף אחד לא בודק. שער ההשלמה
  הקיים מוודא שהקוד **מתקמפל** — לא שהאפליקציה עדיין **עובדת**. משתמשים מגלים
  את השבירה ימים אחר כך, בדרך כלל מול לקוח.
- **מה נוסף:** אחרי כל סבב בנייה שכתב קבצים, רצה בדיקה דטרמיניסטית קצרה בדפדפן
  (עד 6 מסלולים) ומשווה אותה ל**מצב האחרון הידוע כתקין** של הפרויקט. נשבר משהו
  שעבד — מופיעה התראה בצ׳אט עם הפירוט המדויק, בדיוק מעל הכפתור «בטל שינויים»
  הקיים, כך שההחלטה מיודעת.
- נבדק רק מה שאין עליו ויכוח: העמוד נטען (סטטוס < 400), לא נזרקה שגיאת ריצה,
  והשורש לא נשאר ריק (מסך לבן). שיפוט איכות נשאר לתת-סוכן ה-QA.
- **אין החזרה אוטומטית ואין חסימה** — הבדיקה מדווחת בלבד, והמשתמש מחליט.
  כשל תשתיתי (אין Vite / node_modules / Chromium) מדולג בשקט.
- הבסיס נשמר ב-`.nf-blaze/health.json` ומתעדכן **רק כשהדוח תקין**, כך שדוח שבור
  לעולם לא הופך לנקודת הייחוס הבאה. מסלול שכבר היה שבור, מסלול שהמשתמש הסיר
  ושגיאת קונסול מוכרת — לא נספרים כשבירה.

## 1.45.0 - 2026-07-30

### Added — מערכת רישוי (מפתחות חתומים, בלי שרת)
- **המערכת דורשת מפתח רישיון:** 14 יום ניסיון מההתקנה, ואחריהם מסך הפעלה. המפתח נשמר מוצפן ומשויך למחשב שבו הופעל.
- **הנפקה בפקודה אחת אצל המפתח:** `npm run license:issue -- --name "לקוח" --days 90` מייצר מפתח חתום דיגיטלית (Ed25519) לשליחה ללקוח. גם `--forever` ו-`--email`. כל הנפקה נרשמת ביומן מקומי.
- אימות **אופליין לגמרי** — אין שרת, אין תלות באינטרנט. אף אחד לא יכול לייצר מפתחות בלי המפתח הפרטי (`secrets/`, מחוץ לגיט).
- כרטיס «רישיון» בהגדרות: סטטוס, הפעלה, הסרה. `npm run license:verify` לבדיקת מפתח.

### Changed — הקשחת קוד וקניין רוחני
- **הרישיון שונה מ-MIT ל-UNLICENSED** — עד היום כל אחד היה רשאי חוקית להעתיק ולמכור את המוצר. התקנון עודכן בהתאם: אסור להעתיק, להפיץ, לבצע הנדסה לאחור או לעקוף את הרישוי.
- **bytecode:** לוגיקת הסוכן מקומפלת ל-V8 bytecode בהתקנה (`index.jsc`) — לא קריאה כקוד מקור. בפיתוח כבוי כדי לא לפגוע ב-HMR.
- **פרומפט המערכת הוטמע בקוד** ואינו נשלח יותר כקובץ טקסט גלוי בהתקנה (`prompts/` הוסר מהחבילה; מקור האמת נשאר בריפו).

## 1.44.0 - 2026-07-30

### Fixed — «הסוכן תקוע» בזמן כתיבת קבצים ארוכים (נראות חיה כמו Dyad/bolt)
- **הבעיה:** כשהמודל כותב קובץ גדול (אתר שלם), התוכן זורם כ-tool-call — לא כטקסט — והמסך שתק לדקות ארוכות. נראה תקוע למרות שהוא עובד.
- **התיקון:** סטרימינג התקדמות מכל המתאמים (Anthropic + OpenAI/OpenRouter/מקומיים): שורת הסטטוס מציגה חי «כותב את src/App.tsx… (14.2K תווים)» עם עדכון כל רבע שנייה — רואים בדיוק איזה קובץ נכתב וכמה.
- **טיימר עבודה:** ליד הסטטוס החי מוצג «2:47» — כמה זמן הסוכן עובד על הבקשה. אין יותר תהייה אם הוא חי.

## 1.43.0 - 2026-07-30

### Fixed — הסוכן יכול להתקין חבילות
- **`npm install <חבילה>` נחסם לסוכן:** ה-allowlist התיר רק `npm install` ריק — כשהסוכן ניסה להתקין חבילה חדשה (למשל `@radix-ui/react-accordion`) הוא נחסם והקוד נשבר עם import חסר. עכשיו התקנת חבילות מותרת עם ולידציה קפדנית: שמות חבילה חוקיים בלבד (+scope וגרסה), דגלי שמירה בטוחים בלבד — בלי נתיבים, בלי git URLs ובלי דגלים מסוכנים. נוספו טסטים לשני הכיוונים.

### Added — תקנון בהתקנה + הסבר מלא בהפעלה ראשונה
- **דף תקנון ומדיניות באשף ההתקנה:** לפני ההתקנה המשתמש רואה ומאשר את תנאי השימוש בעברית — מהות התוכנה, פרטיות (הכל מקומי, מה נשלח לספק המודל), אחריות על מפתחות ותוכן שנוצר ב-AI, הרצת קוד מקומית, אינטגרציות, עדכונים ורישיון MIT. (`build/license_he.txt`)
- **מסך «ברוכים הבאים» בהפעלה הראשונה:** מדריך של 4 שלבים שמסביר את כל המערכת — מה זה NF-Blaze ואיך מתחילים (מפתח API / Ollama), איך עובדים (פרויקט, תצוגה חיה, סימון אלמנטים), השליטה בסוכן (אישור שינויים, diff, היסטוריה, טרמינל), ומה בודקים לפני פרסום (מכשירים, אבטחה, בדיקות). מוצג פעם אחת בלבד, עם דילוג ונקודות התקדמות.

## 1.42.0 - 2026-07-30

### Added — מסך פרויקט (כמו Dyad, בצבעי המערכת)
- **כניסה לפרויקט פותחת מסך פרויקט חדש:** שם הפרויקט עם עריכה במקום (עיפרון → Enter), תאריכי יצירה ועדכון, נתיב התיקייה עם פתיחה בסייר — וכפתור «פתח בצ׳אט» גדול.
- **כרטיס GitHub:** מצב חיבור עם קישור לריפו, **בורר ענף יעד** (רשימת הענפים האמיתית מהריפו), «סנכרן ל-GitHub» עם משוב חי, וניתוק. לא מחובר? כפתור שפותח את חלון החיבורים.
- **כרטיס Supabase:** שם הפרויקט המחובר, קישור ל-Dashboard, ניתוק — או חיבור אם עוד לא.
- כפתור חדש בסרגל הצ'אט חוזר למסך הפרויקט מכל מקום.

### Fixed — משוב חי לסריקת האבטחה
- לחיצה על «סרוק עכשיו» נראתה כאילו לא קורה כלום (הסריקה מסתיימת מהר מדי) — עכשיו יש אייקון מסתובב בזמן הסריקה וחותמת «סריקה אחרונה: HH:MM:SS» אחרי כל ריצה.

## 1.41.0 - 2026-07-30

### Added — סימון בתצוגה בדיוק כמו Dyad
- **תווית על כל אלמנט מסומן בתוך התצוגה עצמה:** שם הקומפוננטה/תג + נתיב הקובץ והשורה (או ה-selector), עם כפתור ✕ צף להסרת הסימון ישירות מהתצוגה — בדיוק כמו ב-Dyad, בצבעי המערכת. התוויות נצמדות לאלמנט גם בגלילה ושינוי גודל, וממוספרות בבחירה מרובה.

### Added — טאבי «אבטחה» ו«בדיקות» בטולבר
- **אבטחה:** סריקה לפי דרישה (רצה אוטומטית בכניסה הראשונה לטאב) — סודות בקוד, env בקבצי build, RLS מתירני וטבלאות Supabase פתוחות. כל ממצא עם חומרה (חסימה/אזהרה), הסבר, ו«תן לסוכן לתקן»; מונה אדום על הטאב כשיש ממצאים.
- **בדיקות:** הרצת `npm test` של הפרויקט בלחיצה, פלט חי, סטטוס ✓/✗, ו«תן לסוכן לתקן» שמעביר את הכשלים לסוכן.

### Added — ריבוי צ'אטים לפרויקט + «סכם לצ'אט חדש» (כמו Dyad)
- **כמה שיחות לכל פרויקט:** בורר צ'אטים בכותרת הצ'אט, צ'אט חדש בלחיצה, מחיקה (תמיד נשאר אחד), וכותרות אוטומטיות מההודעה הראשונה. שיחות ישנות עוברות מיגרציה שקטה.
- **«סכם לצ'אט חדש»:** מודל מהיר מסכם את השיחה (מה נבנה, החלטות, מה פתוח) ופותח צ'אט חדש שמתחיל מהסיכום — ממשיכים בהקשר נקי בלי לגרור היסטוריה כבדה.

## 1.40.0 - 2026-07-30

### Added — ממשק עבודה בסגנון Dyad (בצבעי המערכת)
- **טולבר תצוגה חדש:** טאבים מפולחים «תצוגה | קוד», שורת כתובת אמיתית (URL של התצוגה או נתיב הקובץ הפתוח, עם רענון מסתובב בזמן טעינה ונקודת «לא נשמר»), וכפתורי פעולה מסודרים — כמו בכלים המקצועיים.
- **סטטיסטיקת שינויים על כל קובץ:** צ'יפ הקובץ בהודעת הסוכן מציג עכשיו `‎+נוספו −נמחקו` בירוק/אדום (חישוב אמיתי מול ה-snapshot, בסגנון git --stat).
- מעברים חלקים (150ms) על טאבים וכפתורים.

### Fixed — ההדגשות בתצוגה נשארות
- **הבחירה שרדה רענון:** אחרי כל שינוי של הסוכן התצוגה נטענת מחדש וההדגשות הירוקות נעלמו (בעוד הצ'יפים נשארו) — עכשיו הבחירה משוחזרת אוטומטית לפי selector אחרי כל טעינה, כולל מצב הבחירה עצמו.
- **הדגשה בולטת בהרבה:** מסגרת עבה + גוון ירקרק על האלמנט + עיגול מספר גדול עם צל — רואים ברור מה מסומן גם בדפים עמוסים.

## 1.39.2 - 2026-07-30

### Added — בחירה מרובה של אלמנטים בתצוגה
- **בוחרים כמה אלמנטים שרוצים:** במצב בחירה, כל לחיצה מוסיפה אלמנט (עם עיגול ממוספר ירוק על האלמנט בתצוגה), לחיצה חוזרת מסירה, ומצב הבחירה נשאר פעיל עד שמכבים אותו. ליד שורת הכתיבה מופיע צ'יפ ממוספר לכל אלמנט (עם X להסרה) ו«נקה הכל».
- **הסוכן מקבל את כולם:** ההקשר כולל רשימה ממוספרת של כל האלמנטים (קובץ+שורה או selector+טקסט), עם הנחיה מפורשת לעבור על כולם — והמספור בצ'אט («הראשון», «השני») תואם לעיגולים בתצוגה.
- מונה בחירה על כפתור הבחירה; אחרי שליחה הכל מתנקה אוטומטית. תאימות מלאה לפרויקטים עם plugin ישן (בחירה בודדת).

### Fixed
- **שמירת מפתח OpenRouter נחסמה:** ולידציה ישנה ב-IPC הרשתה רק openai/anthropic/gemini — «שמור» על מפתח OpenRouter זרק «ספק לא חוקי».

## 1.39.1 - 2026-07-30

### Fixed — «npm לא נמצא» בהתקנה נקייה (תקלת אריזה של Node המצורף)
- electron-builder סינן בכוח את תיקיית `node_modules` של npm מתוך ה-runtime המצורף — `npm.cmd` נארז בלי npm עצמו, ומשתמשים קיבלו מסך «נדרש Node.js» גם אחרי התקנת Node ידנית. נוסף hook `afterPack` שמעתיק את npm לחבילה; אומת שה-npm הארוז רץ (`10.9.2`).
- **עמידות כפולה:** אם ה-runtime המצורף פגום מכל סיבה, האפליקציה נופלת אוטומטית ל-Node המותקן במערכת — «בדיקה מחדש» אחרי התקנת Node תעבוד תמיד.
- תוקן ציטוט נתיבים עם רווחים (Program Files) בבדיקת הסביבה.

## 1.39.0 - 2026-07-30

הכנה לעלייה לאוויר: Node מצורף להתקנה, עדכונים אוטומטיים, תשתית חתימת קוד, ותיקוני ביצועים לתקיעות UI.

### Added — מוכנות שחרור
- **Node.js מצורף להתקנה (v22.14.0):** משתמשי קצה בלי Node מותקן עובדים מהרגע הראשון — אין יותר מסך «נדרש Node». (~98MB ב-`resources/runtime`, נארז אוטומטית.)
- **עדכונים אוטומטיים (electron-updater):** האפליקציה בודקת עדכון בעלייה וכל 4 שעות, מורידה ברקע ומציעה «התקן והפעל מחדש». בטוח-כשל — בלי שרת עדכונים אין שום הפרעה. שרת היעד: `build.publish` ב-package.json (generic, כל אחסון סטטי).
- **תשתית חתימת קוד מוכנה:** `npm run dist` חותם אוטומטית כשמוגדרים `CSC_LINK` + `CSC_KEY_PASSWORD`.
- **`RELEASE.md` חדש:** מדריך שחרור מלא — רכישת תעודה וחיבורה, פרסום עדכונים (שלושה קבצים לשרת סטטי), וצ'קליסט QA למכונה נקייה.

### Fixed — תקיעות UI (ביצועים)
- **סטרימינג:** כל טוקן מהמודל גרם לרנדר מלא של המסך + פרסור מחדש של כל הטקסט החי (O(n²)) — עכשיו הטוקנים נצברים ונשטפים כל ~80ms, וכל הודעות ההיסטוריה עטופות ב-React.memo כך שלא מתרנדרות מחדש בכל טוקן.
- **קונסול/טרמינל:** פלט שרת הפיתוח והפקודות נערם בלי תקרה וגרם להאטה מצטברת עד תקיעה אחרי שימוש ממושך — עכשיו נשמרות עד 400 שורות אחרונות בכל פאנל.

## 1.38.0 - 2026-07-30

השלמת פערים מול Dyad / bolt.new / Lovable / Base44 אחרי מיפוי תכונות מעמיק: היסטוריית גרסאות עם שחזור לכל נקודה, וייבוא פרויקטים קיימים.

### Added — פאנל תחתון מקצועי (בסגנון VS Code)
- **הטרמינל עבר לפאנל תחתון** שנפרס מתחת לתצוגה ולעץ הקבצים, עם שלוש לשוניות: **טרמינל** (אינטראקטיבי — המשתמש מקליד ומריץ פקודות npm/pnpm/yarn בתוך הפרויקט, עם שורת `$` ופלט חי), **קונסול** (פלט שרת הפיתוח + כל פקודה שהסוכן מריץ), ו**בעיות** (שגיאות ריצה וקומפילציה מהתצוגה עם מונה אדום ו«תן לסוכן לתקן» לכל בעיה). הפאנל מתכווץ בלחיצה.
- הטרמינל נשאר מאובטח: רק מנהלי חבילות בתוך תיקיית הפרויקט; פקודות ארוכות-ריצה (dev/start) חסומות עם הפניה לתצוגה החיה.
- **הקלדה בטרמינל:** שורת הפקודה מקבלת פוקוס אוטומטית כשפותחים את הטרמינל וכשפקודה מסתיימת, לחיצה בכל מקום בפאנל ממקדת אותה (כמו טרמינל אמיתי), וה-prompt מתחלף ל-… בזמן ריצה במקום לנעול את השדה.

### Added — תצוגת מכשירים (מובייל / טאבלט / מחשב)
- **סרגל מכשירים מעל התצוגה המקדימה:** מחשב מלא, או דגם ספציפי — iPhone SE / 15 / 15 Pro Max, Samsung Galaxy S24, Google Pixel 8, iPad, iPad Pro — עם viewport מדויק, מסגרת מכשיר, כפתור סיבוב לרוחב, ותצוגת מידות + אחוז סקייל. המסגרת מתכווצת אוטומטית להיכנס לשטח הזמין.

### Fixed — בוחר האלמנטים בתצוגה
- **בחירה עובדת על כל אלמנט:** גם בפרויקטים מיובאים / HTML סטטי בלי תיוג `data-nf-*` — הבחירה נופלת ל-CSS selector + קטע טקסט, והסוכן מונחה לאתר את האלמנט עם grep.
- **רואים מה נבחר:** ריחוף מסמן בכתום, האלמנט הנבחר נשאר מודגש בירוק בתצוגה עצמה, והצ'יפ ליד שורת הכתיבה מציג עכשיו גם את הטקסט של האלמנט. אחרי שליחה ההדגשה מתנקה אוטומטית (קודם נשארה תקועה).

### Added — אנימציה מקצועית למסך הראשי
- שלוש הילות צבע מרחפות ברקע (כתום, סגול, כחול — blur עם תנועה מתמדת), שם המותג עם shimmer גרדיאנט, כניסה מדורגת לכל אלמנטי ההירו וכרטיסי הפרויקטים, פעימת הילה עדינה על כפתור «התחל פרויקט חדש», והילת ריחוף משודרגת על הכרטיסים. הכל CSS בלבד.
- תוקן: ההילות נחתכו ע"י `overflow` וחסימת `prefers-reduced-motion` כיבתה את הכל במחשבי Windows עם «הצג אנימציות» כבוי — האנימציה רצה עכשיו תמיד ואומתה ב-Web Animations API (12 אנימציות פעילות).

### Changed — הסוכן בונה ברמה עולמית, לא גנרית (כל הספקים: Claude/GPT/Gemini)
- **פרומפט המערכת קיבל סעיף «הבן את הלקוח ובנה ברמה עולמית»:** חילוץ כוונה מלאה מהבקשה (תחום, קהל יעד, מטרה, טון), הצהרת הנחות בתוכנית במקום שאלות מיותרות, מחקר רשת כשהתחום לא מוכר, ותוכן עברי אמיתי שמוכר — בלי lorem ipsum ובלי «תכונה 1/2/3».
- **רף עיצוב מוגדר:** כיוון ייחודי לכל פרויקט (בלי מראה AI גנרי), היררכיה וריווח שיטתיים, hover/focus ומעברים לכל אלמנט, ויזואליה מ-CSS/SVG בלי תמונות חיצוניות שבורות, רספונסיביות 375px-דסקטופ, מצבי loading/empty/error, נגישות — וסיום רק אחרי בדיקה עצמית «האם זה עובר סקירה של מעצב בכיר?».
- **בדיקת הדפדפן בודקת גם איכות מוצר:** תת-סוכן ה-QA מדווח כ«שבור» גם placeholder טקסט, אלמנטים חופפים, טקסט לא קריא ועמודים חצי-גמורים — לא רק שגיאות תפקוד.
- **PROJECT_RULES בשתי התבניות** שודרגו עם אותו רף איכות.

### Fixed — באגים שנמצאו בסריקת קוד
- **הגדרות Vercel Team ID נעלמו אחרי הפעלה מחדש:** `SETTINGS_GET` לא החזיר את `vercelTeamId` / `vercelPlatformTeamId` — המשתמש ראה שדה ריק למרות שההגדרה נשמרה.
- **פרויקטים קיימים לא קיבלו תיקונים לבוחר האלמנטים:** ה-plugin של NF-Blaze בפרויקט מסונכרן עכשיו אוטומטית לגרסה העדכנית בכל עליית תצוגה חיה (`syncNfSourcePlugin`).
- **«בטל» מסרגל הכלים השאיר סרגל אישור מיותם:** אחרי ביטול הסבב האחרון, ההודעה מסומנת «השינויים בוטלו» ולא מציעה יותר «בטל שינויים» על snapshot שכבר נמחק.
- **תווית כיוון הפוכה בחלון ה-diff:** «לפני» מוצג משמאל, לא מימין.
- **שגיאות MCP סתומות:** כששרת MCP נופל בעלייה, הודעת השגיאה כוללת עכשיו את סוף ה-stderr שלו במקום «השרת נסגר» גנרי.

### Added — היסטוריית גרסאות עם שחזור לכל נקודה (כמו Dyad/Lovable)
- **כפתור «היסטוריה» חדש בסרגל הצ'אט:** רשימת כל נקודות השחזור (עד 30, נוצרות אוטומטית בכל סבב שהסוכן משנה קבצים) עם תאריך, קטע מהבקשה שהתחילה את הסבב ומספר הקבצים — ו«שחזר לכאן» שמחזיר את הפרויקט למצב שלפני אותה נקודה. השחזור מצטבר (מחיל את כל הנקודות מהחדשה ועד היעד), כך שגם קבצים שנוצרו בסבבים מאוחרים נמחקים כראוי.
- «בטל» הקיים ממשיך לעבוד וגם מתעדכן אחרי שחזור.

### Added — אישור שינויים + תצוגת diff לכל סבב (כמו bolt/Cursor)
- **«לשמור את השינויים?» בסוף כל סבב:** אחרי שהסוכן מסיים סבב עם שינויי קבצים, מופיעים על ההודעה כפתורי «אשר» / «בטל שינויים». ביטול משחזר את כל קבצי הסבב למצב הקודם; ההחלטה נשמרת על ההודעה (✓ אושרו / ✗ בוטלו) גם אחרי סגירת האפליקציה. שליחת הודעה חדשה בלי להחליט = הישארות עם השינויים (לא חוסם).
- **«הצג שינויים» על כל הודעת סוכן:** תצוגת diff מלאה (Monaco, לפני מול אחרי, זיהוי שפה אוטומטי) לכל קובץ שהשתנה בסבב — כולל סימון קבצים שנוצרו ושנמחקו. מוצגים רק קבצים שבאמת השתנו.
- **שחזור מכל הודעה:** לכל סבב בהיסטוריית הצ'אט יש נקודת שחזור משלו (דרך פאנל «היסטוריה» או ביטול הסבב האחרון).

### Added — ייבוא פרויקט קיים (כמו Dyad)
- **מסך «פרויקט חדש» קיבל מצב «ייבוא פרויקט קיים»:** בוחרים תיקייה עם פרויקט Node/JavaScript (React, Vite, Next...), NF-Blaze מתקין תלויות אם חסרות, מרים תצוגה חיה אם זה פרויקט Vite — ולא נוגע באף קובץ קיים. הסוכן ממשיך לעבוד על הקוד שלך עם כל היכולות (אינדקס פרויקט, זיכרון, תוכניות).
- הגנות: נדרש package.json, ותיקייה שכבר מקושרת לפרויקט לא תיובא פעמיים.

## 1.37.0 - 2026-07-30

הסוכן קיבל גישה לרשת, סיום משימות מלא בלי «כתוב המשך», תמיכה במודלים מקומיים (Ollama / LM Studio) ו-OpenRouter, שרתי MCP ו-Supabase מלא בכל התבניות.

### Added — גישה לרשת לסוכן
- **כלי `web_search`:** חיפוש ברשת (DuckDuckGo, בלי מפתח API) — כשמבקשים מהסוכן «בדוק ברשת» (השראה לעיצוב, תוכן, תיעוד ספריות) הוא באמת מחפש, במקום לענות שאין לו גישה לאינטרנט.
- **כלי `web_fetch`:** טעינת עמוד לפי URL והמרתו לטקסט קריא (כולל קישורים להמשך מעקב). זמינים בכל המצבים (שאלה / תכנון / ביצוע).

### Fixed — סיום משימות מלא (בלי «כתוב המשך»)
- **המשך אוטומטי:** תקרת 40 האיטרציות הפכה לתקציב סבב «רך» — כשמשימה גדולה מגיעה אליו, הסוכן ממשיך אוטומטית מאותה נקודה (עם כיווץ הקשר ותזכורת מיקוד לתוכנית), עד תקרת בטיחות של 160 איטרציות. ההודעה «הגעתי לתקרת האיטרציות — כתוב המשך» נעלמה; במקומה סטטוס «ממשיך אוטומטית (סבב N)».

### Added — מודלים מקומיים ו-OpenRouter (בהשראת dyad)
- **Ollama (מקומי):** בחר ספק «Ollama» והרץ מודלים חינם על המחשב — בלי מפתח API. מזוהה לפי קידומת `ollama:` במזהה המודל; ברירת מחדל `http://127.0.0.1:11434/v1` (ניתן לעקוף עם `OLLAMA_BASE_URL`).
- **LM Studio (מקומי):** אותו דבר דרך שרת LM Studio (`http://127.0.0.1:1234/v1`, עקיפה עם `LMSTUDIO_BASE_URL`).
- **OpenRouter:** מפתח אחד למאות מודלים (DeepSeek, Qwen, Llama, Kimi...). נשמר מוצפן כמו שאר המפתחות.
- מתאם OpenAI אוחד ל-factory תואם-OpenAI אחד (`createOpenAICompatAdapter`) שמשרת את כולם — סטרימינג, tool_use וטיפול בשגיאות זהים.
- בהגדרות: ספקים מקומיים מוצגים עם «לא נדרש מפתח — רץ מקומית» וקישור הורדה במקום שדה מפתח.

### Added — MCP servers (כמו Dyad Pro)
- **הסוכן תומך בשרתי MCP:** קובץ `mcp.json` בפורמט המוכר של Claude Desktop/Dyad — גלובלי (`%APPDATA%/nf-blaze/nf-blaze-data/mcp.json`) או פר-פרויקט (`.nf-blaze/mcp.json`, גובר על הגלובלי). כל כלי משרת מחובר נחשף לסוכן כ-`mcp_<שרת>_<כלי>` וזמין בכל המצבים.
- לקוח MCP מינימלי (stdio, JSON-RPC) בלי תלות חיצונית: `agent/mcp/`. שרתים נשארים חיים בין הודעות; שרת שנופל מדווח כסטטוס ולא מפיל את הבקשה; כל השרתים נסגרים ביציאה מהאפליקציה.
- **מסך ניהול MCP בהגדרות:** כרטיס «שרתי MCP» — הוספה/עריכה/מחיקה של שרתים (פקודה, ארגומנטים, משתני סביבה), הפעלה/השבתה, וכפתור «בדוק» שמרים את השרת ומציג את רשימת הכלים שלו. הקונפיג נכתב לאותו `mcp.json` גלובלי.

### Added — Supabase מלא בכל התבניות
- **תבנית «אתר/אפליקציה» (web-app) קיבלה חיווט Supabase מלא** כמו data-app: תלות `@supabase/supabase-js`, לקוח מוכן ב-`src/lib/supabase.ts` (עם `isSupabaseConfigured`), `.env.example`, טיפוסי env, וסעיף Supabase ב-PROJECT_RULES — חיבור Supabase עובד מיד בכל פרויקט חדש.
- **הקשר הסוכן הועשר:** כש-Supabase מחובר, הסוכן מקבל מדריך עבודה מלא — auth (`signUp`/`signInWithPassword`/`onAuthStateChange`), CRUD, Realtime, Storage, חובת RLS עם policies, והנחיה לכתוב SQL מלא למשתמש להרצה ב-SQL Editor.

### Changed
- **נוסח חדש למסך הבית:** תיאור המערכת עודכן כך שהוא מתאר את NF-Blaze עצמה — סביבת פיתוח עם AI שרצה מקומית, הקוד והמפתחות נשארים על המחשב — במקום השוואה למוצרים אחרים. הוסרו אזכורי מוצרים מתחרים גם מ-`README.md`, `prompts/system.md`, מודאל «פרויקט חדש» ומהערות בקוד.

## 1.36.0 - 2026-07-30

שדרוג משמעותי ביכולות הבנייה של הסוכן: המנוע החדש (tool_use עם הגנות) הפך לברירת המחדל לכל המשתמשים, עם זיכרון פרויקט מתמשך, תוכנית בנייה בשלבים שחיה בין הודעות, streaming אמיתי, ומשוב שגיאות ריצה מהתצוגה.

### Changed — המנוע החדש הוא ברירת המחדל
- **`agentEngine: 'new'` לכל המשתמשים** (מיגרציה חד-פעמית; בחירה ידנית ב«ישן» אחרי המיגרציה נשמרת). המשתמשים מקבלים עכשיו: `declare_scope`, אימות לפני כתיבה, תיקון tsc אוטומטי, בדיקת השלמה, כיווץ הקשר, ו-QA דפדפן.
- לפני ההחלפה נסגרו כל פערי החסימה שנמצאו במיפוי מלא של שני המנועים:

### Added — זיכרון ותוכנית (בנייה בשלבים)
- **כלי `update_plan`:** הסוכן שומר תוכנית שלבים ב-`.nf-blaze/plan.json`, מעדכן סטטוסים תוך כדי, וממשיך ממנה בהודעה הבאה («המשך»). רצועת התקדמות חדשה מעל שורת הכתיבה מציגה `תוכנית · 2/4` עם השלבים.
- **כלי `save_memory`:** זיכרון פרויקט ב-`.nf-blaze/memory.md` (עד 6K תווים) — החלטות ארכיטקטורה/סכמות/מוסכמות שמוזרקות לכל בקשה עתידית ושורדות כיווץ הקשר.
- **הקשר מלא בפרומפט המערכת** (פער מול המנוע הישן שנסגר): עץ קבצים, אינטגרציות מחוברות (Supabase/GitHub), קובץ פתוח בעורך, PROJECT_RULES, זיכרון ותוכנית.
- **משוב שגיאות ריצה מהתצוגה:** ‏window.onerror + unhandledrejection בתוך ה-iframe מדווחים ל-NF-Blaze; באנר אדום עם «תן לסוכן לתקן» שולח את השגיאה המלאה לסוכן.

### Fixed — פערי המנוע החדש שנסגרו לפני ההחלפה
- **סטרימינג אמיתי:** הלולאה עברה מ-`call()` (שהחזיר הכל בסוף) ל-`stream()` עם אירוע `model_delta` — טקסט זורם למשתמש תוך כדי יצירה, עם הפרדה בין איטרציות.
- **אבטחה — קבצי סוד:** ‏`read_file`/`grep` חוסמים `.env*`, מפתחות ו-credentials (החרגה: `.env.example`); כתיבה אליהם חסומה גם כן. במנוע החדש לא היה שום סינון.
- **Undo שלם:** snapshot יחיד לסבב שמורחב בכל קובץ חדש (`extendSnapshot`) — «בטל» משחזר את כל קבצי הסבב; קודם קבצים מהרחבת scope לא היו ניתנים לשחזור וה-undo החזיר רק snapshot אחרון חלקי.
- **הגנות כתיבה הרסנית:** חסימת ריקון קובץ קיים, כיווץ קטסטרופלי של קבצי config קריטיים, ו-package.json לא-JSON.
- **היסטוריית שיחה חסומה ל-28 הודעות** (הייתה בלתי מוגבלת — סיכון חריגת הקשר).
- **שגיאת ספק לא מוחקת את הסבב:** ההודעות נשמרות ל-chat.json ומוחזרת תשובה עם השגיאה (קודם — throw שאיבד גם את הודעת המשתמש).
- **תקרת 40 איטרציות מדווחת למשתמש** («כתוב המשך») במקום להיראות כהצלחה.
- **clarify במצב תכנון:** שאלות הבהרה מוצגות ככרטיס מובנה במקום JSON גולמי בצ׳אט.
- **maxTokens הועלה ל-16384** (ברירת מחדל האדפטרים 8192 קטעה קומפוננטות גדולות).
- **`npm run dev/start/preview` חסומים ב-run_command** (תהליכים ארוכים שחסמו את הלולאה 5 דקות).
- **אותו באג FORCE_COLOR מ-1.35.0 תוקן גם בשכבת הסוכן:** worker הבדיקות, run_command ו-browser QA עברו ל-NO_COLOR + ניקוי ANSI ב-parseTscOutput (בלעדיו זיהוי שגיאות build נכשל).
- **ביטול אמיתי ב-Gemini:** ה-signal מועבר ל-SDK (קודם ביטול תפס רק בין chunks); הוסרה קריאת abort כפולה שיצרה אירוע «בוטל» מיותר בתחילת כל סבב.
- **Gemini 3 — ‏thought signatures (נמצא בבדיקת E2E):** מודלי Gemini 3 מחזירים `thoughtSignature` על כל functionCall ו**דוחים ב-400** כל בקשה שמחזירה functionCall בהיסטוריה בלעדיו. החתימה מועברת עכשיו בכל השרשרת (ToolCall ← tool_use ← Gemini parts), כולל עקיפת באג ב-SDK של גוגל שמוחק את השדה באגרגציית ה-stream — החתימות נלכדות מה-chunks הגולמיים וממוזגות חזרה לפי סדר הקריאות. בלי זה המנוע החדש נשבר עם Gemini אחרי האיטרציה הראשונה.
- **`run_command` נכשל ב-`spawn EINVAL` על Windows:** הרצת `npm.cmd` עם `shell:false` נחסמת ב-Node מעודכן (תיקון CVE-2024-27980) — עכשיו רץ עם shell כמו שאר שכבות ההרצה.
- **QA דפדפן בלי Chromium מותקן מדלג בשקט** במקום להוסיף הודעת כישלון לכל תשובה (רלוונטי לגרסה הארוזה).
- **מד ההקשר מדויק למנוע החדש** (קודם חושב לפי קבצי המפתח שהמנוע הישן הזריק).
- **סקריפט select/שגיאות-ריצה נארז ב-EXE** (`packages/vite-plugin-nf-source/src`) ותוקן נתיב הטעינה בגרסה ארוזה.

### Tests
- ‏11 בדיקות חדשות: חסימת קבצי סוד, הגנות כתיבה הרסנית, חסימת סקריפטים ארוכים, `update_plan`/`save_memory`. סה"כ 72 בדיקות עוברות.

## 1.35.0 - 2026-07-30

נמצא בבדיקת E2E מלאה (יצירת פרויקט CRM כמשתמש אמיתי דרך ה-UI): התצוגה החיה מעולם לא עלתה — כל פרויקט נפל למצב גיבוי סטטי שהוצג כמסך לבן.

### Fixed
- **שרת הפיתוח תמיד נפל למצב גיבוי (הבאג המרכזי):** `getRuntimeSpawnEnv` הגדיר `FORCE_COLOR: '0'` — אבל picocolors (ספריית הצבעים של Vite) בודק רק אם המשתנה *קיים* ומדליק צבעים גם על `'0'`. קודי ה-ANSI פיצלו את ה-URL בפלט (`http://localhost:` ‏← `ESC[1m` ‏← `5173`), `extractLocalUrl` תפס `http://localhost` בלי פורט, בדיקת ה-HTTP נכשלה — ונפלנו לגיבוי סטטי בכל יצירת פרויקט. תוקן: `NO_COLOR=1` במקום `FORCE_COLOR` (שנמחק מהסביבה), ניקוי ANSI מכל פלט (`stripAnsi`), וזיהוי ה-URL רץ על הלוג המצטבר כדי לא לפספס URL שהתפצל בין chunks.
- **מצב הגיבוי הסטטי הציג מסך לבן:** ‏`vite build` ברירת מחדל מפיק נתיבי `/assets/...` אבסולוטיים, שתחת `nfblaze://preview/<projectId>/dist/` מתפרשים כ-projectId ומחזירים 404. תוקן: בניית הגיבוי מזהה פרויקט Vite ומעבירה `--base=./` (נתיבים יחסיים).
- **באנר «מצב גיבוי» וטרמינל ריקים אחרי יצירת פרויקט:** ה-workspace הניח ש-URL חם מיצירת הפרויקט הוא תמיד `live` ולא הציג לא באנר ולא לוג. תוקן: IPC חדש `preview:live-status` שמחזיר את המצב האמיתי (live/fallback) + לוג ההרצה, וה-workspace מציג אותם בעלייה.
- **שינויי סוכן לא הופיעו במצב גיבוי:** אחרי שהסוכן כתב קבצים במצב גיבוי, התצוגה המשיכה להציג build ישן (אין HMR). תוקן: בסיום סבב עם שינויים במצב גיבוי מופעלת «הפעל מחדש תצוגה» — שגם בונה מחדש וגם נותנת לשרת הפיתוח הזדמנות נוספת.
- **CSP לא תקין בקונסולה:** ‏`http://[::1]:*` אינו מקור חוקי ב-`frame-src` (שגיאה בכל טעינה). הוסר.

### Changed
- **תבניות `web-app`/`data-app` — יישור ל-shadcn המלא:** ל-`button.tsx` נוספו `variant: destructive/secondary/link` ו-`size: icon` (+ tokens ב-Tailwind וב-CSS) — הסוכן כותב קוד לפי ה-API הסטנדרטי של shadcn וקיבל `TS2322` על `size="icon"` בבדיקה בפועל.
- **תבניות — ‏`noUnusedLocals`/`noUnusedParameters` כובו ב-`tsconfig.app.json`:** משתנה לא-בשימוש הוא תוצר שכיח וזניח של קוד שנוצר על ידי AI (נצפה בפועל: `setDeals`), והוא הפיל את `npm run build` של מצב הגיבוי בלי שום באג אמיתי.

## 1.34.0 - 2026-07-25

### Fixed
- **תצוגה מקדימה נכשלת בפרויקטי React חדשים:** לתבניות `web-app`/`data-app` חסרה תלות `@types/node` ו-`"types": ["node"]` ב-`tsconfig.node.json`, מה שגרם ל-`vite.config.ts` (שמשתמש ב-`node:url`/`import.meta.url` ל-alias של `@`) להיכשל ב-`tsc -b` עם `TS2307`/`TS2339` — בדיוק השגיאות שהוצגו למשתמש כשמצב הגיבוי (`npm run build`) הופעל. אומת עם בנייה מלאה + הרצת `npm run dev` אמיתית על שני התבניות אחרי התיקון.

### Added
- **קונסולת שרת פיתוח בטאב «טרמינל»:** פלט ה-`npm run dev` (התקנה → הרמה → מוכן/קרס/גיבוי) מוזרם עכשיו לטאב הטרמינל הקיים בזמן אמת — כמו ב-bolt.new/lovable — ולא רק כ-12 שורות זמניות מעל התצוגה בזמן עלייה. נורית ירוקה פועמת על הטאב «טרמינל» כשהשרת פעיל.

## 1.33.0 - 2026-07-25

### Fixed
- **`npm run typecheck` נכשל בפועל:** ל-`tsconfig.node.json`/`tsconfig.web.json` לא הוגדר `target`, מה שגרם לקבצי `agent/`+`providers/` (שנטענים דרך main) להיבדק מול ES ישן וליפול על עשרות שגיאות איטרציה של `Set`/`Map`/`RegExp`
- **באג טיפוסים אמיתי ב-`agent/loop.ts`:** מנגנון תיקון ה-tsc (`tscRepair`) קרס ל-`never` אחרי `await` בתוך הלולאה (מגבלת control-flow narrowing של TS למשתנה מוטבע שמשתנה מתוך closures) — נפתר עם פונקציית קריאה מפורשת שמשמרת את הטיפוס
- **דליפת גרסה:** מספר הגרסה בכותרת האפליקציה ובקובץ ה-README כבר לא קבוע (hardcoded) — נלקח דינמית מ-`package.json` דרך IPC חדש (`app:get-version`)
- **אזהרת הצפנה חסרה:** אם `safeStorage` לא זמין במערכת ההפעלה, המפתחות נשמרים כטקסט גלוי — כעת מוצגת אזהרה ברורה במסך ההגדרות + לוג אזהרה ב-main

### Added
- **React Error Boundary** סביב כל האפליקציה — מסך שגיאה ידידותי בעברית במקום מסך לבן בקריסת רינדור
- **תשתית איכות קוד:** ESLint (flat config) + Prettier + `npm run lint`/`format`; `npm test` מריץ עכשיו את כל סוויטות ה-Vitest (core + agent/tools + agent/index + providers) בפקודה אחת
- **CI:** GitHub Actions להרצת typecheck/lint/test בכל push ו-PL

### Security
- עדכון `playwright` לתיקון עקיפת אימות SSL בהורדת דפדפנים (`npm audit fix`)
- הסרת תלות מתה (`zustand`, לא היה בשימוש בקוד האפליקציה)

## 1.32.0 - 2026-07-25

### Fixed
- **תצוגה חיה מיד אחרי יצירת פרויקט:** אחרי העתקת תבנית + npm install — הרמת Vite לפני פתיחת ה-workspace (כמו bolt)
- **לחיצה על index.html בפרויקט React:** כבר לא מחליפה ל-`nfblaze://` סטטי (מסך ריק); נשארת על שרת הפיתוח
- **עריכת קבצי מקור:** לא מוחקת את URL התצוגה החיה; מעבר חזרה ל«תצוגה» עובד
- **Strict Mode / מעבר פרויקט:** עצירת שרת עם השהיה קצרה כדי לא להרוג תצוגה שחוממה ביצירה

## 1.31.0 - 2026-07-24

### Added
- **בדיקת סביבת Node בהפעלה:** זיהוי node/npm וגרסה; אם חסר או מתחת ל־18 — מסך חסימה בעברית עם קישור להורדה ו«בדיקה מחדש» (בלי להתחיל לבנות ולכשל)
- **סטטוס סביבה בהגדרות** + כפתור בדיקה מחדש
- **שכבת הרצה:** העדפת Node מצורף מ־`resources/runtime` (נארז ב־extraResources) על פני Node של המערכת; shell / תצוגה חיה / התקנת תבניות משתמשים בו

## 1.30.0 - 2026-07-24

### Added
- **תצוגה מקדימה דרך שרת פיתוח:** פרויקטים עם `package.json` + סקריפט `dev` מרימים `npm run dev` (התקנה אוטומטית אם חסר `node_modules`), קוראים URL מהפלט, וממתינים לתגובת השרת לפני הצגת ה-iframe
- **מצבי תצוגה חיים:** «מתקין תלויות» → «מרים שרת» → «מוכן»; כפתור «הפעל מחדש תצוגה»; שגיאות/קריסה בעברית באזור התצוגה + «תן לסוכן לתקן»
- **גיבוי סטטי:** אם השרת לא עולה תוך 60ש׳ — `npm run build` והגשת הפלט דרך `nfblaze://` עם באנר «מצב גיבוי בלי עדכון חי»
- **CSP:** `frame-src` / `connect-src` ל-localhost כדי לאפשר iframe + HMR

### Fixed
- פרויקטי React/Vite כבר לא נשארים עם מסך ריק מ-HTML סטטי בלבד

## 1.29.0 - 2026-07-24

### Fixed
- **Gemini tool schemas:** ניקוי רקורסיבי (whitelist) לפני שליחה — מסיר `additionalProperties` ושדות JSON Schema שאינם נתמכים; כלי בלי פרמטרים נשלח בלי שדה `parameters` ריק
- **שגיאת 400 מ-Gemini:** הודעה קצרה בעברית במקום JSON גולמי

## 1.28.0 - 2026-07-24

### Fixed
- **זיהום היסטוריה ב-ASK/PLAN:** בלוקי פעולה (`nfblaze`) מנוקים מההיסטוריה שנשלחת למודל — נשאר רק טקסט
- **מצב אוטומטי:** זיהוי כוונה אמיתי דרך מודל מהיר (`{"intent":"ASK"|"PLAN"|"BUILD"}`); ספק → ASK; בחירה ידנית תמיד גוברת; בנתיב legacy דרך `callLlm` עם אותו ספק

### Added
- **תג מצב על כל תשובה** בממשק (שאלה / תכנון / ביצוע, כולל «אוטומטי · …» כשזוהה)
- **שאלות הבהרה במצב תכנון:** בלוק `clarify` נפרד, כרטיס UI עם אפשרויות + «אחר» / «תחליט אתה»; JSON גולמי מוסתר בסטרימינג; clarify אינו פעולת כתיבה ואינו נחסם; clarify+כתיבה → רק שאלות

## 1.27.0 - 2026-07-24

### Fixed
- **אכיפת מצב עבודה (שאלה / תכנון / ביצוע)** — שלוש שכבות:
  1. המצב נשלח ב-IPC, נשמר על הודעת המשתמש בהיסטוריה; מצב חסר → שאלה (לא ברירת מחדל שקטה ל-auto/ביצוע)
  2. במצב שאלה/תכנון פרומפט המערכת לא כולל פורמט `nfblaze` בכלל; במקום זה הנחיה מפורשת לא לשנות קבצים (תכנון = תוכנית ממוספרת בלי קוד)
  3. בנקודת יישום הפעולות — חסימה מוחלטת של כתיבה ב-ASK/PLAN; בלוק כתיבה מוסתר; בממשק הודעה + כפתור «עבור לביצוע והרץ»
  - נתיב legacy (`src/main/services/ai/agent.ts`) ונתיב חדש (`agent/loop.ts` + `agent_bridge`)

## 1.26.0 - 2026-07-24

### Added
- **ניהול הקשר בלולאת הסוכן** (`agent/context_compact.ts` + `agent/loop.ts` + מד הקשר):
  - כיווץ אוטומטי סביב ~60% מחלון המודל הנבחר — רק בין סבבים שהושלמו (לא באמצע משימת tsc פתוחה)
  - נשמרים: מה נבנה, קבצים שננגעו, החלטות, מה פתוח; נזרקים תוכן קבצים גולמי ופלטי כלים ארוכים
  - פלטי כלים ארוכים נשמרים ב-`.nf-blaze/tool_results/` עם מזהה קצר בהקשר
  - במד ההקשר: סימון «כווץ» + באנר מתי בוצע הכיווץ

## 1.25.0 - 2026-07-24

### Added
- **שער אבטחה לפני פרסום Vercel** (`src/main/services/security/` + שכבת הפרסום + UI):
  - חסימה מוחלטת (לא אזהרה) על: service_role/סוד ב-bundle, קובץ `.env` בפרסום, סודות מקודדים קשיח, טבלת Supabase שנקראת עם anon בלי אימות (כולל SQL לתיקון)
  - אזהרה חזקה על מדיניות RLS שמתירה הכול (`USING (true)` / טבלה פתוחה)
  - בממשק בעברית: מה נמצא, למה מסוכן, «תן לסוכן לתקן», ועקיפה ידנית רק אחרי סימון + הקלדת משפט אישור מפורש
  - קבצי `.env` גם מסוננים מחבילת `source.tgz` כהגנה נוספת

## 1.24.0 - 2026-07-24

### Added
- **תת-סוכן בדיקת דפדפן** (`agent/browser_qa/` + `agent/loop.ts`):
  - אחרי בנייה מוצלחת (completion + tsc) — הרמת תצוגה + Playwright מקומי (Chromium headed)
  - הקשר נפרד: בקשת משתמש + מפת מסכים; לולאה עם לחיצה / הקלדה / ניווט / צילום / שגיאות קונסול בלבד (בלי כלי כתיבה)
  - מחזיר לסוכן הראשי סיכום קצר בלבד; תיקון + בדיקה שנייה אחת בלבד
  - רץ רק כשהשתנה מספיק (BUILD + קבצים משמעותיים), לא אחרי כל הודעה
  - בממשק: תג חי «דפדפן פועל» עם הפעולה הנוכחית

## 1.23.0 - 2026-07-24

### Added
- **שכבת תיקון לפני דיסק** (`agent/correct/` + `agent/loop.ts`):
  - **שלב א׳ (דטרמיניסטי):** תיקון import של אייקונים/קומפוננטות מול ייצוא אמיתי של החבילה המותקנת (fuzzy), ותיקון/סימון ייבוא מקומי חסר — לפני `write_file` / `edit_file`
  - **שלב ב׳ (אחרי הסטרימינג):** עטיפת providers ל-hooks, הוספת תלויות חסרות ל-`package.json`, ותיקוני JSX/TS נפוצים (כולל AST דרך `@babel/*` של פרויקט המשתמש כשזמין)
  - המשתמש לא רואה את המצב השגוי — התיקון רץ לפני הכתיבה/תצוגה
  - מדד מרכזי: אחוז בקשות BUILD עם תצוגה מקדימה תקינה בניסיון הראשון → `.nf-blaze/preview_metrics.json`

## 1.22.0 - 2026-07-24

### Changed
- **tsc/build ב-worker נפרד** (`agent/workers/project_check_worker.mjs` + `agent/completion_check.ts`):
  - בדיקות רצות ב-`worker_threads` על תיקיית המשתמש בלבד (נתיב מוחלט — לא `process.cwd()`)
  - לולאת הסוכן / תהליך Electron הראשי לא נחסמים; אפשר לבטל בדיקה שרצה (כולל ביטול צ׳אט)
  - אינדיקציה חיה בממשק: בודק / עבר / נכשל (+ מספר שגיאות), timeout / בוטל
  - תוצאות ממשיכות להזין את לולאת תיקון tsc כשהן מוכנות
  - אם הבדיקה לא מסתיימת ב־90 שניות — ביטול + אזהרה בעברית

## 1.21.0 - 2026-07-24

### Added
- **Preview element pick → agent** (`packages/vite-plugin-nf-source` + templates + preview UI):
  - Vite plugin (dev/`serve` only) tags JSX with `data-nf-file` / `data-nf-line` / `data-nf-component`
  - Injects select-mode client; templates include the plugin under `plugins/nf-blaze-source`
  - Workspace: select mode (hover outline, click), chip above chat (removable)
  - Selection sent with the user instruction as agent context (path + line + component)
  - Vite projects: live preview via `npm run dev` so tagged elements are available

## 1.20.0 - 2026-07-24

### Added
- **Starter templates with PROJECT_RULES** (`templates/` + create flow):
  - `web-app` — Vite + React + TS + Tailwind + shadcn-style UI (Hebrew RTL built-in)
  - `data-app` — same base + ready Supabase client
  - Each template ships `PROJECT_RULES.md` (stack, naming, allowed UI lib, design rules, RTL: start/end not left/right) — injected into the system prompt every request (legacy + new engine bridge)
  - New project modal: pick template → copy into empty user folder → `npm install` → only then workspace opens for the agent
  - Legacy agent blocks writes/edits to template config files unless the user explicitly asks; rules also warn the new engine

## 1.19.0 - 2026-07-24

### Added
- **Vercel publishing from the app** (integrations layer + Settings + Workspace):
  - **Path A — instant (no user account):** pack project to `.vercel/source.tgz` (excludes `node_modules` / `.git`), upload via `POST /v2/files` + `POST /v13/deployments`, show live URL + **claim URL**, Hebrew explanation of claim
  - **Path B — user account:** Vercel token in Settings (encrypted `safeStorage`), deploy to existing or new project
  - Framework detection from `package.json` → Vercel `projectSettings`
  - Supabase env vars injected automatically when connected
  - After deploy: URL saved on project; Workspace shows **«פרסם עדכון»**
  - Build failure: pull Vercel log, show in Hebrew, **«תן לסוכן לתקן»** feeds the log into chat (BUILD)

## 1.18.0 - 2026-07-24

### Added
- **Work modes for the new agent engine** (`agent/intent.ts` + `agent/loop.ts` + UI):
  - Auto-detect via a short fast-model call → `{ intent: ASK | PLAN | BUILD }` (doubt → ASK)
  - ASK / PLAN: only `read_file`, `list_dir`, `grep` are sent to the model
  - BUILD: full tool set
  - Manual picker in Workspace (אוטומטי / שאלה / תכנון / ביצוע) overrides detection
  - After ASK/PLAN: «בצע את זה» re-runs in BUILD with history — never automatic

## 1.17.0 - 2026-07-24

### Added
- **Project convention index wired to Electron**:
  - On project open / create: `ensureProjectIndex` → `.nf-blaze/project-index.json`
  - Service: `src/main/services/project_index.ts` + IPC ensure/refresh/get
  - Workspace UI: index status + manual refresh
  - New engine bridge refreshes the index after a write round; `runAgentLoop` still injects conventions into the system prompt each request

## 1.16.0 - 2026-07-24

### Added
- **New agent engine behind a settings flag** (default: legacy):
  - Settings → «מנוע סוכן» — `legacy` (nfblaze) / `new` (`agent/loop` + `providers`)
  - Bridge: `src/main/services/ai/agent_bridge.ts` — routes chat to `runAgentLoop` with `project.folderPath` as root
  - UI stream events: `scope`, `tool`, `tsc_fix`, `completion_check` (plus existing status/token/file/undo/preview/context meter)
  - Snapshot before writes on declare_scope; Undo unchanged

## 1.15.0 - 2026-07-24

### Fixed
- **tsc/build run only on the user project** (`agent/completion_check.ts` + `agent/loop.ts`):
  - `rootDir` must be an absolute user-project path (`requireUserProjectRoot`) — no `process.cwd()` fallback
  - Skip tsc silently when no `tsconfig.json` / `jsconfig.json`; skip build when no `scripts.build`
  - Hard timeout 90s (`PROJECT_CHECK_TIMEOUT_MS`) — on timeout emit `check_warning` and continue (do not hang)

## 1.14.0 - 2026-07-24

### Changed
- Updated `prompts/system.md` to match live `agent/` tools and guards:
  `declare_scope`, read-before-edit, intentional `edit_file` uniqueness errors (retry via re-read, not `write_file`), pre-write import/package checks, and completion-gate before declaring done

## 1.13.0 - 2026-07-24

### Added
- **Structured tsc repair** (`/agent/completion_check.ts` + `/agent/loop.ts`):
  1. Parse `tsc` output into file / line / message — never send raw logs to the agent
  2. Group errors by file; fix one file per round
  3. Prompt includes that file's errors + content + `תקן רק את השגיאות האלה. אל תיגע בשום דבר אחר.`
  4. Re-run after each fix; if error count rises, restore the last fix and ask for another approach
  5. After 3 failed attempts — stop with `tsc_unresolved` and show remaining errors (never report success while broken)

## 1.12.0 - 2026-07-24

### Added
- **Completion gate** (`/agent/completion_check.ts` + `/agent/loop.ts`):
  - When the agent stops calling tools after writing files, the loop runs completion checks before accepting "done"
  - Checks: `npx tsc --noEmit`, project `build`, no TODO/FIXME/placeholder, no Lorem/`כאן יבוא`, list UIs have loading+empty+error, imports resolve
  - On failure, injects: `עוד לא סיימת. חסר: [התנאי]. תקן רק את זה.` and continues the loop (emits `completion_blocked`)

## 1.11.0 - 2026-07-24

### Added
- **Session guards** in `/agent/tools` (+ wiring in `/agent/loop`):
  1. `declare_scope` — before first `edit_file` / `write_file`, agent must declare intended files; out-of-scope edits fail with a fixed Hebrew message
  2. `edit_file` requires a prior `read_file` in the same request session
  3. Scope expansion via a new `declare_scope` call with required `reason` — recorded and emitted as a `scope` loop event for the user UI

## 1.10.0 - 2026-07-24

### Added
- **Pre-write validation** in `/agent/tools` for `edit_file` / `write_file`:
  1. External imports must exist in `package.json` (else: package not installed)
  2. Local imports must resolve to a real file (else: lists files in the directory)
  3. Project component JSX props must match the component signature index (else: returns real signature)
- Failures are intentional so the agent can self-correct from the tool error

## 1.9.0 - 2026-07-24

### Added
- **Project convention index** (`/agent/index`):
  - On project open: `ensureProjectIndex(rootDir)` scans exports, file naming, state, styling, error handling, TypeScript/`any`
  - Cached under `{project}/.nf-blaze/project-index.json` (+ memory) - not rescanned every request
  - Injected into the system prompt on every agent run via `systemPromptForProject` / `runAgentLoop`
  - Instruction: match existing code; do not impose another style
- Updated `/prompts/system.md` with the match-existing-code rule

### Notes
- Index + prompt injection only; tools/providers untouched.

## 1.8.0 - 2026-07-24

### Added
- **Agent loop** (`/agent/loop.ts`) over the unified `/providers` `call()` adapter (40-iteration tool loop)

## 1.7.0 - 2026-07-24

### Added
- **Agent tools** under `/agent/tools` - JSON Schema + executors

## 1.6.0 - 2026-07-24

### Added
- Unified provider adapter layer under `/providers`

## 1.5.0 - 2026-07-24

### Added
- E2B session runtime under `/sandbox`
