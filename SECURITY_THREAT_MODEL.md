# מודל איומים — עקיפת הרישוי של NF-Blaze

> ## 🔴 מסמך פנימי. אסור שיגיע ללקוח, לאתר, או לגיט ציבורי.
>
> הוא ממפה במפורש איפה ההגנות נשברות. חשיפתו שקולה למסירת מפת דרכים
> לפריצה. חל עליו כלל #3 שבראש `CHANGELOG.md`.
>
> נכתב 02.08.2026 מול גרסה 1.66.0.

## מה השאלה בעצם

«כמה קשה לאדם שהתקין את NF-Blaze להשתמש בה בלי רישיון?» — לפי רמת יכולת,
מהקל לקשה. לכל דרגה: מה היא מסוגלת לעשות, מה עוצר אותה **היום**, ומה נשאר
פתוח.

**המסקנה מראש, כדי שלא תהיה אשליה:** אימות רישיון שרץ על המחשב של המשתמש
תמיד ניתן לשבירה על ידי מי שמוכן לערוך את הבינארי. זה לא כשל בתכנון —
זו תכונה של המודל. המטרה הריאלית אינה «בלתי אפשרי», אלא:

1. **לעצור לחלוטין** את דרגות 0–2, שהן כמעט כל מי שינסה.
2. **להפוך את דרגה 3 ליקרה** מספיק כדי שלא ישתלם למשתמש בודד.
3. **לזהות ולבטל** את התוצר של דרגה 4 כשהוא מופץ.
4. **להגן בכל מחיר** על דרגה 5 — המפתח הפרטי.

---

## דרגה 0 · משתמש רגיל

**פרופיל:** מתקין, מריץ, עובד. לא פותח את תיקיית ההתקנה.
**כלים:** אין.
**זמן לפריצה:** ∞

**מה הוא יכול:** כלום. מסך הרישיון חוסם, וגם אם היה עוקף אותו — שער ה-IPC
בתהליך הראשי חוסם כל ערוץ שאינו ב-`LICENSE_FREE_CHANNELS`
([src/main/index.ts](src/main/index.ts:334)).

**סטטוס: 🟢 חסום מוחלט.**

---

## דרגה 1 · «סקרן» — יודע לחפש בגוגל ולעקוב אחרי סרטון

**פרופיל:** מנסה את הטריקים המוכרים. משנה שעון, מוחק קבצים, מחפש «crack».
**כלים:** סייר הקבצים, הגדרות Windows.
**זמן:** דקות.

| ניסיון | מה קורה | ההגנה |
|---|---|---|
| החזרת שעון המערכת אחורה | נכשל, ומקבל הודעת «זוהתה החזרה של שעון המערכת» | `trustedEvaluationMs` — max על שעון, high-water מוצפן+HMAC, `iat` חתום, mtime של קבצי האפליקציה, ועוגן `.license-anchor` ([src/shared/license.ts](src/shared/license.ts:301)) |
| מחיקת קובצי הרישיון | הרישיון נעלם — הוא במצב גרוע יותר | — |
| חסימת הדומיין ב-hosts | **נועל אותו**: אחרי 48 שעות → `needs_revalidation` | חלון החסד מוגבל ([license.ts](src/main/services/license.ts:474)) |
| שימוש במפתח של חבר | נתפס באימות המקוון — `seat_exceeded` | ספירת מושבים בשרת (`nfb_claim_seat`), מכסה 3 |
| עריכת `settings.json` | אין שם שום דבר שקשור לרישיון | תקופת הניסיון הוסרה ב-1.57.0 בדיוק בגלל זה |
| העתקת תיקיית `secrets/` למחשב אחר | הקבצים **לא נפתחים** | `safeStorage` = DPAPI, קשור למשתמש ולמכונה. ובנוסף, ה-HMAC של `license-seen`/`license-online` נגזר מ-`machineFingerprint()` |

**סטטוס: 🟢 חסום.** כל הווקטורים הקלאסיים סגורים, וחלקם אפילו מזיקים לתוקף.

⚠️ **הפער היחיד בדרגה הזו:** מפתח משותף על מחשב **מנותק מהרשת** עובד עד
24 שעות (`ACTIVATION_GRACE_MS`) לפני האימות המקוון הראשון. מוגבל ומקובל.

---

## דרגה 2 · «משתמש מתקדם» — מכיר PowerShell, עורך קבצים, יודע מה זה hex editor

**פרופיל:** יפתח את תיקיית ההתקנה ויחטט. אולי ינסה לערוך קובץ בינארי.
**כלים:** עורך טקסט, hex editor, Process Monitor.
**זמן:** שעות.

| ניסיון | מה קורה | ההגנה |
|---|---|---|
| מחיקת `license-fingerprint` | הקשירה המקומית נעלמת — אבל השרת סופר מושבים | `nfb_claim_seat` הוא מקור האמת מ-1.65.0 |
| שתילת קובץ סוד ידני (`plain:`) | נדחה כשההצפנה זמינה | תוקן ב-1.65.0 — `plain:` מתקבל רק במצב fallback אמיתי ([secrets.ts](src/main/services/secrets.ts:48)) |
| עריכת `license-seen` / `license-online` | נפסל | HMAC-SHA256 עם `machineFingerprint()` + מלח |
| **פתיחת `app.asar` וקריאת הקוד** | **מצליח חלקית** | ראה למטה 🟡 |
| החלפת קובץ בתוך `app.asar` | האפליקציה לא עולה | `enableEmbeddedAsarIntegrityValidation` + `onlyLoadAppFromAsar` |
| פתיחת DevTools בגרסה הארוזה | לא זמין | `devTools: !app.isPackaged` + fuse `enableNodeCliInspectArguments: false` |

### 🟡 ממצא: לא כל הקוד מוגן

אומת על החבילה הבנויה של 1.66.0:

| מה | מצב |
|---|---|
| `out/main/index.jsc` | ✅ **V8 bytecode** — לא קריא |
| `out/preload/index.js` | ❌ **JavaScript קריא** |
| `out/renderer/assets/*.js` | ❌ **JavaScript קריא** |

‏`bytecodePlugin` מוגדר רק על `main` ([electron.vite.config.ts](electron.vite.config.ts:9)).
כלומר תוקף בדרגה 2 יכול לקרוא את כל שמות ערוצי ה-IPC, את `LicenseGate`,
ואת כל הלוגיקה של ה-renderer.

**האם זה שובר משהו? לא.** שער ה-IPC יושב ב-main (bytecode), והוא הגבול
האמיתי — בדיוק כפי שמתועד ב-`PROJECT_CONTEXT.md`. אבל זה **מפת דרכים**
שמקצרת לתוקף בדרגה 3 שעות עבודה.

**סטטוס: 🟢 חסום מבחינת שימוש, 🟡 חשוף מבחינת מידע.**

---

## דרגה 3 · מפתח — מכיר Electron, קורא JavaScript, יודע להריץ Node

**פרופיל:** מבין מה זה asar, fuses ו-IPC. לא מהנדס-לאחור, אבל יודע להרכיב.
**כלים:** `npx asar`, Node, הפצת Electron רשמית.
**זמן:** יום–יומיים לניסיון ראשון.

### 🔴 כאן נמצא קו השבירה האמיתי

ה-Fuses (`runAsNode: false`, ‏`onlyLoadAppFromAsar`, אימות שלמות asar)
**צרובים בתוך `NF-Blaze.exe` עצמו** — לא ב-asar. תוקף שמוריד הפצת Electron
רשמית באותה גרסה ומצביע אותה על תוכן ה-asar המחולץ **מנטרל את כל ארבעת
ה-Fuses בבת אחת**, כי הוא פשוט לא משתמש בבינארי שלנו.

מרגע זה:
- אימות שלמות ה-asar לא רץ → אפשר לשנות קבצים.
- `runAsNode` ו-`--require` זמינים → אפשר להזריק קוד לפני עליית האפליקציה.
- ה-preload וה-renderer קריאים ממילא.

ה-bytecode של ה-main עדיין מקשה — הוא לא יכול פשוט לערוך `getLicenseStatus`.
אבל הוא **לא צריך**: ראה דרגה 4.

**סטטוס: 🟠 עובר את ההגנות הטכניות, אך עדיין צריך את המהלך של דרגה 4 כדי
לקבל רישיון תקף.**

**מה כן עוצר אותו היום:** רק המאמץ. אין שום מנגנון שמזהה שהאפליקציה רצה
תחת Electron שאינו שלנו.

---

## דרגה 4 · מהנדס-לאחור / «cracker»

**פרופיל:** עובד עם בינאריים, מבין קריפטוגרפיה ברמת שימוש.
**כלים:** הכל מדרגה 3 + עורך בינארי, ידע ב-Ed25519.
**זמן:** ימים.

### 🔴 השבירה המלאה: החלפת המפתח הציבורי

אימות הרישיון נעשה מול `LICENSE_PUBLIC_KEY_PEM`, שמוטמע בקוד
([src/shared/license.ts](src/shared/license.ts:17)).

⚠️ **ה-bytecode אינו מסתיר אותו.** מחרוזות קבועות שורדות את הקומפילציה
ב-constant pool כטקסט קריא. מפתח Ed25519 ב-PEM הוא **באורך קבוע**, ולכן
אפשר להחליף אותו בייט-בבייט במפתח ציבורי משלו — בלי לשנות את מבנה הקובץ,
בלי לקמפל מחדש, ובלי להבין את הקוד.

מרגע שהמפתח הוחלף, הוא מנפיק לעצמו רישיונות תקפים לחלוטין — כולל תפוגה
רחוקה — והם עוברים כל בדיקה מקומית. את האימות המקוון הוא מנטרל באותה שיטה
(המפתח הציבורי הוא גם זה שמאמת את תשובת השרת), או פשוט חוסם את הדומיין —
כי עכשיו זה כבר לא נועל אותו.

**סטטוס: 🔴 שבירה מלאה. אין הגנה טכנית שמונעת את זה בארכיטקטורה הנוכחית.**

**הסיכון האמיתי אינו המשתמש הבודד** — הוא ש-**התוצר מופץ**. גרסה פרוצה
אחת שעולה לפורום מגיעה למאה אנשים שכולם בדרגה 0.

**מה יש לנו נגד זה היום: כלום.** גרסה פרוצה לא פונה לשרת שלנו אף פעם,
ולכן ספירת המושבים והביטול מרחוק לא רואים אותה בכלל.

---

## דרגה 5 · גישה למפתח הפרטי

**פרופיל:** לא בהכרח מיומן — מספיק שיגיע לקובץ.
**וקטורים:** `secrets/license-private.pem` על מחשב הפיתוח, גיבוי לא מוצפן,
הסוד `LICENSE_PRIVATE_KEY` ב-Supabase, או חשבון המנהל של הפורטל.
**זמן:** רגע אחד.

**מה הוא יכול:** להנפיק רישיונות תקפים **בלי לגעת באפליקציה בכלל**. הם
יעברו כל בדיקה, בכל התקנה, כולל אימות מקוון. **בלתי ניתן לזיהוי.**

**התאוששות:** רק החלפת זוג המפתחות + גרסה חדשה לכל הלקוחות. כלומר אירוע
ברמת המוצר.

**סטטוס: 🔴 קטסטרופלי. זו הנקודה שמצדיקה את רוב מאמצי האבטחה.**

---

## סיכום

| דרגה | מי | זמן | תוצאה | מצב |
|---|---|---|---|---|
| 0 | משתמש רגיל | ∞ | כלום | 🟢 |
| 1 | סקרן | דקות | כלום (ולפעמים נועל את עצמו) | 🟢 |
| 2 | משתמש מתקדם | שעות | קורא קוד, לא משתמש | 🟢 / 🟡 |
| 3 | מפתח | יום–יומיים | עוקף Fuses ואימות שלמות | 🟠 |
| 4 | מהנדס-לאחור | ימים | **רישיונות תקפים + הפצה** | 🔴 |
| 5 | גישה למפתח הפרטי | רגע | **הכל, בלי זיהוי** | 🔴 |

**איפה אנחנו טובים:** דרגות 0–2 סגורות היטב, ובכמה מקומות ההגנה אפילו
הופכת את הניסיון למזיק לתוקף. זה מכסה את המכריע ברוב הלקוחות.

**איפה אנחנו חשופים:** מדרגה 3 ומעלה אין הגנה אפקטיבית, ואין **שום יכולת
זיהוי** של עותק פרוץ.

---

## המלצות לפי תשואה על מאמץ

### 1. 🔴 גיבוי והקשחה של המפתח הפרטי — קודם לכל השאר

דרגה 5 היא הסיכון הגרוע ביותר וגם הזול ביותר למניעה. כבר מופיע ב-
`RELEASE_CHECKLIST.md` סעיף 1 ועדיין לא סומן כבוצע.
- שני עותקים אופליין מוצפנים, אחד מחוץ לבית.
- סיסמה חזקה + 2FA על חשבון המנהל בפורטל ועל Supabase.
- ההנפקה מהפורטל בלבד; להסיר את המפתח ממחשב הפיתוח היומיומי.

### 2. 🟠 חתימה דיגיטלית על המתקין — התשואה הגבוהה ביותר מול הפצה

היום המתקין **לא חתום**, ולכן כל לקוח רואה SmartScreen ממילא — כלומר
**גרסה פרוצה נראית בדיוק אותו דבר**. עם תעודת חתימה, גרסה מקורית עולה
נקייה ומזויפת מקפיצה אזהרה. זה לא מונע פריצה, אבל הופך הפצה לקשה בהרבה.
כבר מופיע כ«מומלץ» ב-`RELEASE_CHECKLIST.md` — לדעתי צריך לעלות לדרגת חובה.

### 3. 🟠 זיהוי במקום מניעה

אי אפשר למנוע דרגה 4, אבל אפשר לראות אותה:
- **התראה על מפתח עם יותר מדי טביעות אצבע** — הנתונים כבר קיימים ב-
  `nfb_license_seats`, אין ממשק. שאילתה אחת בפורטל.
- **ממשק ביטול רישיון** — `revoked` קיים בטבלה מ-0001 ואין לו כפתור.
  היום ביטול מחייב עריכת SQL ידנית.
- ⚠️ שתי אלה עוזרות רק מול **שיתוף מפתח**, לא מול גרסה פרוצה שלא פונה אלינו.

### 4. 🟡 להעלות את דרגה 3 מ«יום» ל«שבוע»

מדרג עלות, לא חומה:
- להחיל `bytecodePlugin` גם על ה-preload.
- ערפול (obfuscation) של ה-renderer.
- בדיקת שלמות עצמית בזמן ריצה: השוואת ה-hash של ה-asar מול ערך חתום,
  ובדיקה שהתהליך רץ תחת הבינארי שלנו (חתימה/גרסה) ולא תחת Electron סטנדרטי.
- ⚠️ כל אלה ניתנות לעקיפה על ידי מי שהגיע לדרגה 4. הן קונות זמן בלבד.

### 5. 🟢 מה **לא** כדאי לעשות

- **לא** לקצר את חלון החסד האופליין. הוא כבר 48 שעות, וקיצור נוסף פוגע
  בלקוחות משלמים הרבה יותר מבתוקפים.
- **לא** להוסיף בדיקות אנטי-דיבאג/אנטי-VM. מטרידות משתמשים לגיטימיים,
  ומעכבות תוקף אמיתי בדקות.
- **לא** להעביר לוגיקה מרכזית לשרת. זה יעבוד מצוין נגד פריצה — ויסתור
  את הבטחת הליבה של המוצר («הכל מקומי, אין ענן»). זה שינוי מוצר, לא
  שינוי אבטחה, ואסור שיוחלט מתוך שיקולי פיראטיות.

---

## שורה תחתונה

ההגנות הקיימות **מתאימות למוצר**: הן עוצרות לחלוטין את מי שינסה בפועל,
ולא מפריעות ללקוח משלם. אין טעם להשקיע עוד בהקשחה טכנית מול דרגות 3–4
לפני ששני הדברים הזולים נעשו: **גיבוי המפתח הפרטי** ו**חתימה דיגיטלית**.
