# צ'קליסט שחרור

מסמך חי — לא קשור לגרסה מסוימת. מה מוכן, ומה חייב לקרות לפני שמתקין יוצא החוצה.

## ✅ מוכן ואומת

| נושא | איך אומת |
|------|----------|
| הנפקת רישיונות | `keygen → issue → verify` הורץ מקצה לקצה, חתימה תקינה |
| התאמת מפתח ציבורי | טסט נכשל אם `secrets/license-public.pem` לא תואם ל-`src/shared/license.ts` |
| Node מצורף | `node.exe -v` → `v22.14.0` מתוך החבילה; `npm-cli.js` נארז |
| Chromium מצורף | headless shell הורץ מתוך החבילה הבנויה ורינדר תוכן עברי |
| «בסיס ירוק» | e2e על פרויקט Vite אמיתי: זיהה קריסה מוזרקת, שמר בסיס תקין, דילג על שינוי לא-ויזואלי |
| חיבור Supabase | אומת מול ה-API החי — מפתח פסול נדחה, הגבלת-נקודה נחשבת הצלחה, כשל רשת מזוהה ככזה |
| חיבור GitHub / Vercel | אומת מול ה-API החי (401 / 403 אמיתיים) |
| שכפול מ-GitHub | ריפו ציבורי שוכפל בפועל, כולל נרמול כל צורות הכתובת |
| אין תהליכים יתומים | `killProcessTree` מחובר לבדיקת דפדפן, `run_command` ותצוגה חיה |
| סרגל חלון | אומת בצילום של האפליקציה **הארוזה** |
| טסטים | 292 עוברים · typecheck נקי · 0 שגיאות lint |

## ⛔ חייב לפני הפצה

### 0. גודל המתקין — הבדיקה שתופסת בנייה חסרה בשנייה

**מתקין תקין שוקל ≈214MB. יצא ≈114MB → חסרים משאבים, אסור להעלות.**

‏`resources/runtime/` (Node, ~98MB) ו-`resources/playwright/` (Chromium,
~274MB) אינם בגיט, ו-**electron-builder פשוט מדלג עליהם בשקט** אם הם לא על
הדיסק — הבנייה «מצליחה» ומייצרת מתקין פגום. קרה בפועל ב-1.66.0.

מה שבור אצל הלקוח, בלי שום הודעת שגיאה:

- אין `resources/runtime/node.exe` → לקוח **בלי Node מותקן נחסם ב-RuntimeGate**
  ולא יכול לפתוח פרויקט.
- אין תיקיית `chromium*` תחת `resources/playwright/` → בדיקת דפדפן ו«בסיס
  ירוק» מדלגות בשקט אצל כולם.

⚠️ `mandatory: true` היא ברירת המחדל — העלאת מתקין כזה **מכריחה כל לקוח קיים**
לרדת לגרסה הפגומה.

- [ ] `resources/runtime/node.exe` ו-`npm.cmd` קיימים (תוכן
      `node-v22.14.0-win-x64.zip`, לא התיקייה שבתוכו)
- [ ] `resources/playwright/chromium_headless_shell-*/` קיימת
      (`npx playwright install chromium-headless-shell` ואז העתקה מ-`%LOCALAPPDATA%\ms-playwright`)
- [ ] אחרי הבנייה: `release/win-unpacked/resources/` מכיל `runtime` **וגם** `playwright`

### 1. גיבוי `secrets/`

**הכי קריטי.** אובדן `license-private.pem` = כל רישיון שהונפק מפסיק לעבוד,
ואי אפשר להנפיק חדשים בלי לבנות מחדש את האפליקציה.

- [ ] גובה לכספת / מנהל סיסמאות / כונן מוצפן (שני עותקים, אחד אופליין)
- [ ] נבדק שהעותק נפתח ומתחיל ב-`-----BEGIN PRIVATE KEY-----`
- [ ] `issued-licenses.jsonl` מגובה גם הוא (רישום הלקוחות) ומרוענן מדי פעם

### 2. הרצת סוויטת ה-Evals

הליבה עברה שינויים רבים שנבדקו **לוגית** אך לא **התנהגותית** מול מודל אמיתי.

```
$env:NF_EVAL_MODEL="claude-sonnet-5"; $env:NF_EVAL_KEY="<מפתח אמיתי>"; npm run eval
```

- [x] **בסיס חלקי נרשם (31.07.2026, ‏gemini-3.1-pro-preview): ‏8/8 משימות עברו, 0 שגיאות הרצה.**
      כוסו: שני דפי נחיתה, CRM, טופס+ולידציה, scoped-edit, no-secret-leak, ASK, PLAN.
      הדוחות ב-`evals/reports/2026-07-31T1*-gemini-*.json` (הדוחות מהבוקר עם sonnet — ניסיונות עם מפתח פסול, להתעלם).
      הריצה חשפה ותיקנה 3 באגי רנר: קריסת EBUSY בניקוי, תהליך שלא יוצא אחרי BUILD, ותיקיות עבודה ב-%TEMP% שנמחקו ע"י Storage Sense (עברו ל-`evals/.work`).
- [ ] השלמת 8 המשימות הנותרות (מלאי, מצב כהה, תמחור, FAQ, לוח הזמנות, קביעת תור, המלצות, תפריט מסעדה) — אפשר על Flash לחיסכון
- [ ] ריצה מלאה אחת על אותו מודל כבסיס אחיד להשוואה עתידית

### 3. QA ידני על מכונה נקייה

הצ'קליסט המלא ב-`RELEASE.md` סעיף 4. הקריטיים:

- [ ] פרויקט חדש מתבנית → התקנת תלויות מסתיימת → תצוגה חיה עולה (מאמת את ה-Node המצורף)
- [ ] יציאה מהאפליקציה → אין תהליכי node ב-Task Manager
- [ ] בדיקת דפדפן רצה (מאמת את ה-Chromium המצורף)

### 4. מפתח רישיון לעצמך (מ-1.57.0)

אין יותר תקופת ניסיון — **גם אתה** צריך מפתח כדי לפתוח את האפליקציה הארוזה.

- [ ] הנפק מפתח `--forever` לעצמך: `node scripts/license.cjs issue --name "<שמך>" --forever`
- [ ] מחק מ-`secrets/issued-licenses.jsonl` את שורת הבדיקה «בדיקה פנימית» (id ‏6512f13d)

## 🟡 מומלץ, לא חוסם

- [ ] **חתימה דיגיטלית** — בלעדיה כל לקוח רואה SmartScreen. הוראות ב-`RELEASE.md` סעיף 2
- [ ] **שרת עדכונים** — `updates.nf-digital.net` לא קיים ב-DNS. בלעדיו אין עדכונים אוטומטיים
      (האפליקציה עצמה עובדת; הכשל נבלע בשקט)

## המלצה

**הפצה מבוקרת ל-2–3 לקוחות קרובים:** מספיק 1 + 2 + 3.

**הפצה רחבה / מכירה:** גם חתימה ושרת עדכונים.
