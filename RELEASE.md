# שחרור NF-Blaze לאוויר — מדריך תפעולי

## 0. רישוי לקוחות — איך מנפיקים מפתח

המערכת דורשת מפתח רישיון חתום. תקופת ניסיון של 14 יום מתחילה בהתקנה;
בסיומה נדרש מפתח.

### המפתח הפרטי — קריטי
`secrets/license-private.pem` הוא **המפתח היחיד שמייצר רישיונות**.
- **גבה אותו** (כספת / מנהל סיסמאות / דיסק חיצוני). אבד = כל הרישיונות הקיימים
  ממשיכים לעבוד, אבל לא תוכל להנפיק חדשים בלי להחליף גם את המפתח הציבורי
  באפליקציה (מה שיבטל את כל הרישיונות הקיימים).
- **לעולם לא בגיט ולא בהתקנה** (כבר ב-.gitignore).

### הנפקת מפתח ללקוח
```bash
npm run license:issue -- --name "משרד תיווך אולמו" --days 90
npm run license:issue -- --name "לקוח" --email a@b.com --days 365
npm run license:issue -- --name "שותף" --forever
```
הפקודה מדפיסה מפתח (`NFB1.…`) — שלח ללקוח בוואטסאפ/מייל.
הלקוח מדביק ב: **הגדרות ← רישיון ← הפעל**, או במסך החסימה.

כל הנפקה נרשמת ב-`secrets/issued-licenses.jsonl` — כך תדע למי הנפקת ומתי.

### בדיקת מפתח
```bash
npm run license:verify -- NFB1.xxx.yyy
```

### מגבלות שכדאי להכיר
- הרישיון נעול למחשב שבו הופעל (טביעת אצבע מקומית).
- **אין ביטול מרחוק** — מפתח שהונפק תקף עד תאריך התפוגה שקבעת.
  לכן עדיף להנפיק לתקופות קצרות (90 יום) ולחדש, מאשר `--forever`.
- אם תרצה ביטול מרחוק ומעקב מושבים — צריך שרת הפעלה (התשתית מוכנה לזה).


## 1. חתימת קוד דיגיטלית (חובה לפני הפצה)

בלי חתימה, Windows SmartScreen מציג אזהרה אדומה שרוב המשתמשים לא עוברים.

### רכישה (פעולה חד-פעמית, שנתית)
- **תעודת OV** (~$100-200/שנה): Sectigo / Certum / SSL.com. האזהרה נעלמת בהדרגה ככל שמצטבר מוניטין הורדות.
- **תעודת EV** (~$250-400/שנה): מסירה את האזהרה **מיד**. מומלץ למוצר מסחרי. דורשת אימות חברה + טוקן חומרה או ענן (Azure Key Vault / SSL.com eSigner).

### חיבור ל-build (אחרי שיש תעודה)
התשתית כבר מוכנה — electron-builder חותם אוטומטית כשמגדירים משתני סביבה לפני `npm run dist`:

```powershell
# תעודת PFX מקומית:
$env:CSC_LINK = "C:\certs\nf-blaze.pfx"
$env:CSC_KEY_PASSWORD = "סיסמת-התעודה"
npm run dist
```

לתעודות ענן (eSigner / Azure Trusted Signing) — ראו `win.azureSignOptions` או
`signtoolOptions` בתיעוד electron-builder; זה שינוי config בלבד.

אימות אחרי build: לחיצה ימנית על ה-EXE → Properties → Digital Signatures.

## 2. עדכונים אוטומטיים (מחובר ופעיל)

האפליקציה בודקת עדכון 10 שניות אחרי עלייה וכל 4 שעות, מורידה ברקע, ומציעה
«התקן והפעל מחדש». אם אין שרת עדכונים — שקט מוחלט, שום הפרעה למשתמש.

### מה צריך להקים (פעם אחת)
שרת העדכונים מוגדר ב-`package.json → build.publish`:

```json
"publish": { "provider": "generic", "url": "https://updates.nf-digital.net/nf-blaze" }
```

זהו **סטטי לחלוטין** — כל אחסון קבצים עם HTTPS עובד (S3 / Cloudflare R2 / nginx / אפילו GitHub Pages).

### פרסום גרסה חדשה
1. העלה גרסה ב-`package.json` + עדכן `CHANGELOG.md`
2. `npm run dist` (עם משתני החתימה)
3. העלה לשרת העדכונים את **שלושת** הקבצים מ-`release/`:
   - `NF-Blaze-Setup-<version>.exe`
   - `NF-Blaze-Setup-<version>.exe.blockmap` (עדכון דלתא — מוריד רק את ההבדל)
   - `latest.yml` (המפתח — האפליקציות בודקות מולו)

לחלופין, מעבר ל-GitHub Releases: שנה את `publish` ל-
`{ "provider": "github", "owner": "<שם>", "repo": "<ריפו>" }` והרץ `npm run dist:publish` עם `GH_TOKEN`.

**חשוב:** עדכונים אוטומטיים דורשים שהגרסאות יהיו חתומות באותה תעודה.

## 3. Node מצורף (בוצע)

`resources/runtime/` מכיל Node v22.14.0 + npm פורטבילי (~98MB) — נארז אוטומטית
להתקנה, ומשתמשי קצה בלי Node מותקן עובדים מהרגע הראשון. התיקייה לא נכנסת ל-git
(`.gitignore`); בשכפול נקי של הריפו יש להוריד שוב לפי `resources/runtime/README.md`.

## 4. QA ידני על מחשב נקי — צ'קליסט לפני כל שחרור

מכונה נקייה = VM של Windows בלי Node, בלי כלי פיתוח.

- [ ] ההתקנה רצה בלי אזהרת SmartScreen (אחרי חתימה) ומסתיימת
- [ ] האפליקציה נפתחת; מסך הבית עם האנימציה
- [ ] הגדרות → הוספת מפתח API (אחד לפחות) → «מפתח מוגדר»
- [ ] פרויקט חדש מתבנית web-app → התקנת תלויות מסתיימת → תצוגה חיה עולה
      (זה מאמת שה-Node המצורף עובד!)
- [ ] בקשת בנייה קצרה בצ'אט («בנה דף נחיתה ל-X») → הסוכן כותב קבצים → תצוגה מתעדכנת
- [ ] «הצג שינויים» מציג diff; «בטל שינויים» משחזר; היסטוריה עובדת
- [ ] טרמינל תחתון: הקלדת `npm run build` רצה ומציגה פלט
- [ ] תצוגת מכשירים: מעבר ל-iPhone 15 ובחזרה למחשב
- [ ] בחירת אלמנט בתצוגה + הוראה («שנה את הכפתור הזה») עובדת
- [ ] ייבוא פרויקט קיים (תיקיית React כלשהי) נפתח ועובד
- [ ] יציאה מהאפליקציה — אין תהליכי node יתומים ב-Task Manager

## 5. מומלץ בהמשך (לא חוסם)
- דיווח קריסות (Sentry / קובץ לוג מקומי שנשלח ידנית)
- מחיקת `sandbox/` והמנוע הישן (`agentEngine: 'legacy'`)
- עמוד הורדה + מדיניות פרטיות/תנאים
- גרסת macOS (`electron-builder --mac`, דורש חתימת Apple + notarization)
