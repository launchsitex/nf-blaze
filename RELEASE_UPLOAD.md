# פרסום גרסה חדשה — נוהל העלאה

כל גרסה חדשה שמועלית לאחסון **חוסמת אוטומטית** את כל הלקוחות עד שיעדכנו.
לכן הסדר כאן חשוב.

## פעם אחת — הכנת התיקייה באחסון

באחסון של Hostinger, לצד קובצי האתר:

```
public_html/
├── index.html          האתר
├── guides/
└── download/           ← ליצור את זה
```

## בכל גרסה

### 1. בונים

```bash
npm run release
```

זה מריץ `npm run dist` ואז מרכז לתיקייה `release/upload/` בדיוק את מה שצריך
לעלות. לגרסה שהיא **המלצה ולא חובה**:

```bash
npm run release:optional
```

הסקריפט גם **מעדכן לבד את קישור ההורדה** ב-`landing/config.js` ומדפיס
בסוף את רשימת מה שצריך לעלות.

### 2. מעלים — הסדר קריטי

מעלים את התוכן של `release/upload/` אל `public_html/download/`,
**בשני שלבים**:

| שלב | קבצים | למה |
|-----|--------|-----|
| **ראשון** | `NF-Blaze-Setup-<ver>.exe` · `.exe.blockmap` · `.htaccess` | הקובץ הכבד (‏~215MB) — לוקח זמן |
| **אחרון** | `latest.yml` · `version.json` | אלה מה שמכריז על הגרסה |

⚠️ **אם תעלה את `version.json` לפני ה-exe** — לקוחות יקבלו חסימה על גרסה
שהקובץ שלה עדיין באמצע העלאה, ויתקעו עד שהיא תסתיים.

### 2ב. מעלים גם את `config.js` של האתר

`npm run release` עדכן אותו עם הקישור לגרסה החדשה. מעלים אותו ל-
`public_html/config.js`.

> **למה זה לא קריטי אבל כן חשוב:** כפתור ההורדה באתר קורא בזמן ריצה את
> `/download/version.json` ובונה ממנו את הקישור, ולכן הוא מתעדכן לבד גם
> בלי הצעד הזה. ה-`downloadUrl` ב-`config.js` הוא **גיבוי** למקרה
> שהמניפסט לא נטען — ואם הוא יישאר ישן, מבקר כזה יקבל קישור לגרסה קודמת.

### 3. מוודאים

```bash
irm https://nf-blaze.dev/download/version.json
```

צריך להחזיר את המספר החדש. אם מוחזרת גרסה ישנה — האחסון מגיש ממטמון;
ה-`.htaccess` בחבילה אמור למנוע את זה, ודא שהוא עלה.

> ⚠️ ב-PowerShell ‏`curl` הוא **כינוי ל-`Invoke-WebRequest`** ואינו מקבל
> `-s`. הוא יבקש ממך `Uri:` ויתקע. להשתמש ב-`irm`, או ב-`curl.exe`
> (עם הסיומת) אם רוצים curl אמיתי.

בדיקה שהקובץ עצמו נגיש ובגודל הנכון:

```bash
(iwr https://nf-blaze.dev/download/NF-Blaze-Setup-1.64.0.exe -Method Head).Headers['Content-Length']
```

## מה קורה אצל הלקוח

1. תוך עד 10 דקות (או מיד כשהוא חוזר לחלון) האפליקציה מזהה גרסה חדשה.
2. ההורדה מתחילה ברקע, ומוצג שער חוסם עם התקדמות.
3. לחיצה על «עדכן גרסה והפעל מחדש» מתקינה ומפעילה מחדש.
4. אחרי העלייה מוצג פופ-אפ «מה חדש» עם השינויים.

הפרויקטים, ההגדרות והמפתחות של המשתמש נשמרים — ההתקנה היא עדכון במקום.

## אם גרסה יצאה תקולה

לא צריך לבנות גרסה מתקנת כדי לשחרר לקוחות. ערוך ישירות באחסון את
`download/version.json`:

```json
{ "version": "1.64.0", "mandatory": false }
```

בבדיקה הבאה (עד 10 דקות) החסימה משתחררת אצל כולם.

לחייב רק חלק מהלקוחות — למשל רק את מי שמתחת ל-1.63.0:

```json
{ "version": "1.64.0", "mandatory": false, "minVersion": "1.63.0" }
```

## מה **לא** יכול לקרות

- **אחסון שנפל לא נועל אף אחד.** מניפסט שלא נגיש, פגום או ריק מתפרש
  כ«אין עדכון» והמשתמש ממשיך לעבוד. חסימה מתרחשת רק על תשובה תקינה
  שאומרת במפורש שהגרסה לא נתמכת.
- **לקוח לא נשאר תקוע.** אחרי שני כשלונות הורדה מוצע לו קישור להורדה
  ידנית מהאתר.

## תזכורת

לפני שמעלים — לעדכן את `src/shared/release_notes.ts` עם השינויים שהלקוח
יראה. **בלשון תועלת, בלי לחשוף פגמים או מבנה פנימי** (הכלל המלא בראש
`CHANGELOG.md`). יש בדיקה אוטומטית שנכשלת אם נכנס לשם מידע שאסור.
