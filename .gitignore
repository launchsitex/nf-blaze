node_modules
dist
out
release
*.log
.DS_Store
Thumbs.db
.env
.env.*
templates/*/node_modules
templates/*/dist
*.tsbuildinfo

# Bundled portable Node runtime (~98MB) — downloaded at release time, not tracked
resources/runtime/*
!resources/runtime/README.md

# Chromium להרצת בדיקות דפדפן (~270MB) — מועתק מ-ms-playwright לפני build
resources/playwright/

# מפתח הרישוי הפרטי — לעולם לא בגיט ולא בהתקנה. גבה אותו בכספת!
secrets/

# קבצי עבודה של Supabase CLI — משתנים בכל פקודה ואין בהם ערך היסטורי
license-portal/supabase/.temp/
